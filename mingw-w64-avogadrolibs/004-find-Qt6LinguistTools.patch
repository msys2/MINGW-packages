--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -85,7 +85,7 @@
 endif()
 
 if(ENABLE_TRANSLATIONS)
-  find_package(Qt5LinguistTools)
+  find_package(Qt${QT_VERSION}LinguistTools)
 endif()
 
 # SKBUILD is set for binary wheel
