--- dosbox-staging-0.83.0/src/misc/cross.h.orig	2026-08-26 14:24:41.000000000 +0200
+++ dosbox-staging-0.83.0/src/misc/cross.h	2026-08-31 08:41:17.946484500 +0200
@@ -46,7 +46,7 @@
 #endif
 
 /* Large file support */
-#if defined(_MSC_VER)
+#if defined(WIN32)
 	// MSVC doesn't support the posix fstream functions,
 	// typedef their equivalents
 	#define cross_ftello _ftelli64
@@ -69,7 +69,7 @@
 // using (platform-specific) _fileno. On other platforms we can use fileno
 // because it's either a POSIX-compliant system, or the function is available
 // when compiling with GNU extensions.
-#if defined (_MSC_VER)
+#if defined (WIN32)
 #define cross_fileno(s) _fileno(s)
 #else
 #define cross_fileno(s) fileno(s)
--- dosbox-staging-0.83.0/CMakeLists.txt.orig	2026-08-26 14:24:41.000000000 +0200
+++ dosbox-staging-0.83.0/CMakeLists.txt	2026-08-31 08:41:17.946484500 +0200
@@ -551,0 +552,6 @@
+if (MINGW)
+  target_link_libraries(dosbox PRIVATE
+    ws2_32
+    winmm
+  )
+endif()
