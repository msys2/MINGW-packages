--- src/CMakeLists.txt.orig	2026-03-06 21:08:35.825978100 -0600
+++ src/CMakeLists.txt	2026-03-09 12:31:34.995706100 -0500
@@ -1387,9 +1387,11 @@
 
     if(BUILD_SHARED_LIBS)
         target_sources(zlib-ng PRIVATE ${ZLIB_DLL_SRCS})
+        set(lib_type "shared")
     else()
         add_library(zlib-ng-static ALIAS zlib-ng)
         add_library(zlib-ng::zlibstatic ALIAS zlib-ng)
+        set(lib_type "static")
     endif()
 
     set(ZLIB_INSTALL_LIBRARIES zlib-ng)
@@ -1540,7 +1542,7 @@
 
 if(NOT SKIP_INSTALL_LIBRARIES AND NOT SKIP_INSTALL_ALL)
     install(TARGETS ${ZLIB_INSTALL_LIBRARIES}
-        EXPORT ${EXPORT_NAME}-targets
+        EXPORT ${EXPORT_NAME}-${lib_type}-targets
         RUNTIME DESTINATION "${CMAKE_INSTALL_BINDIR}"
         ARCHIVE DESTINATION "${CMAKE_INSTALL_LIBDIR}"
         LIBRARY DESTINATION "${CMAKE_INSTALL_LIBDIR}")
@@ -1555,7 +1557,7 @@
 endif()
 if(NOT SKIP_INSTALL_FILES AND NOT SKIP_INSTALL_ALL)
     install(FILES ${ZLIB_PC} DESTINATION "${PKGCONFIG_INSTALL_DIR}")
-    install(EXPORT ${EXPORT_NAME}-targets
+    install(EXPORT ${EXPORT_NAME}-${lib_type}-targets
         DESTINATION "${CMAKE_INSTALL_LIBDIR}/cmake/${EXPORT_NAME}"
         NAMESPACE ${EXPORT_NAME}::)
     # Use GNU-style variable names
--- src/zlib-config.cmake.in.orig 2026-02-03 15:10:03.000000000 -0600
+++ src/zlib-config.cmake.in      2026-03-06 22:22:04.446473400 -0600
@@ -7,6 +7,10 @@
 set_and_check(ZLIB_LIB_DIR "@PACKAGE_LIB_INSTALL_DIR@")
 set(ZLIB_LIBRARIES ZLIB::ZLIB)

-include("${CMAKE_CURRENT_LIST_DIR}/ZLIB-targets.cmake")
+if(ZLIB_USE_STATIC_LIBS OR zlib-ng_USE_STATIC_LIBS)
+  include("${CMAKE_CURRENT_LIST_DIR}/ZLIB-static-targets.cmake")
+else()
+  include("${CMAKE_CURRENT_LIST_DIR}/ZLIB-shared-targets.cmake")
+endif()

 check_required_components(ZLIB)

--- src/zlib-ng-config.cmake.in.orig      2026-02-03 15:10:03.000000000 -0600
+++ src/zlib-ng-config.cmake.in   2026-03-06 22:18:41.848224200 -0600
@@ -5,6 +5,10 @@
 set_and_check(zlib-ng_INCLUDE_DIR "@PACKAGE_INCLUDE_INSTALL_DIR@")
 set_and_check(zlib-ng_LIB_DIR "@PACKAGE_LIB_INSTALL_DIR@")

-include("${CMAKE_CURRENT_LIST_DIR}/zlib-ng-targets.cmake")
+if(ZLIB_USE_STATIC_LIBS OR zlib-ng_USE_STATIC_LIBS)
+  include("${CMAKE_CURRENT_LIST_DIR}/zlib-ng-static-targets.cmake")
+else()
+  include("${CMAKE_CURRENT_LIST_DIR}/zlib-ng-shared-targets.cmake")
+endif()

 check_required_components(zlib-ng)

