# Contributor: Mehdi Chinoune <mehdi.chinoune@hotmail.com>

_perlname=Tie-Cycle
_realname="${_perlname,,}"
pkgbase=mingw-w64-perl-${_realname}
pkgname=("${MINGW_PACKAGE_PREFIX}-perl-${_realname}")
pkgver=1.233
pkgrel=1
pkgdesc="Cycle through a list of values via a scalar. (mingw-w64)"
arch=('any')
mingw_arch=('ucrt64' 'clang64' 'clangarm64')
url="https://metacpan.org/dist/Tie-Cycle"
msys2_repository_url="https://github.com/briandfoy/tie-cycle"
msys2_references=(
  'purl: pkg:cpan/BRIANDFOY/Tie-Cycle'
)
license=('spdx:Artistic-2.0')
groups=("${MINGW_PACKAGE_PREFIX}-perl-modules")
depends=("${MINGW_PACKAGE_PREFIX}-perl")
makedepends=("${MINGW_PACKAGE_PREFIX}-make")
options=('!emptydirs')
source=("https://cpan.metacpan.org/authors/id/B/BR/BRIANDFOY/${_perlname}-${pkgver}.tar.gz")
sha256sums=('043d0bef0afba404eaff236a400a17265cbb609aa2112743212e1f9ee29039f1')

build() {
  cp -r ${_perlname}-${pkgver} build-${MSYSTEM} && cd build-${MSYSTEM}
  MSYS2_ARG_CONV_EXCL="PREFIX=" \
  PERL_MM_USE_DEFAULT=1 \
    perl Makefile.PL \
    PREFIX=${MINGW_PREFIX}
  mingw32-make
}

package() {
  cd build-${MSYSTEM}
  mingw32-make DESTDIR="${pkgdir}/" install
  install -Dm644 LICENSE "${pkgdir}"${MINGW_PREFIX}/share/licenses/perl-${_realname}/LICENSE
}
