post_install() {
  local _prefix="/mingw32"
  local _prefix_win="$(cygpath -m "${_prefix}")"
  # Fix prefix using local path
  sed -s "s|${_prefix}|${_prefix_win}|g" -i "${_prefix}/bin/wx-config-3.1"
}

post_upgrade() {
  post_install
}
