post_install() {
  cd mingw64
  local _prefix=$(pwd -W)
  cd -
  local _it
  for _it in cm2html cm2latex cm2man cm2pseudoxml cm2xetex cm2xml; do
    sed -e "s|/mingw64|${_prefix}|g" \
        -i ${_prefix}/bin/${_it}-script.py
  done
}

post_upgrade() {
  post_install
}
