post_install() {
  cd mingw32
  local _prefix=$(pwd -W)
  cd -
  local _it
  for _it in cm2html2 cm2latex2 cm2man2 cm2pseudoxml2 cm2xetex2 cm2xml2; do
    sed -e "s|/mingw32|${_prefix}|g" \
        -i ${_prefix}/bin/${_it}-script.py
  done
}

post_upgrade() {
  post_install
}
