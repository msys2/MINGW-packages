--- src/ocl-icd-2.3.2/configure.ac.old	2024-11-23 12:39:38.043971600 +0100
+++ src/ocl-icd-2.3.2/configure.ac	2024-11-23 12:40:20.992009400 +0100
@@ -195,12 +195,13 @@
   AC_MSG_NOTICE([using an unofficial custom default layers directory ($OCL_ICD_LAYERDIR)])
 ])
 
-# always use versionned symbols (check required for MacOSX)
-if test "x$macos" == xyes ; then
-AM_CONDITIONAL([USE_MAP], [false])
-else
-AM_CONDITIONAL([USE_MAP], [true])
-fi
+# whether to use versionned symbols (check required for MacOSX)
+AC_ARG_ENABLE([map],
+    AS_HELP_STRING([--enable-map], [Use versioned symbols]))
+AS_IF([test "x$enable_map" != "xno" && test "x$macos" != "xyes"],
+    [use_map=true],
+    [use_map=false])
+AM_CONDITIONAL([USE_MAP], [test x$use_map = xtrue])
 
 AC_CONFIG_FILES([
   Makefile
