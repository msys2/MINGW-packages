--- src/plugins/perl/meson.build.orig	2026-04-20 12:56:03.000000000 -0500
+++ src/plugins/perl/meson.build	2026-04-25 23:58:30.064005500 -0500
@@ -34,7 +34,7 @@
 endif
 foreach flag : ret.stdout().strip().split(' ')
   if flag.startswith('-I') or flag.startswith('-D')
-    perl_cflags += flag
+    perl_cflags += flag.replace('"', '')
   endif
 endforeach
 
@@ -45,6 +45,10 @@
 perl_ldflags = []
 perl_rpath = ''
 foreach flag : ret.stdout().strip().split(' ')
+  if flag.endswith('.a"')
+    perl_ldflags += flag.replace('"', '')
+    continue
+  endif
   if flag.startswith('/') and '/libperl.' in flag
     split = flag.split('/')
     libperl_dir = ''
@@ -62,7 +66,7 @@
     continue
   endif
   if flag.startswith('-L') or flag.startswith('-l')
-    perl_ldflags += flag
+    perl_ldflags += flag.replace('"', '')
   endif
   if flag.startswith('-Wl,-rpath,')
     # Install rpath
