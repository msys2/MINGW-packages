diff --git a/Cargo.toml b/Cargo.toml
index ce400648d..800a5bd8e 100644
--- a/Cargo.toml
+++ b/Cargo.toml
@@ -2,6 +2,7 @@
 lto = true
 codegen-units = 1
 panic = "abort"
 
 [workspace]
 resolver = "3"
diff --git a/build-aux/windows/papers.rc b/build-aux/windows/papers.rc
new file mode 100644
index 000000000..d38eed980
--- /dev/null
+++ b/build-aux/windows/papers.rc
@@ -0,0 +1 @@
+1 ICON "papers.ico"
diff --git a/libdocument/backend/pdf/pps-poppler.c b/libdocument/backend/pdf/pps-poppler.c
index 72d3af507..c09e94e0d 100644
--- a/libdocument/backend/pdf/pps-poppler.c
+++ b/libdocument/backend/pdf/pps-poppler.c
@@ -12,6 +12,10 @@
 #include <gtk/gtk.h>
 #include <poppler.h>
 #include <string.h>
+#ifdef G_OS_WIN32
+#include <io.h>
+#endif
+#include <unistd.h>
 #ifdef HAVE_CAIRO_PDF
 #include <cairo-pdf.h>
 #endif
@@ -309,6 +313,12 @@ pdf_document_load_fd (PpsDocument *document,
                       int fd,
                       GError **error)
 {
+#ifdef G_OS_WIN32
+	g_set_error_literal (error, G_IO_ERROR, G_IO_ERROR_NOT_SUPPORTED,
+	                     "File descriptor support is not available on Windows");
+	close (fd);
+	return FALSE;
+#else
 	GError *err = NULL;
 	PdfDocument *self = PDF_DOCUMENT (document);
 	gboolean success;
@@ -331,6 +341,7 @@ pdf_document_load_fd (PpsDocument *document,
 	g_rw_lock_writer_unlock (&self->rwlock);
 
 	return success;
+#endif
 }
 
 static int
diff --git a/libdocument/meson.build b/libdocument/meson.build
index 86f7457af..1c70e7594 100644
--- a/libdocument/meson.build
+++ b/libdocument/meson.build
@@ -143,6 +143,7 @@ libdocument_deps = [
 cflags = [
   '-DG_LOG_DOMAIN="PapersDocument"',
   '-DPPS_BACKENDSDIR="@0@"'.format(pps_backendsdir),
+  '-DPPS_BACKENDS_SUBDIR="@0@"'.format(pps_backends_subdir),
   '-DPPS_BACKENDSBINARYVERSION="@0@"'.format(pps_backends_binary_version),
   '-DPAPERS_COMPILATION',
 ]
diff --git a/libdocument/pps-document-factory.c b/libdocument/pps-document-factory.c
index e082476bb..d8729fb41 100644
--- a/libdocument/pps-document-factory.c
+++ b/libdocument/pps-document-factory.c
@@ -230,6 +230,15 @@ _pps_document_factory_init (void)
 	if (g_getenv ("PPS_BACKENDS_DIR") != NULL)
 		pps_backends_dir = g_strdup (g_getenv ("PPS_BACKENDS_DIR"));
 
+#ifdef G_OS_WIN32
+	if (!pps_backends_dir) {
+		g_autofree gchar *inst_dir = g_win32_get_package_installation_directory_of_module (NULL);
+		if (inst_dir) {
+			pps_backends_dir = g_build_filename (inst_dir, "lib", PPS_BACKENDS_SUBDIR, NULL);
+		}
+	}
+#endif
+
 	if (!pps_backends_dir) {
 		pps_backends_dir = g_strdup (PPS_BACKENDSDIR);
 	}
@@ -383,7 +392,7 @@ void
 pps_document_factory_add_filters (GtkFileDialog *dialog, PpsDocument *document)
 {
 	GtkFileFilter *all_documents_filter, *default_filter, *all_files_filter;
-	GListStore *filters = g_list_store_new (GTK_TYPE_FILTER);
+	GListStore *filters = g_list_store_new (GTK_TYPE_FILE_FILTER);
 
 	g_return_if_fail (GTK_IS_FILE_DIALOG (dialog));
 	g_return_if_fail (document == NULL || PPS_IS_DOCUMENT (document));
diff --git a/libdocument/pps-document.c b/libdocument/pps-document.c
index 827ad2edf..758d2f1fb 100644
--- a/libdocument/pps-document.c
+++ b/libdocument/pps-document.c
@@ -411,6 +411,12 @@ pps_document_load_fd (PpsDocument *document,
 		return FALSE;
 	}
 
+#ifdef G_OS_WIN32
+	g_set_error_literal (error, G_IO_ERROR, G_IO_ERROR_NOT_SUPPORTED,
+	                     "Loading from file descriptor is not supported on Windows");
+	close (fd);
+	return FALSE;
+#else
 	if (fstat (fd, &statbuf) == -1 ||
 	    (fd_flags = fcntl (fd, F_GETFL, NULL)) == -1) {
 		int errsv = errno;
@@ -446,6 +452,7 @@ pps_document_load_fd (PpsDocument *document,
 	priv->n_pages = klass->get_n_pages (document);
 
 	return TRUE;
+#endif
 }
 
 /**
diff --git a/libdocument/pps-file-helpers.c b/libdocument/pps-file-helpers.c
index 2a1cff2c6..1bf8e66ea 100644
--- a/libdocument/pps-file-helpers.c
+++ b/libdocument/pps-file-helpers.c
@@ -12,6 +12,12 @@
 #include <sys/types.h>
 #include <unistd.h>
 
+#ifdef _WIN32
+#ifndef O_CLOEXEC
+#define O_CLOEXEC 0
+#endif
+#endif
+
 #include <glib.h>
 #include <glib/gi18n-lib.h>
 #include <glib/gstdio.h>
@@ -276,6 +282,30 @@ pps_xfer_uri_simple (const char *from,
 	                          G_FILE_COPY_OVERWRITE,
 	                      NULL, NULL, NULL, error);
 
+#ifdef G_OS_WIN32
+	if (!result && error && *error) {
+		/* On Windows, if target_file is open by Poppler/process, g_file_copy fails
+		 * with G_IO_ERROR_PERMISSION_DENIED due to atomic rename over open handles.
+		 * Fall back to opening target in-place, truncating to 0, and splicing. */
+		g_clear_error (error);
+		g_autoptr (GFileInputStream) in_stream = g_file_read (source_file, NULL, error);
+		if (in_stream) {
+			g_autoptr (GFileIOStream) io_stream = g_file_open_readwrite (target_file, NULL, error);
+			if (io_stream) {
+				GOutputStream *out_stream = g_io_stream_get_output_stream (G_IO_STREAM (io_stream));
+				if (g_seekable_truncate (G_SEEKABLE (out_stream), 0, NULL, error)) {
+					gssize spliced = g_output_stream_splice (out_stream,
+					                                         G_INPUT_STREAM (in_stream),
+					                                         G_OUTPUT_STREAM_SPLICE_CLOSE_SOURCE |
+					                                             G_OUTPUT_STREAM_SPLICE_CLOSE_TARGET,
+					                                         NULL, error);
+					result = (spliced >= 0);
+				}
+			}
+		}
+	}
+#endif
+
 	return result;
 }
 
@@ -295,6 +325,10 @@ pps_file_copy_metadata (const char *from,
                         const char *to,
                         GError **error)
 {
+#ifdef G_OS_WIN32
+	/* Windows NTFS does not support POSIX extended attributes / SELinux contexts */
+	return TRUE;
+#else
 	g_autoptr (GFile) source_file = NULL;
 	g_autoptr (GFile) target_file = NULL;
 	gboolean result;
@@ -311,6 +345,7 @@ pps_file_copy_metadata (const char *from,
 	                                 NULL, error);
 
 	return result;
+#endif
 }
 
 static gchar *
@@ -489,6 +524,7 @@ static const char *compressor_cmds[] = {
 static void
 compression_child_setup_cb (gpointer fd_ptr)
 {
+#ifndef G_OS_WIN32
 	int fd = GPOINTER_TO_INT (fd_ptr);
 	int flags;
 
@@ -497,6 +533,7 @@ compression_child_setup_cb (gpointer fd_ptr)
 		flags &= ~FD_CLOEXEC;
 		fcntl (fd, F_SETFD, flags);
 	}
+#endif
 }
 
 static gchar *
@@ -550,10 +587,17 @@ compression_run (const gchar *uri,
 		GIOStatus read_st, write_st;
 		gsize bytes_read, bytes_written;
 
+#ifdef G_OS_WIN32
+		in = g_io_channel_win32_new_fd (pout);
+		g_io_channel_set_encoding (in, NULL, NULL);
+		out = g_io_channel_win32_new_fd (fd);
+		g_io_channel_set_encoding (out, NULL, NULL);
+#else
 		in = g_io_channel_unix_new (pout);
 		g_io_channel_set_encoding (in, NULL, NULL);
 		out = g_io_channel_unix_new (fd);
 		g_io_channel_set_encoding (out, NULL, NULL);
+#endif
 
 		do {
 			read_st = g_io_channel_read_chars (in, buf,
diff --git a/libdocument/pps-init.c b/libdocument/pps-init.c
index e3aca71bb..c12f19b3f 100644
--- a/libdocument/pps-init.c
+++ b/libdocument/pps-init.c
@@ -9,7 +9,13 @@
 #include <glib.h>
 #include <glib/gi18n-lib.h>
 
+#ifdef HAVE_EXEMPI
 #include <exempi/xmp.h>
+#endif
+
+#ifdef G_OS_WIN32
+#include <windows.h>
+#endif
 
 #include "pps-document-factory.h"
 #include "pps-file-helpers.h"
@@ -36,11 +42,24 @@ pps_init (void)
 	if (pps_init_count++ > 0)
 		return have_backends;
 
+#ifdef G_OS_WIN32
+	wchar_t module_path[MAX_PATH];
+	if (GetModuleFileNameW (NULL, module_path, MAX_PATH)) {
+		wchar_t *last_slash = wcsrchr (module_path, L'\\');
+		if (last_slash) {
+			*last_slash = L'\0';
+			SetDllDirectoryW (module_path);
+		}
+	}
+#endif
+
 	/* set up translation catalog */
 	bindtextdomain (GETTEXT_PACKAGE, PPS_LOCALEDIR);
 	bind_textdomain_codeset (GETTEXT_PACKAGE, "UTF-8");
 
+#ifdef HAVE_EXEMPI
 	xmp_init ();
+#endif
 	gdk_pixbuf_init_modules (EXTRA_GDK_PIXBUF_LOADERS_DIR, NULL);
 	_pps_file_helpers_init ();
 	have_backends = _pps_document_factory_init ();
@@ -61,7 +80,9 @@ pps_shutdown (void)
 	if (--pps_init_count > 0)
 		return;
 
+#ifdef HAVE_EXEMPI
 	xmp_terminate ();
+#endif
 	_pps_document_factory_shutdown ();
 	_pps_file_helpers_shutdown ();
 }
diff --git a/libdocument/pps-xmp.c b/libdocument/pps-xmp.c
index 5a948f852..0ed3b6852 100644
--- a/libdocument/pps-xmp.c
+++ b/libdocument/pps-xmp.c
@@ -14,9 +14,12 @@
 #include <glib/gi18n-lib.h>
 #include <pango/pango.h>
 
+#ifdef HAVE_EXEMPI
 #include <exempi/xmp.h>
 #include <exempi/xmpconsts.h>
+#endif
 
+#ifdef HAVE_EXEMPI
 #define NS_PDFA_ID "http://www.aiim.org/pdfa/ns/id/"
 #define NS_PDFX_ID "http://www.npes.org/pdfx/ns/id/"
 #define NS_PDFX "http://ns.adobe.com/pdfx/1.3/"
@@ -224,6 +227,7 @@ pps_xmp_get_license (XmpPtr xmp)
 
 	return license;
 }
+#endif
 
 /**
  * pps_xmp_parse:
@@ -240,6 +244,7 @@ pps_xmp_parse (const char *metadata,
                gsize size,
                PpsDocumentInfo *info)
 {
+#ifdef HAVE_EXEMPI
 	gchar *fmt;
 	gchar *author;
 	gchar *keywords;
@@ -337,4 +342,7 @@ pps_xmp_parse (const char *metadata,
 	xmp_free (xmp);
 
 	return TRUE;
+#else
+	return FALSE;
+#endif
 }
diff --git a/libview/meson.build b/libview/meson.build
index 1a3182aac..5d93c3507 100644
--- a/libview/meson.build
+++ b/libview/meson.build
@@ -141,6 +141,7 @@ blueprints = custom_target(
   'blueprints',
   input: blueprint_sources,
   output: blueprint_output,
+  env: blueprint_env,
   command: [
     blueprint_compiler,
     'batch-compile',
@@ -158,7 +159,6 @@ sources += gnome.compile_resources(
   dependencies: blueprints,
 )
 
-
 libppsview = shared_library(
   'ppsview-' + pps_api_version,
   version: pps_view_version,
diff --git a/libview/pps-jobs.c b/libview/pps-jobs.c
index 13f87bd4b..b7493f951 100644
--- a/libview/pps-jobs.c
+++ b/libview/pps-jobs.c
@@ -798,6 +798,11 @@ static int
 pps_dupfd (int fd,
            GError **error)
 {
+#ifdef G_OS_WIN32
+	g_set_error_literal (error, G_IO_ERROR, G_IO_ERROR_NOT_SUPPORTED,
+	                     "File descriptor support is not available on Windows");
+	return -1;
+#else
 	int new_fd;
 
 	new_fd = fcntl (fd, F_DUPFD_CLOEXEC, 3);
@@ -808,6 +813,7 @@ pps_dupfd (int fd,
 	}
 
 	return new_fd;
+#endif
 }
 
 static void
diff --git a/meson.build b/meson.build
index 0c27d7e2a..0b93451c4 100644
--- a/meson.build
+++ b/meson.build
@@ -166,7 +166,8 @@ libaw_dep = dependency(
     'tests=false',
     'examples=false',
   ])
-exempi_dep = dependency('exempi-2.0', version: exempi_req_version)
+exempi_dep = dependency('exempi-2.0', version: exempi_req_version, required: false)
+config_h.set('HAVE_EXEMPI', exempi_dep.found())
 
 m_dep = cc.find_library('m')
 
@@ -178,14 +179,14 @@ assert(zlib_dep.found() and cc.has_function('inflate', dependencies: zlib_dep) a
       'No sufficient zlib library found on your system')
 
 # *** Nautilus property page build ***
-enable_nautilus = get_option('nautilus')
+enable_nautilus = get_option('nautilus') and host_machine.system() != 'windows'
 if enable_nautilus
   libnautilus_extension_dep = dependency('libnautilus-extension-4', version: ['>= 43'])
   nautilus_extension_dir = libnautilus_extension_dep.get_variable(pkgconfig: 'extensiondir', pkgconfig_define: ['libdir', pps_libdir])
 endif
 
 # *** GNOME Keyring support ***
-enable_keyring = get_option('keyring').enabled()
+enable_keyring = get_option('keyring').allowed() and host_machine.system() != 'windows'
 
 # GKT+ Unix Printing
 gtk_unix_print_dep = dependency('gtk4-unix-print', version: gtk_req_version, required: get_option('gtk_unix_print'))
@@ -207,6 +208,8 @@ endif
 
 # *** Blueprint-Compiler ***
 blueprint_compiler = find_program('blueprint-compiler', version: '>= 0.16')
+blueprint_env = environment()
+blueprint_env.set('PYTHONUTF8', '1')
 
 # *** Appstream CLI ***
 appstreamcli = find_program('appstreamcli', required: true)
@@ -329,7 +332,7 @@ subdir('nautilus')
 subdir('rust')
 
 # Print Previewer
-enable_previewer = get_option('previewer')
+enable_previewer = get_option('previewer') and host_machine.system() != 'windows'
 if enable_previewer
   subdir('previewer')
 endif
@@ -346,7 +349,7 @@ subdir('po')
 subdir('help')
 
 # *** Thumbnailer ***
-enable_thumbnailer = get_option('thumbnailer')
+enable_thumbnailer = get_option('thumbnailer') and host_machine.system() != 'windows'
 if enable_thumbnailer
   subdir('thumbnailer')
 endif
@@ -359,7 +362,7 @@ configure_file(
 gnome.post_install(
   glib_compile_schemas: true,
   gtk_update_icon_cache: true,
-  update_desktop_database: true,
+  update_desktop_database: host_machine.system() != 'windows',
 )
 
 # *** File Tests ***
diff --git a/previewer/meson.build b/previewer/meson.build
index a72943d5e..c901efba2 100644
--- a/previewer/meson.build
+++ b/previewer/meson.build
@@ -21,6 +21,7 @@ blueprints = custom_target(
   'blueprints',
   input: blueprint_sources,
   output: blueprint_output,
+  env: blueprint_env,
   command: [
     blueprint_compiler,
     'batch-compile',
@@ -38,6 +39,17 @@ previewer_sources += gnome.compile_resources(
   dependencies: blueprints,
 )
 
+if host_machine.system() == 'windows'
+  win = import('windows')
+
+  previewer_win_res = win.compile_resources(
+    'papers-previewer.rc',
+    depend_files: files('../shell/resources/papers.ico'),
+  )
+  previewer_sources += previewer_win_res
+endif
+
+
 previewer_deps = [
   libppsdocument_dep,
   libppsview_dep,
diff --git a/previewer/papers-previewer.rc b/previewer/papers-previewer.rc
new file mode 100644
index 000000000..a3080f5ac
--- /dev/null
+++ b/previewer/papers-previewer.rc
@@ -0,0 +1 @@
+1 ICON "../shell/resources/papers.ico"
diff --git a/previewer/pps-previewer-window.c b/previewer/pps-previewer-window.c
index a1439e0dd..7b65f3636 100644
--- a/previewer/pps-previewer-window.c
+++ b/previewer/pps-previewer-window.c
@@ -591,6 +591,11 @@ pps_previewer_window_set_source_fd (PpsPreviewerWindow *window,
                                     int fd,
                                     GError **error)
 {
+#ifdef G_OS_WIN32
+	g_set_error_literal (error, G_IO_ERROR, G_IO_ERROR_NOT_SUPPORTED,
+	                     "File descriptor support is not available on Windows");
+	return FALSE;
+#else
 	PpsPreviewerWindowPrivate *priv = GET_PRIVATE (window);
 
 	int nfd;
@@ -613,4 +618,5 @@ pps_previewer_window_set_source_fd (PpsPreviewerWindow *window,
 	priv->source_fd = fd;
 
 	return TRUE;
+#endif
 }
diff --git a/previewer/pps-previewer.c b/previewer/pps-previewer.c
index 96b2e6735..337941c3a 100644
--- a/previewer/pps-previewer.c
+++ b/previewer/pps-previewer.c
@@ -133,6 +133,13 @@ check_arguments (int argc,
                  char **argv,
                  GError **error)
 {
+#ifdef G_OS_WIN32
+	if (input_fd != -1) {
+		g_set_error_literal (error, G_IO_ERROR, G_IO_ERROR_NOT_SUPPORTED,
+		                     "File descriptor support is not available on Windows");
+		return FALSE;
+	}
+#else
 	if (input_fd != -1) {
 		struct stat statbuf;
 		int flags;
@@ -180,7 +187,9 @@ check_arguments (int argc,
 			                     "Must not specify --unlink-tempfile");
 			return FALSE;
 		}
-	} else {
+	} else
+#endif
+	{
 		char *path;
 
 		if (argc != 2) {
diff --git a/rust/meson.build b/rust/meson.build
index 748e8ef24..28252a5c1 100644
--- a/rust/meson.build
+++ b/rust/meson.build
@@ -1,6 +1,6 @@
 rust_binding_root = meson.current_source_dir()
 
-cargo_fmt = find_program('cargo-fmt', required: get_option('profile') == 'devel')
+cargo_fmt = find_program('cargo-fmt', required: false)
 if gobject_introspection_dep.found() and cargo_fmt.found()
   gir_dir = rust_binding_root / 'gir-files'
 
diff --git a/shell/Cargo.toml b/shell/Cargo.toml
index ee4fa870a..fb9aaafb2 100644
--- a/shell/Cargo.toml
+++ b/shell/Cargo.toml
@@ -42,3 +42,5 @@ optional = true
 package = "libadwaita"
 version = "0.9"
 features = ["v1_7"]
+
+
diff --git a/shell/resources/meson.build b/shell/resources/meson.build
index 16b236d1f..417859f7d 100644
--- a/shell/resources/meson.build
+++ b/shell/resources/meson.build
@@ -39,6 +39,7 @@ blueprints = custom_target(
   'blueprints',
   input: blueprint_sources,
   output: blueprint_output,
+  env: blueprint_env,
   command: [
     blueprint_compiler,
     'batch-compile',
@@ -58,4 +59,5 @@ papers_resources = gnome.compile_resources(
   source_dir: [data_dir, data_build_dir],
   dependencies: [metainfo_file, blueprints],
   gresource_bundle: true,
+
 )[0]
diff --git a/shell/resources/pps-document-view.blp b/shell/resources/pps-document-view.blp
index 6f27cd96a..371262e04 100644
--- a/shell/resources/pps-document-view.blp
+++ b/shell/resources/pps-document-view.blp
@@ -616,7 +616,12 @@ template $PpsDocumentView: Adw.BreakpointBin {
       }
 
       Shortcut {
-        trigger: "<Ctrl>S|<Ctrl><Shift>S";
+        trigger: "<Ctrl>S";
+        action: "action(doc.save)";
+      }
+
+      Shortcut {
+        trigger: "<Ctrl><Shift>S";
         action: "action(doc.save-as)";
       }
 
@@ -867,7 +872,12 @@ menu document_menu {
     }
 
     item {
-      label: _("_Save As…");
+      label: _("_Save");
+      action: "doc.save";
+    }
+
+    item {
+      label: _("Save _As…");
       action: "doc.save-as";
     }
   }
diff --git a/shell/resources/pps-shortcuts-dialog.blp b/shell/resources/pps-shortcuts-dialog.blp
index 2f9ba387e..ec63f4fd5 100644
--- a/shell/resources/pps-shortcuts-dialog.blp
+++ b/shell/resources/pps-shortcuts-dialog.blp
@@ -20,9 +20,14 @@ Adw.ShortcutsDialog shortcuts_dialog {
       accelerator: "<Ctrl><Shift>O";
     }
 
+    Adw.ShortcutsItem {
+      title: C_("shortcut window", "Save Current Document");
+      accelerator: "<Ctrl>S";
+    }
+
     Adw.ShortcutsItem {
       title: C_("shortcut window", "Save Copy of Current Document");
-      accelerator: "<Ctrl>S <Ctrl><Shift>S";
+      accelerator: "<Ctrl><Shift>S";
     }
 
     Adw.ShortcutsItem {
diff --git a/shell/src/application.rs b/shell/src/application.rs
index 34943c3ac..ca1cfb770 100644
--- a/shell/src/application.rs
+++ b/shell/src/application.rs
@@ -476,7 +476,7 @@ impl PpsApplication {
             .property("application-id", APP_ID)
             .property("flags", flags)
             .property("resource-base-path", "/org/gnome/papers")
-            .property("register-session", true)
+            .property("register-session", cfg!(not(target_os = "windows")))
             .build()
     }
 }
@@ -561,12 +561,24 @@ pub fn spawn(file: Option<&gio::File>, dest: Option<&LinkDest>, mode: Option<Win
 
         // MacOS take this path since GAppInfo doesn't support created by
         // command line on MacOS.
-        if let Err(e) = glib::spawn_command_line_async(cmd) {
-            glib::g_printerr!(
-                "Error launching papers {}: {}\n",
-                uri.unwrap_or_default(),
-                e.message()
-            );
+        unsafe {
+            let mut err = std::ptr::null_mut();
+            let cmd_c = std::ffi::CString::new(cmd).unwrap_or_default();
+            if glib::ffi::g_spawn_command_line_async(cmd_c.as_ptr(), &mut err) == glib::ffi::GFALSE
+            {
+                let err_msg = if !err.is_null() {
+                    let msg = std::ffi::CStr::from_ptr((*err).message).to_string_lossy();
+                    glib::ffi::g_error_free(err);
+                    msg
+                } else {
+                    std::borrow::Cow::Borrowed("Unknown error")
+                };
+                glib::g_printerr!(
+                    "Error launching papers {}: {}\n",
+                    uri.unwrap_or_default(),
+                    err_msg
+                );
+            }
         }
     }
 }
diff --git a/shell/src/document_view/actions.rs b/shell/src/document_view/actions.rs
index 504822056..5d4637185 100644
--- a/shell/src/document_view/actions.rs
+++ b/shell/src/document_view/actions.rs
@@ -157,6 +157,15 @@ impl imp::PpsDocumentView {
                 ))
                 .build(),
             // Document related actions
+            gio::ActionEntryBuilder::new("save")
+                .activate(glib::clone!(
+                    #[weak(rename_to = obj)]
+                    self,
+                    move |_, _, _| {
+                        obj.cmd_save();
+                    }
+                ))
+                .build(),
             gio::ActionEntryBuilder::new("save-as")
                 .activate(glib::clone!(
                     #[weak(rename_to = obj)]
@@ -1012,6 +1021,10 @@ impl imp::PpsDocumentView {
         self.rotate(90);
     }
 
+    fn cmd_save(&self) {
+        self.save();
+    }
+
     fn cmd_save_as(&self) {
         self.save_as();
     }
diff --git a/shell/src/document_view/io.rs b/shell/src/document_view/io.rs
index f6dc19dec..01754d3bd 100644
--- a/shell/src/document_view/io.rs
+++ b/shell/src/document_view/io.rs
@@ -364,6 +364,71 @@ impl imp::PpsDocumentView {
         self.obj().native().and_downcast::<gtk::Window>().unwrap()
     }
 
+    pub(super) fn save(&self) {
+        if let Some(file) = self.file.borrow().clone() {
+            self.execute_save(&file);
+        } else {
+            self.save_as();
+        }
+    }
+
+    pub(super) fn execute_save(&self, file: &gio::File) {
+        self.file_dialog_save_folder(Some(file), UserDirectory::Documents);
+
+        self.clear_save_job();
+
+        let document = self.document().unwrap();
+        let uri = file.uri();
+        let document_uri = self.file.borrow().as_ref().unwrap().uri();
+
+        let save_job = papers_view::JobSave::new(&document, &uri, &document_uri);
+
+        let id = save_job.connect_finished(glib::clone!(
+            #[weak(rename_to = obj)]
+            self,
+            move |job| {
+                match job.is_succeeded() {
+                    Err(e) => {
+                        obj.close_after_save.set(false);
+                        obj.error_message(
+                            Some(&e),
+                            &formatx!(
+                                gettext("The file could not be saved as “{}”."),
+                                job.uri().unwrap_or_default(),
+                            )
+                            .expect("Wrong format in translated string"),
+                        );
+                    }
+                    Ok(_) => {
+                        if let Some(uri) = job.uri() {
+                            gtk::RecentManager::default().add_item(&uri);
+                        }
+                        obj.modified.set(false);
+                        obj.update_title();
+                    }
+                }
+
+                obj.clear_save_job();
+
+                if obj.close_after_save.get() {
+                    glib::idle_add_local_once(glib::clone!(
+                        #[weak]
+                        obj,
+                        move || {
+                            obj.parent_window().destroy();
+                        }
+                    ));
+                }
+            }
+        ));
+
+        self.save_job.replace(Some(save_job.clone()));
+        self.save_job_handler.replace(Some(id));
+
+        // The priority doesn't matter for this job
+        save_job.scheduler_push_job(JobPriority::PriorityNone);
+    }
+
     pub(super) fn save_as(&self) {
         let dialog = gtk::FileDialog::builder()
             .title(gettext("Save As…"))
@@ -383,58 +448,7 @@ impl imp::PpsDocumentView {
                 match result {
                     Err(_) => obj.close_after_save.set(false),
                     Ok(file) => {
-                        obj.file_dialog_save_folder(Some(&file), UserDirectory::Documents);
-
-                        obj.clear_save_job();
-
-                        let document = obj.document().unwrap();
-                        let uri = file.uri();
-                        let document_uri = obj.file.borrow().as_ref().unwrap().uri();
-
-                        let save_job = papers_view::JobSave::new(&document, &uri, &document_uri);
-
-                        let id = save_job.connect_finished(glib::clone!(
-                            #[weak]
-                            obj,
-                            move |job| {
-                                match job.is_succeeded() {
-                                    Err(e) => {
-                                        obj.close_after_save.set(false);
-                                        obj.error_message(
-                                            Some(&e),
-                                            &formatx!(
-                                                gettext("The file could not be saved as “{}”."),
-                                                job.uri().unwrap_or_default(),
-                                            )
-                                            .expect("Wrong format in translated string"),
-                                        );
-                                    }
-                                    Ok(_) => {
-                                        if let Some(uri) = job.uri() {
-                                            gtk::RecentManager::default().add_item(&uri);
-                                        }
-                                    }
-                                }
-
-                                obj.clear_save_job();
-
-                                if obj.close_after_save.get() {
-                                    glib::idle_add_local_once(glib::clone!(
-                                        #[weak]
-                                        obj,
-                                        move || {
-                                            obj.parent_window().destroy();
-                                        }
-                                    ));
-                                }
-                            }
-                        ));
-
-                        obj.save_job.replace(Some(save_job.clone()));
-                        obj.save_job_handler.replace(Some(id));
-
-                        // The priority doesn't matter for this job
-                        save_job.scheduler_push_job(JobPriority::PriorityNone);
+                        obj.execute_save(&file);
                     }
                 }
             }
diff --git a/shell/src/document_view/signals.rs b/shell/src/document_view/signals.rs
index 22e744f42..e0fd12144 100644
--- a/shell/src/document_view/signals.rs
+++ b/shell/src/document_view/signals.rs
@@ -11,6 +11,7 @@ impl imp::PpsDocumentView {
     #[template_callback]
     pub(crate) fn doc_restrictions_changed(&self) {
         let Some(document) = self.document() else {
+            self.set_action_enabled("save", false);
             self.set_action_enabled("save-as", false);
             self.set_action_enabled("print", false);
             return;
@@ -43,6 +44,7 @@ impl imp::PpsDocumentView {
             }
         }
 
+        self.set_action_enabled("save", ok_to_copy);
         self.set_action_enabled("save-as", ok_to_copy);
         self.set_action_enabled("print", ok_to_print);
     }
@@ -335,6 +337,9 @@ impl imp::PpsDocumentView {
             _ if name.eq_ignore_ascii_case("Print") => {
                 self.document_action_group.activate_action("print", None)
             }
+            _ if name.eq_ignore_ascii_case("Save") => {
+                self.document_action_group.activate_action("save", None)
+            }
             _ if name.eq_ignore_ascii_case("SaveAs") => {
                 self.document_action_group.activate_action("save-as", None)
             }
diff --git a/shell/src/main.rs b/shell/src/main.rs
index 058b4c8d0..75445d288 100644
--- a/shell/src/main.rs
+++ b/shell/src/main.rs
@@ -1,3 +1,5 @@
+#![windows_subsystem = "windows"]
+
 mod config {
     #![allow(dead_code)]
 
@@ -44,7 +46,27 @@ mod window;
 
 use deps::*;
 
+#[cfg(target_os = "windows")]
+fn setup_environment() {
+    // Disable DBus session bus lookup on Windows if not configured to prevent GLib connection timeouts
+    if std::env::var_os("DBUS_SESSION_BUS_ADDRESS").is_none() {
+        unsafe {
+            std::env::set_var("DBUS_SESSION_BUS_ADDRESS", "none");
+        }
+    }
+    // Force GTK file chooser fallback and in-memory settings storage on Windows environments without portals/dconf
+    unsafe {
+        std::env::set_var("GTK_USE_PORTAL", "0");
+        std::env::set_var("GSETTINGS_BACKEND", "memory");
+    }
+}
+
+#[cfg(not(target_os = "windows"))]
+fn setup_environment() {}
+
 fn main() -> glib::ExitCode {
+    setup_environment();
+
     let mut log_builder = env_logger::builder();
     log_builder.format_timestamp_millis();
 
@@ -54,7 +76,22 @@ fn main() -> glib::ExitCode {
 
     log_builder.init();
 
-    gettextrs::bindtextdomain(GETTEXT_PACKAGE, PPS_LOCALEDIR)
+    gettextrs::setlocale(gettextrs::LocaleCategory::LcAll, "");
+
+    let locale_dir = if cfg!(target_os = "windows") {
+        if let Ok(mut path) = std::env::current_exe() {
+            path.pop();
+            path.push("share");
+            path.push("locale");
+            path
+        } else {
+            std::path::PathBuf::from(PPS_LOCALEDIR)
+        }
+    } else {
+        std::path::PathBuf::from(PPS_LOCALEDIR)
+    };
+
+    gettextrs::bindtextdomain(GETTEXT_PACKAGE, &locale_dir)
         .expect("Unable to bind the text domain");
     gettextrs::bind_textdomain_codeset(GETTEXT_PACKAGE, "UTF-8")
         .expect("Unable to bind the text domain codeset");
diff --git a/shell/src/meson.build b/shell/src/meson.build
index db1bc7b42..07afb9a23 100644
--- a/shell/src/meson.build
+++ b/shell/src/meson.build
@@ -24,13 +24,25 @@ papers_resources_env.set('PAPERS_RESOURCES_FILE', papers_resources.full_path())
 meson.add_devenv(papers_resources_env)
 
 
+cargo_deps = [papers_resources]
+
+if host_machine.system() == 'windows'
+  win = import('windows')
+  shell_win_res = win.compile_resources(
+    '../../build-aux/windows/papers.rc',
+    depend_files: files('../../build-aux/windows/papers.ico'),
+  )
+  cargo_deps += shell_win_res
+  cargo_env.set('RUSTFLAGS', '-C link-arg=' + shell_win_res[0].full_path())
+endif
+
 cargo_build = custom_target(
   'cargo-build',
   build_by_default: true,
   build_always_stale: true,
   output: 'src',
   console: true,
-  depends: [papers_resources],
+  depends: cargo_deps,
   env: cargo_env,
   command: [
     cargo, 'build',
@@ -38,6 +50,13 @@ cargo_build = custom_target(
   ]
 )
 
+
+if host_machine.system() == 'windows'
+  bin_name = meson.project_name() + '.exe'
+else
+  bin_name = meson.project_name()
+endif
+
 custom_target(
   'cp-binary',
   depends: cargo_build,
@@ -45,10 +64,11 @@ custom_target(
   build_always_stale: true,
   install: true,
   install_dir: get_option('bindir'),
-  output: meson.project_name(),
+  output: bin_name,
   command: [
-    'cp',
-    meson.current_build_dir() / rust_target / meson.project_name(),
+    find_program('python3', 'python'),
+    '-c', 'import sys, shutil; shutil.copy2(sys.argv[1], sys.argv[2])',
+    meson.current_build_dir() / rust_target / bin_name,
     '@OUTPUT@',
   ],
 )
diff --git a/shell/src/window.rs b/shell/src/window.rs
index aec51d4f4..bce20d3e3 100644
--- a/shell/src/window.rs
+++ b/shell/src/window.rs
@@ -398,8 +398,10 @@ mod imp {
                             obj.document_view.set_fullscreen_mode(enabled);
 
                             if enabled {
-                                obj.obj().fullscreen();
-                            } else {
+                                if !obj.obj().is_fullscreen() {
+                                    obj.obj().fullscreen();
+                                }
+                            } else if obj.obj().is_fullscreen() {
                                 obj.obj().unfullscreen();
                             }
 
diff --git a/thumbnailer/Cargo.toml b/thumbnailer/Cargo.toml
index 7f1cecc08..957663cd8 100644
--- a/thumbnailer/Cargo.toml
+++ b/thumbnailer/Cargo.toml
@@ -15,3 +15,4 @@ env_logger.workspace = true
 log.workspace = true
 gettext-rs.workspace = true
 image.workspace = true
+
diff --git a/thumbnailer/meson.build b/thumbnailer/meson.build
index 3c4e2518c..519037872 100644
--- a/thumbnailer/meson.build
+++ b/thumbnailer/meson.build
@@ -20,12 +20,25 @@ endif
 
 cargo_env = cargo_common_env
 
+cargo_deps = []
+
+if host_machine.system() == 'windows'
+  win = import('windows')
+  thumb_win_res = win.compile_resources(
+    'papers-thumbnailer.rc',
+    depend_files: files('../shell/resources/papers.ico'),
+  )
+  cargo_deps += thumb_win_res
+  cargo_env.set('RUSTFLAGS', '-C link-arg=' + thumb_win_res[0].full_path())
+endif
+
 cargo_build = custom_target(
   'cargo-build',
   build_by_default: true,
   build_always_stale: true,
   output: 'src',
   console: true,
+  depends: cargo_deps,
   env: cargo_env,
   command: [
     cargo,
@@ -34,6 +47,13 @@ cargo_build = custom_target(
   ],
 )
 
+
+if host_machine.system() == 'windows'
+  thumb_bin_name = meson.project_name() + '-thumbnailer.exe'
+else
+  thumb_bin_name = meson.project_name() + '-thumbnailer'
+endif
+
 copy = custom_target(
   'cp-binary',
   depends: cargo_build,
@@ -41,11 +61,11 @@ copy = custom_target(
   build_always_stale: true,
   install: true,
   install_dir: get_option('bindir'),
-  output: meson.project_name() + '-thumbnailer',
+  output: thumb_bin_name,
   command: [
-    'cp',
-    meson.current_build_dir() / rust_target / meson.project_name()
-    + '-thumbnailer',
+    find_program('python3', 'python'),
+    '-c', 'import sys, shutil; shutil.copy2(sys.argv[1], sys.argv[2])',
+    meson.current_build_dir() / rust_target / thumb_bin_name,
     '@OUTPUT@',
   ],
 )
