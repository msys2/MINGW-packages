--- a/build/cmake/config.cmake
+++ b/build/cmake/config.cmake
@@ -92,7 +92,7 @@
         "wx-config-inplace.in"
         "inplace-${TOOLCHAIN_FULLNAME}"
         )
-    if(WIN32_MSVC_NAMING)
+    if(WIN32)
         set(COPY_CMD copy)
     else()
         set(COPY_CMD create_symlink)
--- wxWidgets-3.2.9/build/cmake/install.cmake.orig	2026-01-01 10:26:48.819780100 +0100
+++ wxWidgets-3.2.9/build/cmake/install.cmake	2026-01-01 10:31:35.536682400 +0100
@@ -64,11 +64,16 @@
                     WORLD_EXECUTE WORLD_READ
         )
 
+    if(WIN32)
+        set(COPY_CMD copy)
+    else()
+        set(COPY_CMD create_symlink)
+    endif()
     wx_get_install_platform_dir(runtime)
     install(DIRECTORY DESTINATION "${runtime_dir}")
     install(CODE "execute_process( \
-        COMMAND ${CMAKE_COMMAND} -E create_symlink \
-        \"${CMAKE_INSTALL_PREFIX}/${library_dir}/wx/config/${wxBUILD_FILE_ID}\" \
+        COMMAND ${CMAKE_COMMAND} -E ${COPY_CMD} \
+        \"\$ENV{DESTDIR}${CMAKE_INSTALL_PREFIX}/${library_dir}/wx/config/${wxBUILD_FILE_ID}\" \
         \"\$ENV{DESTDIR}${CMAKE_INSTALL_PREFIX}/${runtime_dir}/wx-config\" \
         )"
     )
--- wxWidgets-3.2.9/build/cmake/utils/CMakeLists.txt.orig	2026-01-01 10:32:33.635778400 +0100
+++ wxWidgets-3.2.9/build/cmake/utils/CMakeLists.txt	2026-01-01 10:33:18.375741800 +0100
@@ -38,10 +38,15 @@
             set(EXE_SUFFIX ${CMAKE_EXECUTABLE_SUFFIX})
         endif()
 
+        if(WIN32)
+            set(COPY_CMD copy)
+        else()
+            set(COPY_CMD create_symlink)
+        endif()
         # Don't use wx_install() here to preserve escaping.
         install(CODE "execute_process( \
-            COMMAND ${CMAKE_COMMAND} -E create_symlink \
-            \"${CMAKE_INSTALL_PREFIX}/${runtime_dir}/${wxrc_output_name}${EXE_SUFFIX}\" \
+            COMMAND ${CMAKE_COMMAND} -E ${COPY_CMD} \
+            \"\$ENV{DESTDIR}${CMAKE_INSTALL_PREFIX}/${runtime_dir}/${wxrc_output_name}${EXE_SUFFIX}\" \
             \"\$ENV{DESTDIR}${CMAKE_INSTALL_PREFIX}/${runtime_dir}/wxrc${EXE_SUFFIX}\" \
             )"
         )
