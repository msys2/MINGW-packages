diff -bur scribus-1.6.5-orig/CMakeLists_Directories.cmake scribus-1.6.5/CMakeLists_Directories.cmake
--- scribus-1.6.5-orig/CMakeLists_Directories.cmake	2025-12-28 03:08:08.925350800 -0700
+++ scribus-1.6.5/CMakeLists_Directories.cmake	2025-12-28 03:09:00.958593700 -0700
@@ -37,7 +37,7 @@
 
 #DOC - use the default on Apple as TAG_VERSION is empty
 if(NOT DOCDIR)
-	if(WIN32 OR OS2)
+	if(OS2)
 		set(DOCDIR "${CMAKE_INSTALL_DATAROOTDIR}/doc/")
 	elseif(TAG_VERSION OR BUILD_OSX_BUNDLE)
 		set(DOCDIR "${CMAKE_INSTALL_DATAROOTDIR}/doc/${MAIN_DIR_NAME}${TAG_VERSION}/")
@@ -58,7 +58,7 @@
 
 #ICONS
 if(WIN32)
-	set(ICONDIR "${CMAKE_INSTALL_DATAROOTDIR}/icons/")
+	set(ICONDIR "${CMAKE_INSTALL_DATAROOTDIR}/${MAIN_DIR_NAME}${TAG_VERSION}/icons/")
 else()
 	set(ICONDIR "${CMAKE_INSTALL_DATAROOTDIR}/${MAIN_DIR_NAME}${TAG_VERSION}/icons/")
 endif()
@@ -73,7 +73,7 @@
 
 #SAMPLES
 if(WIN32)
-	set(SAMPLESDIR "${CMAKE_INSTALL_DATAROOTDIR}/samples/")
+	set(SAMPLESDIR "${CMAKE_INSTALL_DATAROOTDIR}/${MAIN_DIR_NAME}${TAG_VERSION}/samples/")
 else()
 	set(SAMPLESDIR "${CMAKE_INSTALL_DATAROOTDIR}/${MAIN_DIR_NAME}${TAG_VERSION}/samples/")
 endif()
@@ -88,7 +88,7 @@
 
 #SCRIPTS
 if(WIN32)
-	set(SCRIPTSDIR "${CMAKE_INSTALL_DATAROOTDIR}/scripts/")
+	set(SCRIPTSDIR "${CMAKE_INSTALL_DATAROOTDIR}/${MAIN_DIR_NAME}${TAG_VERSION}/scripts/")
 else()
 	set(SCRIPTSDIR "${CMAKE_INSTALL_DATAROOTDIR}/${MAIN_DIR_NAME}${TAG_VERSION}/scripts/")
 endif()
@@ -103,7 +103,7 @@
 
 #TEMPLATES
 if(WIN32)
-	set(TEMPLATEDIR "${CMAKE_INSTALL_DATAROOTDIR}/templates/")
+	set(TEMPLATEDIR "${CMAKE_INSTALL_DATAROOTDIR}/${MAIN_DIR_NAME}${TAG_VERSION}/templates/")
 else()
 	set(TEMPLATEDIR "${CMAKE_INSTALL_DATAROOTDIR}/${MAIN_DIR_NAME}${TAG_VERSION}/templates/")
 endif()
@@ -118,7 +118,7 @@
 
 #QML
 if(WIN32)
-	set(QMLDIR "${CMAKE_INSTALL_DATAROOTDIR}/qml/")
+	set(QMLDIR "${CMAKE_INSTALL_DATAROOTDIR}/${MAIN_DIR_NAME}${TAG_VERSION}/qml/")
 else()
 	set(QMLDIR "${CMAKE_INSTALL_DATAROOTDIR}/${MAIN_DIR_NAME}${TAG_VERSION}/qml/")
 endif()
@@ -198,7 +198,7 @@
 
 #PLUGINS
 if(WIN32)
-	set(PLUGINDIR "plugins")
+	set(PLUGINDIR "${LIB_DIR_NAME}/${MAIN_DIR_NAME}${TAG_VERSION}/plugins/")
 elseif (APPLEBUNDLE)
 	set(PLUGINDIR "${LIBDIR}")
 else()diff -bur scribus-1.6.5-orig/scribus/scpaths.cpp scribus-1.6.5/scribus/scpaths.cpp
--- scribus-1.6.5-orig/scribus/scpaths.cpp	2025-12-28 04:36:22.812273400 -0700
+++ scribus-1.6.5/scribus/scpaths.cpp	2025-12-28 04:36:42.778701700 -0700
@@ -55,7 +55,7 @@
 {
 // On *nix, all paths are initialized to compile-time defaults passed in
 // as preprocessor macros and set by autoconf.
-#if !defined(Q_OS_MACOS) && !defined(_WIN32) && defined(WANT_RELOCATABLE)
+#if !defined(Q_OS_MACOS) && defined(WANT_RELOCATABLE)
 	QString appPath = qApp->applicationDirPath();
 
 	// Sometime, for eg when running scribus as an appimage, the applicationDirPath returned 
@@ -126,28 +126,6 @@
 	qDebug() << QString("scpaths: plugin dir=%1").arg(m_pluginDir);
 	qDebug() << QString("scpaths: QML dir=%1").arg(m_qmlDir);
 	qDebug() << QString("scpaths: qtplugins=%1").arg(QApplication::libraryPaths().join(":"));
-
-#elif defined(_WIN32)
-	QFileInfo appInfo(QCoreApplication::applicationDirPath());
-	QString appPath = QCoreApplication::applicationDirPath();
-	QString cleanAppPath = appInfo.canonicalFilePath();
-	if (!cleanAppPath.isEmpty())
-		appPath = cleanAppPath;
-
-	m_shareDir = QString("%1/share/").arg(appPath);
-	m_docDir = QString("%1/share/doc/").arg(appPath);
-	m_fontDir = QString("%1/share/fonts/").arg(appPath);
-	m_iconDir = QString("%1/share/icons/").arg(appPath);
-	m_sampleScriptDir = QString("%1/share/samples/").arg(appPath);
-	m_scriptDir = QString("%1/share/scripts/").arg(appPath);
-	m_templateDir = QString("%1/share/templates/").arg(appPath);
-	m_libDir = QString("%1/libs/").arg(appPath);
-	m_pluginDir = QString("%1/plugins/").arg(appPath);
-	m_qmlDir = QString("%1/share/qml/").arg(appPath);
-
-	QString qtpluginDir = QString("%1/qtplugins/").arg(appPath);
-	if (QDir(qtpluginDir).exists())
-		QApplication::setLibraryPaths( QStringList(qtpluginDir) );
 #endif
 	
 // 	if (!m_shareDir.endsWith("/"))        m_shareDir += "/";
diff -bur scribus-1.6.5orig/CMakeLists.txt scribus-1.6.5/CMakeLists.txt
--- scribus-1.6.5orig/CMakeLists.txt	2025-12-28 04:45:54.204280600 -0700
+++ scribus-1.6.5/CMakeLists.txt	2025-12-28 04:46:05.130930000 -0700
@@ -308,7 +308,7 @@
 ##############################################################################################################
 ########## Relocatability                                                                           ##########
 
-if (APPLEBUNDLE OR WIN32)
+if (APPLEBUNDLE)
 	if (WANT_RELOCATABLE)
 		message(STATUS "Ignoring relocatable option on Win32 or OSX when building bundle")
 		set(WANT_RELOCATABLE OFF)
