--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -1,6 +1,7 @@
 cmake_minimum_required(VERSION 3.20)
 project(aas-sign VERSION 0.3.0 LANGUAGES CXX C)
+include(GNUInstallDirs)
 set(CMAKE_CXX_STANDARD 20)
 set(CMAKE_CXX_STANDARD_REQUIRED ON)
 
 # Enable Windows resource compiler (windres on MinGW / rc on MSVC).
@@ -10,11 +11,7 @@ find_package(Threads REQUIRED)
 
 include(FetchContent)
 
-FetchContent_Declare(json
-  URL                     https://github.com/nlohmann/json/releases/download/v3.11.3/json.tar.xz
-  URL_HASH                SHA256=d6c65aca6b1ed68e7a182f4757257b107ae403032760ed6ef121c9d55e81757d
-  DOWNLOAD_EXTRACT_TIMESTAMP FALSE)
-FetchContent_MakeAvailable(json)
+find_package(nlohmann_json CONFIG REQUIRED)
 
 if(NOT WIN32)
   set(ENABLE_PROGRAMS OFF CACHE BOOL "" FORCE)
@@ -76,3 +73,5 @@ if(AAS_SIGN_FUZZ)
   list(APPEND CMAKE_MODULE_PATH "${CMAKE_CURRENT_SOURCE_DIR}/cmake")
   include(Fuzzing)
 endif()
+
+install(TARGETS aas-sign RUNTIME DESTINATION "${CMAKE_INSTALL_BINDIR}")
