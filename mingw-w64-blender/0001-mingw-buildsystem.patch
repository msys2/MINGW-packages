diff -Naur blender-5.2.0/CMakeLists.txt blender-5.2.0-patched/CMakeLists.txt
--- blender-5.2.0/CMakeLists.txt	2026-06-03 15:03:48.000000000 +0200
+++ blender-5.2.0-patched/CMakeLists.txt	2026-08-09 00:21:05.932473900 +0200
@@ -396,7 +396,7 @@
   option(WITH_X11_XFIXES "Enable X11 XWayland cursor warping workaround" ON)
 endif()
 
-if(UNIX AND NOT APPLE)
+if((UNIX AND NOT APPLE) OR MINGW)
   option(WITH_SYSTEM_FREETYPE "Use the FreeType library provided by the operating system" OFF)
 else()
   set(WITH_SYSTEM_FREETYPE OFF)
@@ -515,7 +515,7 @@
 option(WITH_FREESTYLE "Enable Freestyle (advanced edges rendering)" ON)
 
 # Libraries.
-if(UNIX AND NOT APPLE)
+if((UNIX AND NOT APPLE) OR MINGW)
   # Optionally build without pre-compiled libraries.
   # NOTE: this could be supported on all platforms however in practice UNIX is the only platform
   # that has good support for detecting installed libraries.
@@ -543,7 +543,7 @@
 option(WITH_INPUT_NDOF "Enable NDOF input devices (SpaceNavigator and friends)" ON)
 # On Windows and for the Blender application on macOS, portable install
 # is the only supported installation type, so there is no option.
-if(UNIX AND (NOT APPLE OR WITH_PYTHON_MODULE))
+if((UNIX AND (NOT APPLE OR WITH_PYTHON_MODULE)) OR MINGW)
   option(WITH_INSTALL_PORTABLE "\
 Install redistributable runtime, otherwise install into CMAKE_INSTALL_PREFIX"
     ON
@@ -891,7 +891,7 @@
 
 
 # Documentation
-if(UNIX AND NOT APPLE)
+if((UNIX AND NOT APPLE) OR MINGW)
   option(WITH_DOC_MANPAGE "Create a manual page (Unix manpage)" OFF)
 endif()
 
@@ -956,7 +956,7 @@
   set(WITH_METAL_BACKEND OFF)
 endif()
 
-if(WIN32)
+if(MSVC)
   getDefaultWindowsPrefixBase(CMAKE_GENERIC_PROGRAM_FILES)
   set(CPACK_INSTALL_PREFIX ${CMAKE_GENERIC_PROGRAM_FILES}/)
 endif()
@@ -1132,7 +1132,7 @@
   mark_as_advanced(WITH_COMPILER_SHORT_FILE_MACRO)
 endif()
 
-if(WIN32)
+if(MSVC)
   # Use hardcoded paths or find_package to find externals
   option(WITH_WINDOWS_FIND_MODULES "Use find_package to locate libraries" OFF)
   mark_as_advanced(WITH_WINDOWS_FIND_MODULES)
@@ -1540,8 +1540,10 @@
 
 if(UNIX AND NOT APPLE)
   include(platform_unix)
-elseif(WIN32)
+elseif(MSVC)
   include(platform_win32)
+elseif(MINGW)
+  include(platform_win32_mingw64)
 elseif(APPLE)
   include(platform_apple)
 endif()
@@ -1578,7 +1580,7 @@
 
 if(WITH_IMAGE_OPENJPEG)
   # Special handling of Windows platform where openjpeg is always static.
-  if(WIN32)
+  if(WIN32 AND WITH_STATIC_LIBS)
     set(OPENJPEG_DEFINES "-DOPJ_STATIC")
   else()
     set(OPENJPEG_DEFINES "")
@@ -1642,8 +1644,6 @@
     if(NOT GFLAGS_FOUND)
       message(FATAL_ERROR "System wide Gflags is requested but was not found")
     endif()
-    # `FindGflags` does not define this, and we are not even sure what to use here.
-    set(GFLAGS_DEFINES "")
   else()
     set(GFLAGS_DEFINES
       -DGFLAGS_DLL_DEFINE_FLAG=
@@ -1656,12 +1656,14 @@
   endif()
 
   if(WITH_SYSTEM_GLOG)
-    find_package(Glog)
+    find_package(Glog CONFIG)
+    set(GLOG_FOUND ${glog_FOUND})
     if(NOT GLOG_FOUND)
       message(FATAL_ERROR "System wide Glog is requested but was not found")
     endif()
-    # `FindGlog` does not define this, and we are not even sure what to use here.
-    set(GLOG_DEFINES "")
+    set(GLOG_INCLUDE_DIR ${glog_INCLUDE_DIR})
+    set(GLOG_LIBRARIES ${glog_LIBRARIES})
+    get_target_property(GLOG_DEFINES glog::glog INTERFACE_COMPILE_DEFINITIONS)
   else()
     set(GLOG_DEFINES
       -DGOOGLE_GLOG_DLL_DECL=
@@ -2273,7 +2275,7 @@
     )
   endif()
 
-  if(WIN32)
+  if(MSVC)
     # Always use numpy bundled in precompiled libs.
   elseif((WITH_PYTHON_INSTALL AND WITH_PYTHON_INSTALL_NUMPY) OR WITH_PYTHON_NUMPY)
     if(("${PYTHON_NUMPY_PATH}" STREQUAL "") OR (${PYTHON_NUMPY_PATH} MATCHES NOTFOUND))
@@ -2283,13 +2285,13 @@
     endif()
   endif()
 
-  if(WIN32 OR APPLE)
+  if(MSVC OR APPLE)
     # Always copy from precompiled libs.
   elseif(WITH_PYTHON_INSTALL_REQUESTS)
     find_python_package(requests "")
   endif()
 
-  if(WIN32 OR APPLE)
+  if(MSVC OR APPLE)
     # Always copy from precompiled libs.
   elseif(WITH_PYTHON_INSTALL_ZSTANDARD)
     find_python_package(zstandard "")
diff -Naur blender-5.2.0/build_files/cmake/Modules/FindGflags.cmake blender-5.2.0-patched/build_files/cmake/Modules/FindGflags.cmake
--- blender-5.2.0/build_files/cmake/Modules/FindGflags.cmake	2026-06-03 15:03:48.000000000 +0200
+++ blender-5.2.0-patched/build_files/cmake/Modules/FindGflags.cmake	2026-08-09 00:21:05.945145400 +0200
@@ -384,6 +384,7 @@
     message(STATUS "Detected gflags version: ${gflags_VERSION}")
     set(GFLAGS_FOUND ${gflags_FOUND})
     set(GFLAGS_INCLUDE_DIR ${gflags_INCLUDE_DIR})
+    get_target_property(GFLAGS_DEFINES ${GFLAGS_TARGET} INTERFACE_COMPILE_DEFINITIONS)
     set(GFLAGS_LIBRARY ${gflags_LIBRARIES})
 
     # gflags does not export the namespace in their CMake configuration, so
diff -Naur blender-5.2.0/build_files/cmake/Modules/FindPythonLibsUnix.cmake blender-5.2.0-patched/build_files/cmake/Modules/FindPythonLibsUnix.cmake
--- blender-5.2.0/build_files/cmake/Modules/FindPythonLibsUnix.cmake	2026-06-03 15:03:48.000000000 +0200
+++ blender-5.2.0-patched/build_files/cmake/Modules/FindPythonLibsUnix.cmake	2026-08-09 00:21:05.932473900 +0200
@@ -50,7 +50,9 @@
   endif()
 else()
   # See: http://docs.python.org/extending/embedding.html#linking-requirements
-  set(PYTHON_LINKFLAGS "-Xlinker -export-dynamic" CACHE STRING "Linker flags for python")
+  if(NOT MINGW)
+    set(PYTHON_LINKFLAGS "-Xlinker -export-dynamic" CACHE STRING "Linker flags for python")
+  endif()
   mark_as_advanced(PYTHON_LINKFLAGS)
 endif()
 
diff -Naur blender-5.2.0/build_files/cmake/macros.cmake blender-5.2.0-patched/build_files/cmake/macros.cmake
--- blender-5.2.0/build_files/cmake/macros.cmake	2026-06-03 15:03:48.000000000 +0200
+++ blender-5.2.0-patched/build_files/cmake/macros.cmake	2026-08-09 00:21:05.935008200 +0200
@@ -1690,7 +1690,7 @@
 # which modifies `CMAKE_*_FLAGS` in the caller's scope.
 macro(optimize_debug_target executable)
   if(WITH_OPTIMIZED_BUILD_TOOLS)
-    if(WIN32)
+    if(MSVC)
       remove_c_and_cxx_flag("/Od" "/RTC1")
       target_compile_options(${executable} PRIVATE "/Ox")
       target_compile_definitions(${executable} PRIVATE "_ITERATOR_DEBUG_LEVEL=0")
diff -Naur blender-5.2.0/build_files/cmake/platform/dependency_targets.cmake blender-5.2.0-patched/build_files/cmake/platform/dependency_targets.cmake
--- blender-5.2.0/build_files/cmake/platform/dependency_targets.cmake	2026-06-03 15:03:48.000000000 +0200
+++ blender-5.2.0-patched/build_files/cmake/platform/dependency_targets.cmake	2026-08-09 00:21:05.937068300 +0200
@@ -29,7 +29,7 @@
 add_library(bf_deps_optional_manifold INTERFACE)
 add_library(bf::dependencies::optional::manifold ALIAS bf_deps_optional_manifold)
 if(WITH_MANIFOLD)
-  if(WIN32)
+  if(MSVC)
     target_compile_definitions(bf_deps_optional_manifold INTERFACE WITH_MANIFOLD)
     target_include_directories(bf_deps_optional_manifold SYSTEM INTERFACE ${MANIFOLD_INCLUDE_DIRS})
     target_link_libraries(bf_deps_optional_manifold INTERFACE ${MANIFOLD_LIBRARIES} bf::dependencies::optional::tbb)
diff -Naur blender-5.2.0/extern/CMakeLists.txt blender-5.2.0-patched/extern/CMakeLists.txt
--- blender-5.2.0/extern/CMakeLists.txt	2026-06-03 15:03:48.000000000 +0200
+++ blender-5.2.0-patched/extern/CMakeLists.txt	2026-08-09 00:21:05.940608200 +0200
@@ -76,7 +76,9 @@
   if(NOT WITH_SYSTEM_GFLAGS)
     add_subdirectory(gflags)
   endif()
-  add_subdirectory(glog)
+  if(NOT WITH_SYSTEM_GLOG)
+    add_subdirectory(glog)
+  endif()
 endif()
 
 if(WITH_GTESTS)
diff -Naur blender-5.2.0/intern/cycles/app/CMakeLists.txt blender-5.2.0-patched/intern/cycles/app/CMakeLists.txt
--- blender-5.2.0/intern/cycles/app/CMakeLists.txt	2026-06-03 15:03:48.000000000 +0200
+++ blender-5.2.0-patched/intern/cycles/app/CMakeLists.txt	2026-08-09 00:21:05.937068300 +0200
@@ -62,6 +62,8 @@
 if(APPLE AND NOT CYCLES_STANDALONE_REPOSITORY)
   set(app_output_dir Blender.app/Contents/MacOS)
   set(app_install_dir Blender.app/Contents/MacOS)
+elseif(MINGW)
+  set(app_install_dir ${CMAKE_INSTALL_PREFIX}/bin)
 else()
   set(app_install_dir ${CMAKE_INSTALL_PREFIX})
 endif()
diff -Naur blender-5.2.0/source/blender/blentranslation/msgfmt/CMakeLists.txt blender-5.2.0-patched/source/blender/blentranslation/msgfmt/CMakeLists.txt
--- blender-5.2.0/source/blender/blentranslation/msgfmt/CMakeLists.txt	2026-06-03 15:03:49.000000000 +0200
+++ blender-5.2.0-patched/source/blender/blentranslation/msgfmt/CMakeLists.txt	2026-08-09 00:21:05.940608200 +0200
@@ -26,7 +26,9 @@
 add_c_and_cxx_flags_custom_test(msgfmt)
 
 if(WIN32)
+  if(MSVC)
   string(APPEND CMAKE_EXE_LINKER_FLAGS_DEBUG " /nodefaultlib:MSVCRT.lib")
+  endif()
   list(APPEND LIB bf_intern_utfconv)
 endif()
 
diff -Naur blender-5.2.0/source/creator/CMakeLists.txt blender-5.2.0-patched/source/creator/CMakeLists.txt
--- blender-5.2.0/source/creator/CMakeLists.txt	2026-06-11 16:38:45.000000000 +0200
+++ blender-5.2.0-patched/source/creator/CMakeLists.txt	2026-08-09 00:21:05.943637000 +0200
@@ -379,7 +379,7 @@
 #
 # Setup version directory, libraries, `bpy` & text files.
 
-if(UNIX AND NOT APPLE)
+if((UNIX AND NOT APPLE) OR MINGW)
   if(WITH_PYTHON_MODULE)
     if(WITH_INSTALL_PORTABLE)
       set(TARGETDIR_BPY "bpy")
@@ -566,7 +566,7 @@
   DIRECTORY ${CMAKE_SOURCE_DIR}/release/datafiles/colormanagement
   DESTINATION ${TARGETDIR_VER}/datafiles
 )
-if(WIN32)
+if(MSVC)
   if(EXISTS ${LIBDIR}/osl/lib/python${PYTHON_VERSION}/site-packages/oslquery) # 4.4+
     install(
       DIRECTORY ${LIBDIR}/osl/lib/python${PYTHON_VERSION}/site-packages/oslquery
@@ -739,7 +739,7 @@
 # -----------------------------------------------------------------------------
 # Install Targets (Platform Specific)
 
-if(UNIX AND NOT APPLE)
+if((UNIX AND NOT APPLE) OR MINGW)
 
   if(PLATFORM_BUNDLED_LIBRARIES AND TARGETDIR_LIB)
     install(
@@ -2085,13 +2085,17 @@
   install(
     TARGETS blender blender-launcher
     COMPONENT Blender
-    DESTINATION "."
+    RUNTIME DESTINATION bin
+    ARCHIVE DESTINATION lib
+    LIBRARY DESTINATION lib
   )
   if(WITH_CPU_CHECK)
     install(
       TARGETS blender_cpu_check
       COMPONENT Blender
-      DESTINATION "."
+      RUNTIME DESTINATION bin
+      ARCHIVE DESTINATION lib
+      LIBRARY DESTINATION lib
     )
   endif()
   set_target_properties(
@@ -2113,7 +2117,7 @@
 # As executing Blender is needed - it's important this operation runs after the shared
 # libraries have been installed to their destination.
 
-if(UNIX AND NOT APPLE)
+if((UNIX AND NOT APPLE) OR MINGW)
   if(NOT WITH_PYTHON_MODULE)
     if(WITH_DOC_MANPAGE)
       # Only run the command to generate the man-page when it may be outdated.
