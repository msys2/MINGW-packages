diff -Naur blender-5.2.0/intern/ghost/CMakeLists.txt blender-5.2.0-patched/intern/ghost/CMakeLists.txt
--- blender-5.2.0/intern/ghost/CMakeLists.txt	2026-08-09 00:21:09.247746500 +0200
+++ blender-5.2.0-patched/intern/ghost/CMakeLists.txt	2026-08-09 00:21:10.335505700 +0200
@@ -434,7 +434,6 @@
     intern/GHOST_ContextD3D.cc
     intern/GHOST_DropTargetWin32.cc
     intern/GHOST_SystemWin32.cc
-    intern/GHOST_TrackpadWin32.cc
     intern/GHOST_WindowWin32.cc
     intern/GHOST_Wintab.cc
 
diff -Naur blender-5.2.0/intern/ghost/intern/GHOST_SystemWin32.cc blender-5.2.0-patched/intern/ghost/intern/GHOST_SystemWin32.cc
--- blender-5.2.0/intern/ghost/intern/GHOST_SystemWin32.cc	2026-08-09 00:21:07.262550100 +0200
+++ blender-5.2.0-patched/intern/ghost/intern/GHOST_SystemWin32.cc	2026-08-09 00:21:10.339042600 +0200
@@ -433,9 +433,9 @@
     if (timerMgr->fireTimers(getMilliSeconds())) {
       hasEventHandled = true;
     }
-
+#ifdef _MSC_VER
     driveTrackpad();
-
+#endif
     /* Process all the events waiting for us. */
     while (::PeekMessageW(&msg, nullptr, 0, 0, PM_REMOVE) != 0) {
       /* #TranslateMessage doesn't alter the message, and doesn't change our raw keyboard data.
@@ -444,9 +444,9 @@
       ::DispatchMessageW(&msg);
       hasEventHandled = true;
     }
-
+#ifdef _MSC_VER
     processTrackpad();
-
+#endif
     /* `PeekMessage` above is allowed to dispatch messages to the `wndproc` without us
      * noticing, so we need to check the event manager here to see if there are
      * events waiting in the queue. */
@@ -1581,7 +1581,7 @@
   return eventSent;
 }
 #endif /* WITH_INPUT_NDOF */
-
+#ifdef _MSC_VER
 void GHOST_SystemWin32::driveTrackpad()
 {
   GHOST_WindowWin32 *active_window = static_cast<GHOST_WindowWin32 *>(
@@ -1628,7 +1628,7 @@
                                                             false));
   }
 }
-
+#endif
 LRESULT WINAPI GHOST_SystemWin32::s_wndProc(HWND hwnd, uint msg, WPARAM wParam, LPARAM lParam)
 {
   std::unique_ptr<GHOST_Event> event = nullptr;
@@ -2264,8 +2264,9 @@
                        suggestedWindowRect->right - suggestedWindowRect->left,
                        suggestedWindowRect->bottom - suggestedWindowRect->top,
                        SWP_NOZORDER | SWP_NOACTIVATE);
-
+#ifdef _MSC_VER
           window->updateDPI();
+#endif
           break;
         }
         case WM_DISPLAYCHANGE: {
@@ -2377,6 +2378,7 @@
            * In GHOST, we let DefWindowProc call the timer callback. */
           break;
         }
+#ifdef _MSC_VER
         case DM_POINTERHITTEST: {
           /* The DM_POINTERHITTEST message is sent to a window, when pointer input is first
            * detected, in order to determine the most probable input target for Direct
@@ -2386,6 +2388,7 @@
           }
           break;
         }
+#endif
       }
     }
     else {
diff -Naur blender-5.2.0/intern/ghost/intern/GHOST_SystemWin32.hh blender-5.2.0-patched/intern/ghost/intern/GHOST_SystemWin32.hh
--- blender-5.2.0/intern/ghost/intern/GHOST_SystemWin32.hh	2026-06-03 15:03:48.000000000 +0200
+++ blender-5.2.0-patched/intern/ghost/intern/GHOST_SystemWin32.hh	2026-08-09 00:21:10.342363300 +0200
@@ -444,7 +444,7 @@
    */
   bool processNDOF(RAWINPUT const &raw);
 #endif
-
+#ifdef _MSC_VER
   /**
    * Drives Direct Manipulation update.
    */
@@ -454,7 +454,7 @@
    * Creates trackpad events for the active window.
    */
   void processTrackpad();
-
+#endif
   /**
    * Check current key layout for AltGr
    */
diff -Naur blender-5.2.0/intern/ghost/intern/GHOST_WindowWin32.cc blender-5.2.0-patched/intern/ghost/intern/GHOST_WindowWin32.cc
--- blender-5.2.0/intern/ghost/intern/GHOST_WindowWin32.cc	2026-06-03 15:03:48.000000000 +0200
+++ blender-5.2.0-patched/intern/ghost/intern/GHOST_WindowWin32.cc	2026-08-09 00:21:10.342363300 +0200
@@ -81,8 +81,10 @@
       last_pointer_tablet_data_(GHOST_TABLET_DATA_NONE),
       normal_state_(GHOST_kWindowStateNormal),
       user32_(::LoadLibrary("user32.dll")),
-      parent_window_hwnd_(parent_window ? parent_window->h_wnd_ : HWND_DESKTOP),
+      parent_window_hwnd_(parent_window ? parent_window->h_wnd_ : HWND_DESKTOP)
+#ifdef _MSC_VER
       direct_manipulation_helper_(nullptr)
+#endif
 {
   DWORD style = parent_window ?
                     WS_POPUPWINDOW | WS_CAPTION | WS_MAXIMIZEBOX | WS_MINIMIZEBOX | WS_SIZEBOX :
@@ -215,13 +217,16 @@
   CoCreateInstance(
       CLSID_TaskbarList, nullptr, CLSCTX_INPROC_SERVER, IID_ITaskbarList3, (LPVOID *)&bar_);
 
+#ifdef _MSC_VER
   /* Initialize Direct Manipulation. */
   direct_manipulation_helper_ = GHOST_DirectManipulationHelper::create(h_wnd_, getDPIHint());
+#endif
 
   /* Initialize HDR info. */
   updateHDRInfo();
 }
 
+#ifdef _MSC_VER
 void GHOST_WindowWin32::updateDirectManipulation()
 {
   if (!direct_manipulation_helper_) {
@@ -254,6 +259,7 @@
 
   return direct_manipulation_helper_->getTrackpadInfo();
 }
+#endif
 
 GHOST_WindowWin32::~GHOST_WindowWin32()
 {
@@ -307,8 +313,10 @@
     h_wnd_ = 0;
   }
 
+#ifdef _MSC_VER
   delete direct_manipulation_helper_;
   direct_manipulation_helper_ = nullptr;
+#endif
 }
 
 void GHOST_WindowWin32::adjustWindowRectForClosestMonitor(LPRECT win_rect,
@@ -1031,12 +1039,14 @@
   }
 }
 
+#ifdef _MSC_VER
 void GHOST_WindowWin32::updateDPI()
 {
   if (direct_manipulation_helper_) {
     direct_manipulation_helper_->setDPI(getDPIHint());
   }
 }
+#endif
 
 uint16_t GHOST_WindowWin32::getDPIHint()
 {
diff -Naur blender-5.2.0/intern/ghost/intern/GHOST_WindowWin32.hh blender-5.2.0-patched/intern/ghost/intern/GHOST_WindowWin32.hh
--- blender-5.2.0/intern/ghost/intern/GHOST_WindowWin32.hh	2026-06-03 15:03:48.000000000 +0200
+++ blender-5.2.0-patched/intern/ghost/intern/GHOST_WindowWin32.hh	2026-08-09 00:21:10.345404300 +0200
@@ -14,7 +14,9 @@
 #endif /* WIN32 */
 
 #include "GHOST_TaskbarWin32.hh"
+#ifdef _MSC_VER
 #include "GHOST_TrackpadWin32.hh"
+#endif
 #include "GHOST_Window.hh"
 #include "GHOST_Wintab.hh"
 #ifdef WITH_INPUT_IME
@@ -25,6 +27,7 @@
 
 class GHOST_SystemWin32;
 class GHOST_DropTargetWin32;
+class GHOST_DirectManipulationHelper;
 
 /* typedefs for user32 functions to allow dynamic loading of Windows 10 DPI scaling functions. */
 typedef UINT(API *GHOST_WIN32_GetDpiForWindow)(HWND);
@@ -287,7 +290,9 @@
    */
   GHOST_TabletData getTabletData();
 
+#ifdef _MSC_VER
   void updateDPI();
+#endif
 
   uint16_t getDPIHint() override;
 
@@ -314,6 +319,7 @@
   /**
    * Drive DirectManipulation context.
    */
+#ifdef _MSC_VER
   void updateDirectManipulation();
 
   /**
@@ -323,6 +329,7 @@
   void onPointerHitTest(WPARAM wParam);
 
   GHOST_TTrackpadInfo getTrackpadInfo();
+#endif
 
   /** Update HDR info on initialization and window changes. */
   void updateHDRInfo();
