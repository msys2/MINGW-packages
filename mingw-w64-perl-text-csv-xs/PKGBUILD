# Contributor: Mehdi Chinoune <mehdi.chinoune@hotmail.com>

_perlname=Text-CSV_XS
_realname="${_perlname,,}"
_realname="${_realname/_/-}"
pkgbase=mingw-w64-perl-${_realname}
pkgname=("${MINGW_PACKAGE_PREFIX}-perl-${_realname}")
pkgver=1.64
pkgrel=2
pkgdesc="Comma-Separated Values manipulation routines. (mingw-w64)"
arch=('any')
mingw_arch=('ucrt64' 'clang64' 'clangarm64')
url="https://metacpan.org/dist/Text-CSV_XS"
msys2_repository_url="https://github.com/cpan-authors/Text-CSV_XS"
msys2_references=(
  'purl: pkg:cpan/HMBRAND/Text-CSV_XS'
)
license=('spdx:Artistic-1.0-Perl OR GPL-1.0-or-later')
depends=("${MINGW_PACKAGE_PREFIX}-perl")
makedepends=("${MINGW_PACKAGE_PREFIX}-cc"
             "${MINGW_PACKAGE_PREFIX}-make")
source=("https://cpan.metacpan.org/authors/id/H/HM/HMBRAND/${_perlname}-${pkgver}.tgz")
sha256sums=('65c5662d4fe8ef3039a1b32f641634d0aae6ab10eabbb24f740c75332f2caf30')

build() {
  cp -r ${_perlname}-${pkgver} build-${MSYSTEM} && cd build-${MSYSTEM}
  MSYS2_ARG_CONV_EXCL="PREFIX=" \
  PERL_MM_USE_DEFAULT=1 \
    perl Makefile.PL \
    PREFIX=${MINGW_PREFIX}
  mingw32-make
}

package() {
  cd build-${MSYSTEM}
  MSYS2_ARG_CONV_EXCL="PREFIX=" \
  mingw32-make DESTDIR="${pkgdir}/" install
}
