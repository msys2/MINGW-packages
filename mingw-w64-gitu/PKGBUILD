# Maintainer: Jing Yufei <master-hash@outlook.com>

_realname=gitu
pkgbase=mingw-w64-${_realname}
pkgname="${MINGW_PACKAGE_PREFIX}-${_realname}"
pkgver=0.43.0
pkgrel=1
pkgdesc='A TUI Git client inspired by Magit (mingw-w64)'
msys2_references=(
  'anitya: 387618'
  'archlinux: gitu'
  'purl: pkg:cargo/gitu'
)
url='https://github.com/altsem/gitu'
msys2_repository_url='https://github.com/altsem/gitu'
arch=('any')
mingw_arch=('ucrt64' 'clang64' 'clangarm64')
license=('spdx:MIT')
depends=("${MINGW_PACKAGE_PREFIX}-libgit2")
makedepends=("${MINGW_PACKAGE_PREFIX}-rust"
             "${MINGW_PACKAGE_PREFIX}-pkgconf"
             'git')
source=("git+https://github.com/altsem/gitu.git#tag=v${pkgver}")
sha256sums=('925e6bcc0e9e8a9ce9efc81c5da6d0f00e24a0b272eb602c76a66e9302d2bf90')

prepare() {
  cd "${_realname}"

  cargo fetch --locked --target "${RUST_CHOST}"
}

build() {
  cd "${_realname}"

  cargo build \
    --frozen \
    --release
}

check() {
  cd "${_realname}"

  cargo test \
    --frozen \
    --release
}

package() {
  cd "${_realname}"

  cargo install \
    --frozen \
    --offline \
    --no-track \
    --path . \
    --root "${pkgdir}${MINGW_PREFIX}"

  install -Dm644 LICENSE "${pkgdir}${MINGW_PREFIX}/share/licenses/${_realname}/LICENSE"
}
