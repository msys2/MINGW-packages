--- a/qtbase/mkspecs/features/configure_base.prf
+++ b/qtbase/mkspecs/features/configure_base.prf
@@ -10,10 +10,10 @@
 !isEmpty(QMAKE_MAKE) {
     # We were called recursively. Use the same make.
 } else: if(equals(MAKEFILE_GENERATOR, UNIX)|equals(MAKEFILE_GENERATOR, MINGW)) {
-    !equals(QMAKE_HOST.os, Windows): \
-        QMAKE_MAKE = make
-    else: \
+    equals(QMAKE_HOST.os, Windows):isEmpty(QMAKE_SH): \
         QMAKE_MAKE = mingw32-make
+    else: \
+        QMAKE_MAKE = make
 } else: if(equals(MAKEFILE_GENERATOR, MSVC.NET)|equals(MAKEFILE_GENERATOR, MSBUILD)) {
     QMAKE_MAKE = nmake
 } else {
