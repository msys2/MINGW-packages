From 2db237b83b5a5922c8ae4977ab3614525284cd7f Mon Sep 17 00:00:00 2001
From: Tim Stahlhut <stahta01@gmail.com>
Date: Wed, 8 Feb 2023 09:53:15 -0500
Subject: msys2 workaround copy bug

---
 deps/llvm.mk | 1 +
 1 file changed, 1 insertion(+)

diff --git a/deps/llvm.mk b/deps/llvm.mk
index 61210728e6..ddd36c5acf 100644
--- a/deps/llvm.mk
+++ b/deps/llvm.mk
@@ -255,6 +255,7 @@ endif
 
 LLVM_INSTALL = \
 	cd $1 && mkdir -p $2$$(build_depsbindir) && \
+	cp -r $$(SRCCACHE)/$$(LLVM_SRC_DIR)/llvm/utils/lit $2$$(build_depsbindir)/ || true && \
 	cp -r $$(SRCCACHE)/$$(LLVM_SRC_DIR)/llvm/utils/lit $2$$(build_depsbindir)/ && \
 	$$(CMAKE) -DCMAKE_INSTALL_PREFIX="$2$$(build_prefix)" -P cmake_install.cmake
 ifeq ($(OS), WINNT)
-- 
2.39.1.windows.1

