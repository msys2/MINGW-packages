diff --git a/thirdparty/libcontext/libcontext.cpp b/thirdparty/libcontext/libcontext.cpp
index 51d9a706..1abb7fca 100644
--- a/thirdparty/libcontext/libcontext.cpp
+++ b/thirdparty/libcontext/libcontext.cpp
@@ -259,6 +259,94 @@ __asm (
 
 #endif
 
+#if defined(LIBCONTEXT_PLATFORM_windows_arm64) && defined(LIBCONTEXT_COMPILER_gcc)
+__asm (
+".text\n"
+".balign 4\n"
+".globl	jump_fcontext\n"
+".def	jump_fcontext;	.scl	2;	.type	32;	.endef\n"
+".seh_proc	jump_fcontext\n"
+"jump_fcontext:\n"
+".seh_endprologue\n"
+"    sub  sp, sp, #0xd0\n"
+"    stp  d8,  d9,  [sp, #0x00]\n"
+"    stp  d10, d11, [sp, #0x10]\n"
+"    stp  d12, d13, [sp, #0x20]\n"
+"    stp  d14, d15, [sp, #0x30]\n"
+"    stp  x19, x20, [sp, #0x40]\n"
+"    stp  x21, x22, [sp, #0x50]\n"
+"    stp  x23, x24, [sp, #0x60]\n"
+"    stp  x25, x26, [sp, #0x70]\n"
+"    stp  x27, x28, [sp, #0x80]\n"
+"    stp  x29, x30, [sp, #0x90]\n"
+"    str  x30, [sp, #0xc0]\n"
+"    ldp  x5,  x6,  [x18, #0x08]\n"
+"    stp  x5,  x6,  [sp, #0xa0]\n"
+"    ldr  x5, [x18, #0x1478]\n"
+"    ldr  x6, [x18, #0x20]\n"
+"    stp  x5,  x6,  [sp, #0xb0]\n"
+"    mov  x4, sp\n"
+"    str  x4, [x0]\n"
+"    mov  sp, x1\n"
+"    ldp  x5,  x6,  [sp, #0xa0]\n"
+"    stp  x5,  x6,  [x18, #0x08]\n"
+"    ldp  x5,  x6,  [sp, #0xb0]\n"
+"    str  x5, [x18, #0x1478]\n"
+"    str  x6, [x18, #0x20]\n"
+"    ldp  d8,  d9,  [sp, #0x00]\n"
+"    ldp  d10, d11, [sp, #0x10]\n"
+"    ldp  d12, d13, [sp, #0x20]\n"
+"    ldp  d14, d15, [sp, #0x30]\n"
+"    ldp  x19, x20, [sp, #0x40]\n"
+"    ldp  x21, x22, [sp, #0x50]\n"
+"    ldp  x23, x24, [sp, #0x60]\n"
+"    ldp  x25, x26, [sp, #0x70]\n"
+"    ldp  x27, x28, [sp, #0x80]\n"
+"    ldp  x29, x30, [sp, #0x90]\n"
+"    mov  x0, x2\n"
+"    ldr  x4, [sp, #0xc0]\n"
+"    add  sp, sp, #0xd0\n"
+"    ret  x4\n"
+".seh_endproc\n"
+);
+
+#endif
+
+#if defined(LIBCONTEXT_PLATFORM_windows_arm64) && defined(LIBCONTEXT_COMPILER_gcc)
+__asm (
+".text\n"
+".balign 4\n"
+".globl	make_fcontext\n"
+".def	make_fcontext;	.scl	2;	.type	32;	.endef\n"
+".seh_proc	make_fcontext\n"
+"make_fcontext:\n"
+".seh_endprologue\n"
+"    mov  x3, x0\n"
+"    and  x0, x0, ~0xF\n"
+"    sub  x0, x0, #0xd0\n"
+"    str  x3, [x0, #0xa0]\n"
+"    sub  x3, x3, x1\n"
+"    stp  x3, x3, [x0, #0xa8]\n"
+"    str  xzr, [x0, #0xb8]\n"
+"    str  x2, [x0, #0x40]\n"
+"    adr  x2, trampoline\n"
+"    str  x2, [x0, #0xc0]\n"
+"    adr  x1, finish\n"
+"    str  x1, [x0, #0x98]\n"
+"    ret  x30\n"
+"trampoline:\n"
+"    stp  fp, lr, [sp, #-0x10]!\n"
+"    mov  fp, sp\n"
+"    blr  x19\n"
+"finish:\n"
+"    mov  x0, #0\n"
+"    bl   _exit\n"
+".seh_endproc\n"
+".def	_exit;	.scl	2;	.type	32;	.endef\n"
+);
+
+#endif
+
 #if defined(LIBCONTEXT_PLATFORM_linux_i386) && defined(LIBCONTEXT_COMPILER_gcc)
 __asm (
 ".text\n"
diff --git a/thirdparty/libcontext/libcontext.h b/thirdparty/libcontext/libcontext.h
index 20a1f2a3..1c16ac24 100644
--- a/thirdparty/libcontext/libcontext.h
+++ b/thirdparty/libcontext/libcontext.h
@@ -74,6 +74,12 @@
         #define LIBCONTEXT_PLATFORM_windows_i386
         #define LIBCONTEXT_CALL_CONVENTION __cdecl
     #endif
+
+    #if defined(__aarch64__)
+        #define LIBCONTEXT_COMPILER_gcc
+        #define LIBCONTEXT_PLATFORM_windows_arm64
+        #define LIBCONTEXT_CALL_CONVENTION
+    #endif
     #elif defined(__APPLE__) && defined(__MACH__)
     #if defined(__i386__)
         #define LIBCONTEXT_PLATFORM_apple_i386
@@ -90,12 +96,12 @@
 
 #define LIBCONTEXT_CALL_CONVENTION __cdecl
 
-#if defined(_WIN64)
+#if defined( _M_ARM64 )
+    #define LIBCONTEXT_PLATFORM_msvc_arm64
+#elif defined(_WIN64)
 	#define LIBCONTEXT_PLATFORM_msvc_x86_64
 #elif defined(_WIN32)
 	#define LIBCONTEXT_PLATFORM_msvc_i386
-#elif defined( _M_ARM64 )
-    #define LIBCONTEXT_PLATFORM_msvc_arm64
 #endif
 #endif
 
