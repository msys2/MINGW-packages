# Maintainer: J. Peter Mugaas <jpmugaas@suddenlink.net>

_realname='pycodestyle'
pkgbase="mingw-w64-python-${_realname}"
pkgname="${MINGW_PACKAGE_PREFIX}-python-${_realname}"
# should be updated together with python-flake8 and python-pyflakes
pkgver=2.15.0
pkgrel=1
pkgdesc='Python style guide checker (mingw-w64)'
arch=('any')
mingw_arch=('mingw64' 'ucrt64' 'clang64' 'clangarm64')
url='https://pycodestyle.pycqa.org/'
msys2_repository_url='https://github.com/pycqa/pycodestyle/'
msys2_references=(
  'anitya: 11773'
  'archlinux: python-pycodestyle'
  'gentoo: dev-python/pycodestyle'
  'purl: pkg:pypi/pycodestyle'
)
license=('spdx:MIT')
depends=(
  "${MINGW_PACKAGE_PREFIX}-python"
)
makedepends=(
  "${MINGW_PACKAGE_PREFIX}-python-build"
  "${MINGW_PACKAGE_PREFIX}-python-installer"
  "${MINGW_PACKAGE_PREFIX}-python-setuptools"
)
options=('!strip')
source=(
  "https://pypi.org/packages/source/${_realname::1}/${_realname}/${_realname}-${pkgver}.tar.gz"
)
sha256sums=('318f5db083869b4c4dad922d0b11124fb27ab181b6730b93371da671e31bd50e')

build() {
  cp -r "${_realname}-${pkgver}" "build-${MSYSTEM}" && cd "build-${MSYSTEM}"

  python -m build --wheel --skip-dependency-check --no-isolation
}

check() {
  cd "build-${MSYSTEM}"

  python -m unittest discover testsuite || warning "Tests failed"
}

package() {
  cd "build-${MSYSTEM}"

  MSYS2_ARG_CONV_EXCL="--prefix=" \
    python -m installer --prefix="${MINGW_PREFIX}" --destdir="${pkgdir}" dist/*.whl

  install -Dm644 LICENSE -t "${pkgdir}${MINGW_PREFIX}/share/licenses/python-${_realname}"
}
