--- a/Makerules
+++ b/Makerules
@@ -375,3 +375,7 @@
   HAVE_LIBCRYPTO=no
   CFLAGS += -pthread
 endif
+
+ifeq ($(OS),$(filter $(OS),Linux MINGW Windows_NT))
+  CFLAGS += -msse4.1
+endif
\ No newline at end of file
