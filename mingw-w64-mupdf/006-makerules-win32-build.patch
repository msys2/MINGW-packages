--- a/Makerules
+++ b/Makerules
@@ -212,6 +212,11 @@
   CFLAGS += -DCLUSTER
 endif
 
+ifeq ($(OS),$(filter $(OS),Linux MINGW Windows_NT))
+  WINDRES := windres
+  HAVE_WIN32 := yes
+endif
+
 ifeq ($(OS),Darwin)
   HAVE_GLUT := yes
   SYS_GLUT_CFLAGS := -Wno-deprecated-declarations
