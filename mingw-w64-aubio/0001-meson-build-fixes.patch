--- a/meson.build
+++ b/meson.build
@@ -2,1 +2,1 @@
-project('aubio', 'c')
+project('aubio', 'c', version : '0.5.0-alpha')
@@ -5,2 +5,8 @@
 subdir ('examples')
-subdir ('tests')
+pkgconfig = import('pkgconfig')
+pkgconfig.generate(
+  aubio,
+  name : 'aubio',
+  filebase : 'aubio',
+  description : 'a library for audio labelling',
+  version : meson.project_version())
--- a/src/meson.build
+++ b/src/meson.build
@@ -25,1 +25,1 @@
-  conf_data.set ('HAVE_STDARG_H', 1)
+  conf_data.set ('HAVE_LIMITS_H', 1)
@@ -35,0 +36,6 @@
+fftw3f = dependency('fftw3f', version : '>= 3.0.0', required : false)
+if fftw3f.found()
+  dependencies += [fftw3f]
+  conf_data.set ('HAVE_FFTW3', 1)
+  conf_data.set ('HAVE_FFTW3F', 1)
+endif
@@ -80,1 +80,1 @@
-if flac.found()
+if rubberband.found()
@@ -85,1 +85,1 @@
-audiotoolbox = dependency('appleframeworks', modules : ['CoreFoundation', 'AudioToolbox'])
+audiotoolbox = dependency('appleframeworks', modules : ['CoreFoundation', 'AudioToolbox'], required : false)
@@ -92,1 +92,1 @@
-accelerate = dependency('appleframeworks', modules : 'Accelerate')
+accelerate = dependency('appleframeworks', modules : 'Accelerate', required : false)
@@ -173,2 +173,80 @@
 aubio = both_libraries('aubio', sources, c_args : c_args, dependencies : dependencies,
-    soversion: '5.4.8' )
+    soversion: '5',
+    install : true )
+install_headers(
+  'aubio.h',
+  'cvec.h',
+  'fmat.h',
+  'fvec.h',
+  'lvec.h',
+  'mathutils.h',
+  'musicutils.h',
+  'types.h',
+  'vecutils.h',
+  subdir : 'aubio')
+install_headers(
+  'effects/pitchshift.h',
+  'effects/timestretch.h',
+  subdir : 'aubio/effects')
+install_headers(
+  'io/audio_unit.h',
+  'io/ioutils.h',
+  'io/sink.h',
+  'io/sink_apple_audio.h',
+  'io/sink_sndfile.h',
+  'io/sink_wavwrite.h',
+  'io/source.h',
+  'io/source_apple_audio.h',
+  'io/source_avcodec.h',
+  'io/source_sndfile.h',
+  'io/source_wavread.h',
+  subdir : 'aubio/io')
+install_headers(
+  'notes/notes.h',
+  subdir : 'aubio/notes')
+install_headers(
+  'onset/onset.h',
+  'onset/peakpicker.h',
+  subdir : 'aubio/onset')
+install_headers(
+  'pitch/pitch.h',
+  'pitch/pitchfcomb.h',
+  'pitch/pitchmcomb.h',
+  'pitch/pitchschmitt.h',
+  'pitch/pitchspecacf.h',
+  'pitch/pitchyin.h',
+  'pitch/pitchyinfast.h',
+  'pitch/pitchyinfft.h',
+  subdir : 'aubio/pitch')
+install_headers(
+  'spectral/awhitening.h',
+  'spectral/dct.h',
+  'spectral/fft.h',
+  'spectral/filterbank.h',
+  'spectral/filterbank_mel.h',
+  'spectral/mfcc.h',
+  'spectral/phasevoc.h',
+  'spectral/specdesc.h',
+  'spectral/tss.h',
+  subdir : 'aubio/spectral')
+install_headers(
+  'synth/sampler.h',
+  'synth/wavetable.h',
+  subdir : 'aubio/synth')
+install_headers(
+  'tempo/beattracking.h',
+  'tempo/tempo.h',
+  subdir : 'aubio/tempo')
+install_headers(
+  'temporal/a_weighting.h',
+  'temporal/biquad.h',
+  'temporal/c_weighting.h',
+  'temporal/filter.h',
+  'temporal/resampler.h',
+  subdir : 'aubio/temporal')
+install_headers(
+  'utils/hist.h',
+  'utils/log.h',
+  'utils/parameter.h',
+  'utils/scale.h',
+  subdir : 'aubio/utils')
--- a/examples/meson.build
+++ b/examples/meson.build
@@ -26,1 +26,2 @@
-            link_with : aubio)
+            link_with : aubio,
+            install : true)
