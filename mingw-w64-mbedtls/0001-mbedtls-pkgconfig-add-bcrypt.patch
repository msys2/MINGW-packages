--- a/pkgconfig/CMakeLists.txt
+++ b/pkgconfig/CMakeLists.txt
@@ -11,1 +11,5 @@
-  configure_file(mbedcrypto.pc.in mbedcrypto.pc @ONLY)
+  set(MBEDTLS_PKGCONFIG_LIBS_PRIVATE "")
+  if(WIN32)
+    set(MBEDTLS_PKGCONFIG_LIBS_PRIVATE "-lbcrypt")
+  endif()
+  configure_file(mbedcrypto.pc.in mbedcrypto.pc @ONLY)
--- a/pkgconfig/mbedcrypto.pc.in
+++ b/pkgconfig/mbedcrypto.pc.in
@@ -10,1 +10,2 @@
-Libs: -L"${libdir}" -ltfpsacrypto
+Libs: -L"${libdir}" -ltfpsacrypto
+Libs.private: @MBEDTLS_PKGCONFIG_LIBS_PRIVATE@
--- a/tf-psa-crypto/pkgconfig/CMakeLists.txt
+++ b/tf-psa-crypto/pkgconfig/CMakeLists.txt
@@ -11,1 +11,5 @@
-  configure_file(tfpsacrypto.pc.in tfpsacrypto.pc @ONLY)
+  set(TF_PSA_CRYPTO_PKGCONFIG_LIBS_PRIVATE "")
+  if(WIN32)
+    set(TF_PSA_CRYPTO_PKGCONFIG_LIBS_PRIVATE "-lbcrypt")
+  endif()
+  configure_file(tfpsacrypto.pc.in tfpsacrypto.pc @ONLY)
--- a/tf-psa-crypto/pkgconfig/tfpsacrypto.pc.in
+++ b/tf-psa-crypto/pkgconfig/tfpsacrypto.pc.in
@@ -10,1 +10,2 @@
-Libs: -L"${libdir}" -ltfpsacrypto
+Libs: -L"${libdir}" -ltfpsacrypto
+Libs.private: @TF_PSA_CRYPTO_PKGCONFIG_LIBS_PRIVATE@
