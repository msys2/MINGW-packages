--- qtwebkit-tp5/Source/WebKit/PlatformQt.cmake.orig	2017-01-27 00:27:15.000000000 +0300
+++ qtwebkit-tp5/Source/WebKit/PlatformQt.cmake	2017-02-10 10:25:41.539860000 +0300
@@ -437,7 +437,7 @@
     INSTALL
 )
 
-if (KDE_INSTALL_USE_QT_SYS_PATHS)
+if (KDE_INSTALL_USE_QT_SYS_PATHS OR MINGW)
     set(WebKit_PRI_ARGUMENTS
         INCLUDE_INSTALL_DIR "$$QT_MODULE_INCLUDE_BASE"
         INCLUDE_INSTALL_DIR2 "$$QT_MODULE_INCLUDE_BASE/QtWebKit"
@@ -464,6 +464,32 @@
 )
 install(FILES ${WebKit_PRI_FILENAME} DESTINATION ${ECM_MKSPECS_INSTALL_DIR})
 
+if (KDE_INSTALL_USE_QT_SYS_PATHS OR MINGW)
+    set(WebKit_Private_PRI_ARGUMENTS
+        INCLUDE_INSTALL_DIR "$$QT_MODULE_INCLUDE_BASE/${PROJECT_VERSION_STRING}"
+        INCLUDE_INSTALL_DIR2 "$$QT_MODULE_INCLUDE_BASE/${PROJECT_VERSION_STRING}/QtWebKit"
+        LIB_INSTALL_DIR "$$QT_MODULE_LIB_BASE"
+    )
+else ()
+    set(WebKit_Private_PRI_ARGUMENTS
+        INCLUDE_INSTALL_DIR ${KDE_INSTALL_INCLUDEDIR}/${PROJECT_VERSION_STRING}
+        INCLUDE_INSTALL_DIR2 "${KDE_INSTALL_INCLUDEDIR}/${PROJECT_VERSION_STRING}/QtWebKit"
+        SET_RPATH ON
+    )
+endif ()
+ecm_generate_pri_file(
+    BASE_NAME webkit_private
+    LIB_NAME QtWebKit
+    DEPS "webkit"
+    RUNTIME_DEPS " "
+    DEFINES " "
+    QT_MODULES webkit
+    EXTRA_LIBS " "
+    FILENAME_VAR WebKit_Private_PRI_FILENAME
+    ${WebKit_Private_PRI_ARGUMENTS}
+)
+install(FILES ${WebKit_Private_PRI_FILENAME} DESTINATION ${ECM_MKSPECS_INSTALL_DIR})
+
 if (QT_STATIC_BUILD)
     set(WebKit_LIBRARY_TYPE STATIC)
 else ()
@@ -602,7 +637,7 @@
     INSTALL
 )
 
-if (KDE_INSTALL_USE_QT_SYS_PATHS)
+if (KDE_INSTALL_USE_QT_SYS_PATHS OR MINGW)
     set(WebKitWidgets_PRI_ARGUMENTS
         INCLUDE_INSTALL_DIR "$$QT_MODULE_INCLUDE_BASE"
         INCLUDE_INSTALL_DIR2 "$$QT_MODULE_INCLUDE_BASE/QtWebKitWidgets"
@@ -628,6 +663,32 @@
 )
 install(FILES ${WebKitWidgets_PRI_FILENAME} DESTINATION ${ECM_MKSPECS_INSTALL_DIR})
 
+if (KDE_INSTALL_USE_QT_SYS_PATHS OR MINGW)
+    set(WebKitWidgets_Private_PRI_ARGUMENTS
+        INCLUDE_INSTALL_DIR "$$QT_MODULE_INCLUDE_BASE/${PROJECT_VERSION_STRING}"
+        INCLUDE_INSTALL_DIR2 "$$QT_MODULE_INCLUDE_BASE/${PROJECT_VERSION_STRING}/QtWebKitWidgets"
+        LIB_INSTALL_DIR "$$QT_MODULE_LIB_BASE"
+    )
+else ()
+    set(WebKitWidgets_Private_PRI_ARGUMENTS
+        INCLUDE_INSTALL_DIR ${KDE_INSTALL_INCLUDEDIR}/${PROJECT_VERSION_STRING}
+        INCLUDE_INSTALL_DIR2 "${KDE_INSTALL_INCLUDEDIR}/${PROJECT_VERSION_STRING}/QtWebKitWidgets"
+        SET_RPATH ON
+    )
+endif ()
+ecm_generate_pri_file(
+    BASE_NAME webkitwidgets_private
+    LIB_NAME QtWebKitWidgets
+    DEPS "webkitwidgets"
+    RUNTIME_DEPS " "
+    DEFINES " "
+    QT_MODULES webkitwidgets
+    EXTRA_LIBS " "
+    FILENAME_VAR WebKitWidgets_Private_PRI_FILENAME
+    ${WebKitWidgets_Private_PRI_ARGUMENTS}
+)
+install(FILES ${WebKitWidgets_Private_PRI_FILENAME} DESTINATION ${ECM_MKSPECS_INSTALL_DIR})
+
 if (MSVC)
     if (CMAKE_SIZEOF_VOID_P EQUAL 8)
         enable_language(ASM_MASM)
