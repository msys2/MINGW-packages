--- gimp-3.0.6/meson.build.orig	2025-10-06 20:00:09.645778900 +0200
+++ gimp-3.0.6/meson.build	2025-10-06 20:01:25.862550800 +0200
@@ -337,7 +337,7 @@
   elif cc.get_argument_syntax() == 'msvc'
     # CodeView symbols for DIA or DbgHelp debuggers and LLDB on Windows
     debugging_format = 'Native'
-  elif platform_windows and get_option('win-debugging') == 'native' and cc.has_argument('-gcodeview') and cc.has_link_argument('-Wl,--pdb=') and cc.get_id() == 'clang'
+  elif platform_windows and get_option('win-debugging') == 'native' and cc.has_argument('-gcodeview') and cc.has_link_argument('-Wl,--pdb=') and cc.get_id() == 'clang' and false
     # CodeView symbols for DIA or DbgHelp debuggers and LLDB on Windows
     debugging_format = 'native'
     compiler_args += '-gcodeview'
