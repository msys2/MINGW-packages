--- glade-3.18.0/src/glade-http.c.orig	2014-03-25 23:09:11.209000000 +0400
+++ glade-3.18.0/src/glade-http.c	2014-03-25 23:11:04.496200000 +0400
@@ -185,7 +185,7 @@
 
   if (priv->data->len != count)
     {
-      g_warning ("priv->data_size = %ld, write = %ld", priv->data->len, count);
+      g_warning ("priv->data_size = %zd, write = %zd", priv->data->len, count);
       return;
     }
 
