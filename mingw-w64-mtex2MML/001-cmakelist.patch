--- CMakeLists.txt	2021-02-08 19:57:57.965587800 -0700
+++ CMakeLists.txt.new	2021-02-08 19:57:58.105599900 -0700
@@ -40,9 +40,11 @@
 #   Installation paths
 #
 #
-set(BIN_INSTALL_DIR bin CACHE PATH "Where to install binaries to.")
-set(LIB_INSTALL_DIR lib CACHE PATH "Where to install libraries to.")
-set(INCLUDE_INSTALL_DIR include CACHE PATH "Where to install headers to.")
+
+include(GNUInstallDirs)
+# set(BIN_INSTALL_DIR bin CACHE PATH "Where to install binaries to.")
+# set(LIB_INSTALL_DIR lib CACHE PATH "Where to install libraries to.")
+# set(INCLUDE_INSTALL_DIR include/${PROJECT_NAME} CACHE PATH "Where to install headers to.")
 
 #
 #
@@ -107,8 +109,13 @@
 )
 configure_file(${CMAKE_CURRENT_SOURCE_DIR}/src/libmtex2MML.pc.in
 ${CMAKE_CURRENT_BINARY_DIR}/libmtex2MML.pc @ONLY)
+
+install(TARGETS ${PROJECT_NAME} RUNTIME DESTINATION ${CMAKE_INSTALL_BINDIR})
+install(TARGETS ${LIBRARY} LIBRARY DESTINATION ${CMAKE_INSTALL_LIBDIR})
+install(TARGETS ${STATICLIBRARY} ARCHIVE DESTINATION ${CMAKE_INSTALL_LIBDIR})
+install(FILES ${SRC_H} DESTINATION ${CMAKE_INSTALL_INCLUDEDIR}/${PROJECT_NAME})
 install(FILES ${CMAKE_CURRENT_BINARY_DIR}/libmtex2MML.pc
-  DESTINATION lib/pkgconfig)
+  DESTINATION ${CMAKE_INSTALL_LIBDIR}/pkgconfig)
 
 if(UNIX AND NOT APPLE AND NOT CYGWIN)
   target_link_libraries(${PROGRAM} ${M_LIB})
