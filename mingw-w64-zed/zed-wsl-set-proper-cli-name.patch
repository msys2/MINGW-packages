--- a/crates/zed/resources/windows/zed.sh
+++ b/crates/zed/resources/windows/zed.sh
@@ -17,9 +17,9 @@
     if [ -z "$WSL_USER" ]; then
         WSL_USER="$USERNAME"
     fi
-    "$ZED_PATH/zed.exe" --wsl "$WSL_USER@$WSL_DISTRO_NAME" "$@"
+    "$ZED_PATH/zeditor.exe" --wsl "$WSL_USER@$WSL_DISTRO_NAME" "$@"
     exit $?
 else
-    "$ZED_PATH/zed.exe" "$@"
+    "$ZED_PATH/zeditor.exe" "$@"
     exit $?
 fi
