--- elementary-icon-theme-9.0.0/cursors/meson.build.orig	2026-08-23 10:57:06.000000000 +0800
+++ elementary-icon-theme-9.0.0/cursors/meson.build	2026-08-27 22:14:30.651260000 +0800
@@ -1,6 +1,5 @@
 cursor_dir = join_paths(icon_dir, 'cursors')
 rsvg = find_program('rsvg-convert')
-xcursorgen = find_program('xcursorgen')
 
 cursors = [
     'alias',
@@ -118,22 +117,6 @@
     'wait'
 ]
 
-foreach cursor : cursors
-    message('generating: ' + cursor)
-
-    run_command(
-        xcursorgen,
-        cursor + '.in',
-        '-p', join_paths (meson.project_build_root(), 'cursors'),
-        join_paths (meson.project_build_root(), 'cursors', cursor)
-    )
-
-    install_data(
-        join_paths (meson.project_build_root(), 'cursors', cursor),
-        install_dir: cursor_dir
-    )
-endforeach
-
 install_subdir(
     'cursors_scalable',
     install_dir: icon_dir
