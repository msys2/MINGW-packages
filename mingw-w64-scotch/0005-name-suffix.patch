diff -urN scotch-v7.0.5_orig/CMakeLists.txt scotch-v7.0.5/CMakeLists.txt
--- scotch-v7.0.5_orig/CMakeLists.txt	2024-08-13 11:00:50.000000000 +0200
+++ scotch-v7.0.5/CMakeLists.txt	2024-10-17 20:20:18.972597700 +0200
@@ -126,13 +126,13 @@
 )
 foreach(f IN LISTS scotch_man_pages)
   install(FILES ${CMAKE_CURRENT_SOURCE_DIR}/man/man1/${f}
-    DESTINATION ${CMAKE_INSTALL_DATADIR}/man/man1 COMPONENT scotch)
+    DESTINATION ${CMAKE_INSTALL_DATADIR}/man/man1${NAME_SUFFIX} COMPONENT scotch)
 endforeach()
 
 if (BUILD_PTSCOTCH)
   set(ptscotch_man_pages dgmap.1 dgord.1 dgscat.1 dgtst.1)
   foreach(f IN LISTS ptscotch_man_pages)
     install(FILES ${CMAKE_CURRENT_SOURCE_DIR}/man/man1/${f}
-      DESTINATION ${CMAKE_INSTALL_DATADIR}/man/man1 COMPONENT ptscotch)
+      DESTINATION ${CMAKE_INSTALL_DATADIR}/man/man1${NAME_SUFFIX} COMPONENT ptscotch)
   endforeach()
 endif(BUILD_PTSCOTCH)
diff -urN scotch-v7.0.5_orig/src/CMakeLists.txt scotch-v7.0.5/src/CMakeLists.txt
--- scotch-v7.0.5_orig/src/CMakeLists.txt	2024-08-13 11:00:50.000000000 +0200
+++ scotch-v7.0.5/src/CMakeLists.txt	2024-10-17 20:30:01.428100000 +0200
@@ -205,7 +205,7 @@
 
 configure_package_config_file(${PROJECT_SOURCE_DIR}/cmake/SCOTCHConfig.cmake.in
   ${CMAKE_CURRENT_BINARY_DIR}/SCOTCHConfig.cmake
-  INSTALL_DESTINATION ${CMAKE_INSTALL_LIBDIR}/cmake/scotch
+  INSTALL_DESTINATION ${CMAKE_INSTALL_LIBDIR}/cmake/scotch${NAME_SUFFIX}
   PATH_VARS CMAKE_INSTALL_INCLUDEDIR CMAKE_INSTALL_LIBDIR)
 
 write_basic_package_version_file(SCOTCHConfigVersion.cmake
@@ -216,4 +216,4 @@
 install(FILES ${CMAKE_CURRENT_BINARY_DIR}/SCOTCHConfig.cmake
   ${CMAKE_CURRENT_BINARY_DIR}/SCOTCHConfigVersion.cmake
   COMPONENT scotch
-  DESTINATION ${CMAKE_INSTALL_LIBDIR}/cmake/scotch)
+  DESTINATION ${CMAKE_INSTALL_LIBDIR}/cmake/scotch${NAME_SUFFIX})
diff -urN scotch-v7.0.5_orig/src/esmumps/CMakeLists.txt scotch-v7.0.5/src/esmumps/CMakeLists.txt
--- scotch-v7.0.5_orig/src/esmumps/CMakeLists.txt	2024-08-13 11:00:50.000000000 +0200
+++ scotch-v7.0.5/src/esmumps/CMakeLists.txt	2024-10-17 20:37:29.955259800 +0200
@@ -128,8 +128,9 @@
   COMPONENT libscotch
   FILE esmumpsTargets.cmake
   NAMESPACE SCOTCH::
-  DESTINATION ${CMAKE_INSTALL_LIBDIR}/cmake/scotch)
+  DESTINATION ${CMAKE_INSTALL_LIBDIR}/cmake/scotch${NAME_SUFFIX})
 
+set_target_properties(esmumps PROPERTIES OUTPUT_NAME esmumps${NAME_SUFFIX})
 install(TARGETS esmumps
   COMPONENT libscotch
   EXPORT esmumpsTargets
@@ -162,8 +163,9 @@
   COMPONENT libptscotch
   FILE ptesmumpsTargets.cmake
   NAMESPACE SCOTCH::
-  DESTINATION ${CMAKE_INSTALL_LIBDIR}/cmake/scotch)
+  DESTINATION ${CMAKE_INSTALL_LIBDIR}/cmake/scotch${NAME_SUFFIX})
 
+set_target_properties(ptesmumps PROPERTIES OUTPUT_NAME ptesmumps${NAME_SUFFIX})
 install(TARGETS ptesmumps
   COMPONENT libptscotch
   EXPORT ptesmumpsTargets
diff -urN scotch-v7.0.5_orig/src/libscotch/CMakeLists.txt scotch-v7.0.5/src/libscotch/CMakeLists.txt
--- scotch-v7.0.5_orig/src/libscotch/CMakeLists.txt	2024-08-13 11:00:50.000000000 +0200
+++ scotch-v7.0.5/src/libscotch/CMakeLists.txt	2024-10-27 11:15:02.323859300 +0100
@@ -567,7 +567,7 @@
   target_compile_definitions(scotch PRIVATE SCOTCH_DEBUG_LIBRARY1)
 endif()
 
-add_library(scotcherr library_error.c)
+add_library(scotcherr STATIC library_error.c)
 set_target_properties(scotcherr PROPERTIES VERSION ${SCOTCH_VERSION_LONG}
   SOVERSION ${SCOTCH_VERSION}.${SCOTCH_RELEASE})
 target_include_directories(scotcherr PUBLIC
@@ -577,7 +577,17 @@
 
 add_dependencies(scotcherr scotch_h)
 
-add_library(scotcherrexit library_error_exit.c)
+option(SCOTCH_SHARED_USE_SCOTCHERR "Linking shared library against libscotcherr instead of libscotcherrexit" ON)
+
+if (BUILD_SHARED_LIBS AND SCOTCH_SHARED_USE_SCOTCHERR)
+  message(STATUS "Building shared library linked against libscotcherr")
+  target_link_libraries(scotch PRIVATE scotcherr)
+elseif (BUILD_SHARED_LIBS AND NOT SCOTCH_SHARED_USE_SCOTCHERR)
+  message(STATUS "Building shared library linked against libscotcherrexit")
+  target_link_libraries(scotch PRIVATE scotcherrexit)
+endif()
+
+add_library(scotcherrexit STATIC library_error_exit.c)
 set_target_properties(scotcherrexit PROPERTIES VERSION ${SCOTCH_VERSION_LONG}
   SOVERSION ${SCOTCH_VERSION}.${SCOTCH_RELEASE})
 target_include_directories(scotcherrexit PUBLIC
@@ -802,6 +812,10 @@
 
   add_dependencies(ptscotcherr ptscotch_h scotch_h)
 
+  if (BUILD_SHARED_LIBS)
+    target_link_libraries(ptscotch PRIVATE ptscotcherr)
+  endif()
+
   add_library(ptscotcherrexit library_error_exit.c)
   set_target_properties(ptscotcherrexit PROPERTIES VERSION ${SCOTCH_VERSION_LONG}
     SOVERSION ${SCOTCH_VERSION}.${SCOTCH_RELEASE})
@@ -822,29 +836,37 @@
 set(TARGETS_LIST "scotch;scotcherr;scotcherrexit")
 
 foreach(_target ${TARGETS_LIST})
+  #set_target_properties(${_target} PROPERTIES POSITION_INDEPENDENT_CODE
+    #${BUILD_SHARED_LIBS})
+  set_target_properties(${_target} PROPERTIES OUTPUT_NAME ${_target}${NAME_SUFFIX})
   install(EXPORT ${_target}Targets
     COMPONENT libscotch
     FILE ${_target}Targets.cmake
     NAMESPACE SCOTCH::
-    DESTINATION ${CMAKE_INSTALL_LIBDIR}/cmake/scotch)
-
+    DESTINATION ${CMAKE_INSTALL_LIBDIR}/cmake/scotch${NAME_SUFFIX})
+  
+  set_target_properties(${_target} PROPERTIES OUTPUT_NAME ${_target}${NAME_SUFFIX})
   install(TARGETS ${_target}
     COMPONENT libscotch
     EXPORT ${_target}Targets
+    RUNTIME DESTINATION ${CMAKE_INSTALL_BINDIR}
     ARCHIVE DESTINATION ${CMAKE_INSTALL_LIBDIR}
     LIBRARY DESTINATION ${CMAKE_INSTALL_LIBDIR}
     PUBLIC_HEADER DESTINATION ${CMAKE_INSTALL_INCLUDEDIR})
 
   if (BUILD_PTSCOTCH)
+    set_target_properties(pt${_target} PROPERTIES OUTPUT_NAME pt${_target}${NAME_SUFFIX})
     install(EXPORT pt${_target}Targets
       COMPONENT libptscotch
       FILE pt${_target}Targets.cmake
       NAMESPACE SCOTCH::
-      DESTINATION ${CMAKE_INSTALL_LIBDIR}/cmake/scotch)
-
+      DESTINATION ${CMAKE_INSTALL_LIBDIR}/cmake/scotch${NAME_SUFFIX})
+    
+    set_target_properties(pt${_target} PROPERTIES OUTPUT_NAME pt${_target}${NAME_SUFFIX})
     install(TARGETS pt${_target}
       COMPONENT libptscotch
       EXPORT pt${_target}Targets
+      RUNTIME DESTINATION ${CMAKE_INSTALL_BINDIR}
       ARCHIVE DESTINATION ${CMAKE_INSTALL_LIBDIR}
       LIBRARY DESTINATION ${CMAKE_INSTALL_LIBDIR}
       PUBLIC_HEADER DESTINATION ${CMAKE_INSTALL_INCLUDEDIR})
diff -urN scotch-v7.0.5_orig/src/libscotchmetis/CMakeLists.txt scotch-v7.0.5/src/libscotchmetis/CMakeLists.txt
--- scotch-v7.0.5_orig/src/libscotchmetis/CMakeLists.txt	2024-08-13 11:00:50.000000000 +0200
+++ scotch-v7.0.5/src/libscotchmetis/CMakeLists.txt	2024-10-26 21:40:01.833347200 +0200
@@ -120,7 +120,7 @@
     COMPONENT libscotch
     FILE scotchmetisTargets.cmake
     NAMESPACE SCOTCH::
-    DESTINATION ${CMAKE_INSTALL_LIBDIR}/cmake/scotch)
+    DESTINATION ${CMAKE_INSTALL_LIBDIR}/cmake/scotch${NAME_SUFFIX})
   if(INSTALL_METIS_HEADERS)
     install(TARGETS scotchmetisv${version}
       COMPONENT libscotch
@@ -192,23 +192,28 @@
     endif(APPLE)
 
     # libPTScotchParMeTiS targets install
+    set_target_properties(${_target} PROPERTIES OUTPUT_NAME ${_target}${NAME_SUFFIX})
     install(EXPORT ptscotchparmetisTargets
       COMPONENT libptscotch
       FILE ptscotchparmetisTargets.cmake
       NAMESPACE SCOTCH::
-      DESTINATION ${CMAKE_INSTALL_LIBDIR}/cmake/scotch)
+      DESTINATION ${CMAKE_INSTALL_LIBDIR}/cmake/scotch${NAME_SUFFIX})
 
     if(INSTALL_METIS_HEADERS)
+      set_target_properties(${_target} PROPERTIES OUTPUT_NAME ${_target}${NAME_SUFFIX})
       install(TARGETS ptscotchparmetisv${version}
         COMPONENT libptscotch
         EXPORT ptscotchparmetisTargets
+	RUNTIME DESTINATION ${CMAKE_INSTALL_BINDIR}
         ARCHIVE DESTINATION ${CMAKE_INSTALL_LIBDIR}
         LIBRARY DESTINATION ${CMAKE_INSTALL_LIBDIR}
         PUBLIC_HEADER DESTINATION ${CMAKE_INSTALL_INCLUDEDIR})
     else()
+      set_target_properties(${_target} PROPERTIES OUTPUT_NAME ${_target}${NAME_SUFFIX})
       install(TARGETS ptscotchparmetisv${version}
         COMPONENT libptscotch
         EXPORT ptscotchparmetisTargets
+	RUNTIME DESTINATION ${CMAKE_INSTALL_BINDIR}
         ARCHIVE DESTINATION ${CMAKE_INSTALL_LIBDIR}
         LIBRARY DESTINATION ${CMAKE_INSTALL_LIBDIR})
     endif()
diff -urN scotch-v7.0.5_orig/src/scotch/CMakeLists.txt scotch-v7.0.5/src/scotch/CMakeLists.txt
--- scotch-v7.0.5_orig/src/scotch/CMakeLists.txt	2024-08-13 11:00:50.000000000 +0200
+++ scotch-v7.0.5/src/scotch/CMakeLists.txt	2024-10-26 21:38:28.527403500 +0200
@@ -120,6 +120,7 @@
 
 # Install all Scotch executables
 foreach(target ${scotch_exe})
+  set_target_properties(${target} PROPERTIES OUTPUT_NAME ${target}${NAME_SUFFIX})
   install(TARGETS ${target} COMPONENT scotch RUNTIME DESTINATION ${CMAKE_INSTALL_BINDIR})
 endforeach()
 
@@ -167,6 +168,9 @@
   target_compile_definitions(dgpart PRIVATE SCOTCH_COMPILE_PART)
   # Install all PT-Scotch executables
   foreach(target ${ptscotch_exe})
-    install(TARGETS ${target} COMPONENT ptscotch RUNTIME DESTINATION ${CMAKE_INSTALL_BINDIR})
+    set_target_properties(${target} PROPERTIES OUTPUT_NAME ${target}${NAME_SUFFIX})
+    install(TARGETS ${target} 
+      COMPONENT ptscotch 
+      RUNTIME DESTINATION ${CMAKE_INSTALL_BINDIR})
   endforeach()
  endif(BUILD_PTSCOTCH)
