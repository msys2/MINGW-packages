# Maintainer: Alexey Pavlov <alexpux@gmail.com>
# Maintainer: Ray Donnelly <mingw.android@gmail.com>
# The traineddata and related files for English language should be placed in tesseract ocr's tessdata directory

_realname=tesseract-data-eng
pkgbase=mingw-w64-${_realname}
pkgname=("${MINGW_PACKAGE_PREFIX}-${_realname}")
pkgver=3.04.00
pkgrel=1
pkgdesc="English Language tessdata for Tesseract OCR engine (mingw-w64)"
arch=('any')
url="https://github.com/tesseract-ocr/tessdata"
license=("Apache License 2.0")
depends=()

source=(${_realname}-$pkgver.tar.gz::https://github.com/tesseract-ocr/tessdata/archive/$pkgver.tar.gz)
sha256sums=('5dcb37198336b6953843b461ee535df1401b41008d550fc9e43d0edabca7adb1')

build() {
  true
}

package() {
    mkdir -p $pkgdir/${MINGW_PREFIX}/share/tessdata
    cp $srcdir/tessdata-$pkgver/eng.* $pkgdir/${MINGW_PREFIX}/share/tessdata/
    find $pkgdir/${MINGW_PREFIX}/share/tessdata -type f -exec chmod 0644 {} \;
}
