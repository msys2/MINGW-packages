--- nspr-4.39/pr/src/md/windows/ntdllmn.c.orig	2026-07-09 23:16:11.529733700 +0800
+++ nspr-4.39/pr/src/md/windows/ntdllmn.c	2026-07-09 23:19:47.779208500 +0800
@@ -25,6 +25,7 @@
 
   switch (fdwReason) {
     case DLL_PROCESS_ATTACH:
+#ifdef _MSC_VER
       /*
        * If lpvReserved is NULL, we are dynamically loaded
        * and therefore can't use static thread-local storage.
@@ -34,6 +35,13 @@
       } else {
         _pr_use_static_tls = TRUE;
       }
+#else
+      /*
+        * No __declspec(thread) on mingw, so just use
+        * the Tls* API always.
+        */
+      _pr_use_static_tls = FALSE;
+#endif
       break;
     case DLL_THREAD_ATTACH:
       break;
