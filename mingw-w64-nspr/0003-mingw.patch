--- nspr-4.40/pr/src/md/windows/ntdllmn.c.orig	2026-08-07 23:34:23.440755700 +0800
+++ nspr-4.40/pr/src/md/windows/ntdllmn.c	2026-08-07 23:36:04.829937500 +0800
@@ -27,6 +27,7 @@
 
     switch (fdwReason) {
         case DLL_PROCESS_ATTACH:
+#ifdef _MSC_VER
             /*
              * If lpvReserved is NULL, we are dynamically loaded
              * and therefore can't use static thread-local storage.
@@ -36,6 +37,13 @@
             } else {
                 _pr_use_static_tls = TRUE;
             }
+#else
+            /*
+             * No __declspec(thread) on mingw, so just use
+             * the Tls* API always.
+             */
+            _pr_use_static_tls = FALSE;
+#endif
             break;
         case DLL_THREAD_ATTACH:
             break;
