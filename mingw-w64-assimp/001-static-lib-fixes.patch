--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -526,6 +526,7 @@
   IF( NOT ASSIMP_BUILD_MINIZIP )
     use_pkgconfig(UNZIP minizip)
   ENDIF()
+  SET(ZLIB_LIBRARIES_LINKED "-lminizip -lbz2 -lz")
 ELSE ()
   IF( NOT BUILD_SHARED_LIBS )
     IF( NOT ASSIMP_BUILD_MINIZIP )
