--- ghostpdl-9.26/pcl/pl/pjparse.c.orig	2018-11-21 08:08:53.078734200 -0500
+++ ghostpdl-9.26/pcl/pl/pjparse.c	2018-11-21 08:15:17.522815200 -0500
@@ -666,7 +666,11 @@ pjl_fsinit(pjl_parser_state_t * pst, cha
 #ifdef GS_NO_FILESYSTEM
     return -1;
 #else
+    #ifdef _WIN32
+    return _mkdir(fname);
+    #else
     return mkdir(fname, 0777);
+    #endif
 #endif
 }
 
@@ -682,7 +686,11 @@ pjl_fsmkdir(pjl_parser_state_t * pst, ch
 #ifdef GS_NO_FILESYSTEM
     return -1;
 #else
+    #ifdef _WIN32
+    return _mkdir(fname);
+    #else
     return mkdir(fname, 0777);
+    #endif
 #endif
 }
 
