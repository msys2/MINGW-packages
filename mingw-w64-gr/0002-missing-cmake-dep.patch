--- a/cmake/GenPreviewImages.cmake
+++ b/cmake/GenPreviewImages.cmake
@@ -935,7 +935,7 @@ if(NOT CMAKE_CROSSCOMPILING OR GR_HOST_DIRECTORY)
     ${GR_FILL_STYLE_IMAGES}
     COMMAND
       "${CMAKE_COMMAND}" -E env GKS_FONTPATH="${CMAKE_CURRENT_LIST_DIR}/../lib/gks" ${GR_GEN_PREVIEW_IMAGES_EXECUTABLE}
-    DEPENDS ${GR_GEN_PREVIEW_IMAGES_EXECUTABLE}
+    DEPENDS ${GR_GEN_PREVIEW_IMAGES_EXECUTABLE} cairoplugin
   )
 else()
   add_custom_command(
