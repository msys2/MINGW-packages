diff -urN icu.wip.pre-0012/source/config/mh-mingw icu.wip/source/config/mh-mingw
--- icu.wip.pre-0012/source/config/mh-mingw	2014-04-17 02:27:35.392677000 +0100
+++ icu.wip/source/config/mh-mingw	2014-04-17 02:28:50.049947200 +0100
@@ -61,13 +61,13 @@
 endif
 
 # Static library prefix and file extension
-LIBSICU = lib$(LIBPREFIX)$(STATIC_PREFIX)$(ICUPREFIX)
+LIBSICU = $(LIBPREFIX)$(STATIC_PREFIX)$(ICUPREFIX)
 A = a
 
 ## An import library is needed for z/OS and MSVC
 IMPORT_LIB_EXT = .dll.a
 
-LIBPREFIX=
+LIBPREFIX=lib
 
 # Change the stubnames so that poorly working FAT disks and installation programs can work.
 # This is also for backwards compatibility.
diff -urN icu.wip.pre-0012/source/config/mh-mingw64 icu.wip/source/config/mh-mingw64
--- icu.wip.pre-0012/source/config/mh-mingw64	2014-04-17 02:27:35.393677100 +0100
+++ icu.wip/source/config/mh-mingw64	2014-04-17 02:28:45.821705300 +0100
@@ -61,13 +61,13 @@
 endif
 
 # Static library prefix and file extension
-LIBSICU = lib$(LIBPREFIX)$(STATIC_PREFIX)$(ICUPREFIX)
+LIBSICU = $(LIBPREFIX)$(STATIC_PREFIX)$(ICUPREFIX)
 A = a
 
 ## An import library is needed for z/OS and MSVC
 IMPORT_LIB_EXT = .dll.a
 
-LIBPREFIX=
+LIBPREFIX=lib
 
 # Change the stubnames so that poorly working FAT disks and installation programs can work.
 # This is also for backwards compatibility.
