Suggested usage notes:

Add "${MINGW_PACKAGE_PREFIX}-wxmsw3.0" to depends.
And in some/most cases, add "${MINGW_PACKAGE_PREFIX}-wxWidgets3.0"
to makedepends. In very rare cases, it might be necessary to add 
"${MINGW_PACKAGE_PREFIX}-bakefile3.0" to makedepends.
