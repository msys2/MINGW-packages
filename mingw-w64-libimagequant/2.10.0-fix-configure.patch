--- libimagequant-2.10.0/configure.orig	2017-07-02 17:40:53.000000000 -0400
+++ libimagequant-2.10.0/configure	2017-07-04 00:03:19.381744500 -0400
@@ -174,14 +174,18 @@ if [ -n "$OPENMP" ]; then
     else
         OPENMPFLAGS="-fopenmp"
     fi
-    if [[ "$("$CC" -xc -E $OPENMPFLAGS <(echo "#ifdef _OPENMP
+    if [[ $MSYSTEM -ne "MINGW64" ]] && [[ $MSYSTEM! -ne "MINGW32" ]]; then
+      if [[ "$("$CC" -xc -E $OPENMPFLAGS <(echo "#ifdef _OPENMP
            #include <omp.h>
            #endif") 2>&1)" =~ "omp_get_thread_num" ]]; then
         cflags "$OPENMPFLAGS"
         lflags "$OPENMPFLAGS"
         status "OpenMP" "yes"
-    else
+      else
         error "OpenMP" "not supported by compiler (please install a compiler that supports OpenMP (e.g. gcc) and specify it with the CC= argument)"
+      fi
+    else
+      status "OpenMP" "yes"
     fi
 else
     # silence warnings about omp pragmas
