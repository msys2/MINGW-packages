diff -Naur highlight-4.19-orig/src/core/lspclient.cpp highlight-4.19/src/core/lspclient.cpp
--- highlight-4.19-orig/src/core/lspclient.cpp	2026-02-18 18:06:54.000000000 -0300
+++ highlight-4.19/src/core/lspclient.cpp	2026-04-22 01:33:55.986561387 -0300
@@ -365,7 +365,7 @@
             while (totalRead < remainingSize && std::chrono::steady_clock::now() - bodyStartTime < std::chrono::seconds(10)) {
                 #ifdef _WIN32
                 DWORD bytesRead = 0;
-                if (!ReadFile(g_hChildStd_OUT_Rd, (void*)buffer.data() + totalRead, remainingSize - totalRead, &bytesRead, NULL) || bytesRead == 0) {
+                if (!ReadFile(g_hChildStd_OUT_Rd, (void*)((char*)buffer.data() + totalRead), remainingSize - totalRead, &bytesRead, NULL) || bytesRead == 0) {
                      return ""; // Error or pipe closed
                 }
                 #else
