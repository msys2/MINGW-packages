diff --git a/src/3rdparty/chromium/media/gpu/windows/supported_profile_helpers.cc b/src/3rdparty/chromium/media/gpu/windows/supported_profile_helpers.cc
index c3e6b8e8a41..f2ad1171a78 100644
--- a/src/3rdparty/chromium/media/gpu/windows/supported_profile_helpers.cc
+++ b/src/3rdparty/chromium/media/gpu/windows/supported_profile_helpers.cc
@@ -447,14 +447,18 @@ SupportedResolutionRangeMap GetSupportedD3D11VideoDecoderResolutions(
 SupportedResolutionRangeMap GetSupportedD3D12VideoDecoderResolutions(
     ComD3D12Device device,
     const gpu::GpuDriverBugWorkarounds& workarounds) {
-  ComD3D12VideoDevice video_device;
-  std::unique_ptr<D3D12VideoDeviceWrapper> video_device_wrapper;
-  if (device && SUCCEEDED(device.As(&video_device))) {
-    video_device_wrapper =
-        std::make_unique<D3D12VideoDeviceWrapper>(video_device);
-  }
-  return GetSupportedD3DVideoDecoderResolutions(video_device_wrapper.get(),
-                                                workarounds);
+#if BUILDFLAG(IS_MINGW)
+  return {};
+#else
+  ComD3D12VideoDevice video_device;
+  std::unique_ptr<D3D12VideoDeviceWrapper> video_device_wrapper;
+  if (device && SUCCEEDED(device.As(&video_device))) {
+    video_device_wrapper =
+        std::make_unique<D3D12VideoDeviceWrapper>(video_device);
+  }
+  return GetSupportedD3DVideoDecoderResolutions(video_device_wrapper.get(),
+                                                workarounds);
+#endif
 }
 
 UINT GetGPUVendorID(ComDXGIDevice dxgi_device) {
