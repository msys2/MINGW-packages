diff -ruN patch0126-before/src/3rdparty/chromium/components/viz/service/display_embedder/skia_output_device_dcomp.cc patch0126-after/src/3rdparty/chromium/components/viz/service/display_embedder/skia_output_device_dcomp.cc
--- a/src/3rdparty/chromium/components/viz/service/display_embedder/skia_output_device_dcomp.cc	2026-06-18 14:32:31.594016200 +0200
+++ b/src/3rdparty/chromium/components/viz/service/display_embedder/skia_output_device_dcomp.cc	2026-06-18 14:36:51.243153200 +0200
@@ -57,10 +57,12 @@
     Microsoft::WRL::ComPtr<IDCompositionDevice3> dcomp_device =
         gl::GetDirectCompositionDevice();
 
+#if !defined(__MINGW32__)
     if (Microsoft::WRL::ComPtr<PREVIEW_IDCompositionDevice5> dcomp_device5;
         SUCCEEDED(dcomp_device.As(&dcomp_device5))) {
       return OutputSurface::DCSupportLevel::kDCompDynamicTexture;
     }
+#endif
 
     return OutputSurface::DCSupportLevel::kDCompTexture;
   }
diff -ruN patch0126-before/src/3rdparty/chromium/content/child/BUILD.gn patch0126-after/src/3rdparty/chromium/content/child/BUILD.gn
--- a/src/3rdparty/chromium/content/child/BUILD.gn	2026-06-18 14:32:30.771426600 +0200
+++ b/src/3rdparty/chromium/content/child/BUILD.gn	2026-06-18 14:36:50.431548900 +0200
@@ -146,6 +146,8 @@
 
   if (is_mingw) {
     sources += [
+      "child_process_sandbox_support_impl_win.cc",
+      "child_process_sandbox_support_impl_win.h",
       "font_warmup_win.cc",
       "font_warmup_win.h",
     ]
diff -ruN patch0126-before/src/3rdparty/chromium/gpu/command_buffer/service/BUILD.gn patch0126-after/src/3rdparty/chromium/gpu/command_buffer/service/BUILD.gn
--- a/src/3rdparty/chromium/gpu/command_buffer/service/BUILD.gn	2026-06-18 14:32:30.834876300 +0200
+++ b/src/3rdparty/chromium/gpu/command_buffer/service/BUILD.gn	2026-06-18 14:36:50.493133000 +0200
@@ -619,12 +619,18 @@
 
   if (is_mingw) {
     sources += [
+      "dxgi_shared_handle_manager.cc",
+      "dxgi_shared_handle_manager.h",
+      "shared_image/d3d_image_backing.cc",
+      "shared_image/d3d_image_backing.h",
+      "shared_image/d3d_image_backing_factory.cc",
+      "shared_image/d3d_image_backing_factory.h",
       "shared_image/d3d_image_representation.cc",
       "shared_image/d3d_image_representation.h",
       "shared_image/d3d_image_utils.cc",
       "shared_image/d3d_image_utils.h",
     ]
-    libs = [ "dxguid" ]
+    libs = [ "d3d12", "dxguid", "onecore" ]
     all_dependent_configs = [ ":service_win_linker_flags" ]
   }
 
diff -ruN patch0126-before/src/3rdparty/chromium/gpu/command_buffer/service/shared_image/d3d_image_backing.cc patch0126-after/src/3rdparty/chromium/gpu/command_buffer/service/shared_image/d3d_image_backing.cc
--- a/src/3rdparty/chromium/gpu/command_buffer/service/shared_image/d3d_image_backing.cc	2026-06-18 14:50:24.405259500 +0200
+++ b/src/3rdparty/chromium/gpu/command_buffer/service/shared_image/d3d_image_backing.cc	2026-06-18 14:50:04.461087200 +0200
@@ -1005,12 +1005,14 @@
 }
 #endif  // BUILDFLAG(USE_DAWN)
 
+#if BUILDFLAG(IS_MSVC)
 void D3DImageBacking::UpdateExternalFence(
     scoped_refptr<gfx::D3DSharedFence> external_fence) {
   // TODO(crbug.com/40192861): Handle cases that write_fences_ is not empty.
   AutoLock auto_lock(this);
   write_fences_.insert(std::move(external_fence));
 }
+#endif
 
 std::unique_ptr<VideoImageRepresentation> D3DImageBacking::ProduceVideo(
     SharedImageManager* manager,
@@ -1907,6 +1909,7 @@
 
 std::optional<gl::DCLayerOverlayImage>
 D3DImageBacking::GetDCLayerOverlayImage() {
+#if !defined(__MINGW32__)
   if (dcomp_texture_) {
     return std::make_optional<gl::DCLayerOverlayImage>(
         size(), dcomp_texture_, /*dcomp_surface_serial=*/0);
@@ -1914,6 +1917,7 @@
   if (swap_chain_) {
     return std::make_optional<gl::DCLayerOverlayImage>(size(), swap_chain_);
   }
+#endif
   return std::make_optional<gl::DCLayerOverlayImage>(size(), d3d11_texture_,
                                                      array_slice_);
 }
diff -ruN patch0126-before/src/3rdparty/chromium/gpu/config/gpu_info_collector_win.cc patch0126-after/src/3rdparty/chromium/gpu/config/gpu_info_collector_win.cc
--- a/src/3rdparty/chromium/gpu/config/gpu_info_collector_win.cc	2026-06-18 14:32:30.900403900 +0200
+++ b/src/3rdparty/chromium/gpu/config/gpu_info_collector_win.cc	2026-06-18 14:36:50.554553500 +0200
@@ -207,6 +207,9 @@
 
 void CollectNPUInformation(GPUInfo* gpu_info) {
   // Enumerate all dxcore adapters to retrieve the NPUs.
+#if defined(__MINGW32__)
+  return;
+#else
   base::ScopedNativeLibrary dxcore_library(
       base::LoadSystemLibrary(L"DXCore.dll"));
   if (!dxcore_library.is_valid()) {
@@ -314,6 +317,7 @@
       gpu_info->npus.push_back(device);
     }
   }
+#endif
 }
 
 bool CollectDriverInfoD3D(GPUInfo* gpu_info) {
@@ -508,6 +512,9 @@
       if (!dml_library.is_valid()) {
         return;
       }
+#if defined(__MINGW32__)
+      return;
+#else
       // On older versions of windows DMLCreateDevice accepts a different
       // number of parameters. We should use DMLCreateDevice1 which always
       // takes the same number of parameters to ensure consistency
@@ -541,6 +548,7 @@
         directml_feature_level =
             feature_levels_supported.MaxSupportedFeatureLevel;
       }
+#endif
     }
   }
 }
diff -ruN patch0126-before/src/3rdparty/chromium/media/base/win/mf_initializer.cc patch0126-after/src/3rdparty/chromium/media/base/win/mf_initializer.cc
--- a/src/3rdparty/chromium/media/base/win/mf_initializer.cc	2026-06-18 14:32:30.975684300 +0200
+++ b/src/3rdparty/chromium/media/base/win/mf_initializer.cc	2026-06-18 14:36:50.619917800 +0200
@@ -113,6 +113,9 @@
   HRESULT ResolveMediaFoundationDelayloads() {
     // Mitigate the issues caused by loading DLLs on a background thread; see
     // https://crbug.com/41464781. The DLLs should already be loaded on account
+#if defined(__MINGW32__)
+    return S_OK;
+#else
     // of `LoadMediaFoundationLibraries()`, but it is possible that resolving
     // the imports could lead to hard faults to page in the DLLs.
     SCOPED_MAY_LOAD_LIBRARY_AT_BACKGROUND_PRIORITY();
@@ -155,6 +158,7 @@
       }
     }
     return S_OK;
+#endif
   }
 
   bool has_media_foundation_ = false;
diff -ruN patch0126-before/src/3rdparty/chromium/media/capture/video/video_capture_buffer_tracker_factory_impl.cc patch0126-after/src/3rdparty/chromium/media/capture/video/video_capture_buffer_tracker_factory_impl.cc
--- a/src/3rdparty/chromium/media/capture/video/video_capture_buffer_tracker_factory_impl.cc	2026-06-18 14:32:31.048844100 +0200
+++ b/src/3rdparty/chromium/media/capture/video/video_capture_buffer_tracker_factory_impl.cc	2026-06-18 14:36:50.687325000 +0200
@@ -23,7 +23,7 @@
 
 VideoCaptureBufferTrackerFactoryImpl::VideoCaptureBufferTrackerFactoryImpl() {}
 
-#if BUILDFLAG(IS_MSVC)
+#if BUILDFLAG(IS_WIN)
 VideoCaptureBufferTrackerFactoryImpl::VideoCaptureBufferTrackerFactoryImpl(
     scoped_refptr<DXGIDeviceManager> dxgi_device_manager)
     : dxgi_device_manager_(std::move(dxgi_device_manager)) {}
diff -ruN patch0126-before/src/3rdparty/chromium/media/device_monitors/BUILD.gn patch0126-after/src/3rdparty/chromium/media/device_monitors/BUILD.gn
--- a/src/3rdparty/chromium/media/device_monitors/BUILD.gn	2026-06-18 14:32:31.110207900 +0200
+++ b/src/3rdparty/chromium/media/device_monitors/BUILD.gn	2026-06-18 14:37:21.991119700 +0200
@@ -37,7 +37,7 @@
     frameworks = [ "Foundation.framework" ]
   }
 
-  if (is_msvc) {
+  if (is_win) {
     sources += [
       "system_message_window_win.cc",
       "system_message_window_win.h",
diff -ruN patch0126-before/src/3rdparty/chromium/printing/common/metafile_utils.cc patch0126-after/src/3rdparty/chromium/printing/common/metafile_utils.cc
--- a/src/3rdparty/chromium/printing/common/metafile_utils.cc	2026-06-18 15:03:15.424835300 +0200
+++ b/src/3rdparty/chromium/printing/common/metafile_utils.cc	2026-06-18 15:03:16.416963000 +0200
@@ -292,6 +292,9 @@
 
 #if BUILDFLAG(IS_WIN)
 sk_sp<SkDocument> MakeXpsDocument(SkWStream* stream) {
+#if defined(__MINGW32__)
+  return nullptr;
+#else
   IXpsOMObjectFactory* factory = nullptr;
   HRESULT hr = CoCreateInstance(CLSID_XpsOMObjectFactory, nullptr,
                                 CLSCTX_INPROC_SERVER, IID_PPV_ARGS(&factory));
@@ -302,6 +305,7 @@
   }
 
   return SkXPS::MakeDocument(stream, factory);
+#endif
 }
 #endif
 
diff -ruN patch0126-before/src/3rdparty/chromium/services/device/geolocation/location_provider_manager.cc patch0126-after/src/3rdparty/chromium/services/device/geolocation/location_provider_manager.cc
--- a/src/3rdparty/chromium/services/device/geolocation/location_provider_manager.cc	2026-06-18 14:32:31.181723800 +0200
+++ b/src/3rdparty/chromium/services/device/geolocation/location_provider_manager.cc	2026-06-18 14:36:50.828276400 +0200
@@ -380,7 +380,7 @@
   CHECK(geolocation_system_permission_manager_);
   return device::NewSystemLocationProvider(
       geolocation_system_permission_manager_->GetSystemGeolocationSource());
-#elif BUILDFLAG(IS_WIN) || BUILDFLAG(IS_ANDROID)
+#elif BUILDFLAG(IS_MSVC) || BUILDFLAG(IS_ANDROID)
   return device::NewSystemLocationProvider();
 #else
   return nullptr;
diff -ruN patch0126-before/src/3rdparty/chromium/skia/BUILD.gn patch0126-after/src/3rdparty/chromium/skia/BUILD.gn
--- a/src/3rdparty/chromium/skia/BUILD.gn	2026-06-18 14:32:31.250809900 +0200
+++ b/src/3rdparty/chromium/skia/BUILD.gn	2026-06-18 14:36:50.903614300 +0200
@@ -379,7 +379,9 @@
   sources += skia_codec_png_base
   sources += skia_codec_libpng_srcs
   sources += skia_utils_private
-  sources += skia_xps_sources
+  if (!is_mingw) {
+    sources += skia_xps_sources
+  }
   sources += skia_encode_srcs
   sources += skia_encode_png_base
   sources += skia_encode_libpng_srcs
diff -ruN patch0126-before/src/3rdparty/chromium/third_party/cpuinfo/src/src/x86/windows/init.c patch0126-after/src/3rdparty/chromium/third_party/cpuinfo/src/src/x86/windows/init.c
--- a/src/3rdparty/chromium/third_party/cpuinfo/src/src/x86/windows/init.c	2026-06-18 14:32:31.320398200 +0200
+++ b/src/3rdparty/chromium/third_party/cpuinfo/src/src/x86/windows/init.c	2026-06-18 14:36:50.979074100 +0200
@@ -16,7 +16,7 @@
 #define CPUINFO_ALLOCA _alloca
 #endif
 
-#ifndef _WIN32
+#if !defined(_WIN32) || defined(__MINGW32__)
 #define max(a,b)             \
 ({                           \
     __typeof__ (a) _a = (a); \
diff -ruN patch0126-before/src/3rdparty/chromium/third_party/zstd/BUILD.gn patch0126-after/src/3rdparty/chromium/third_party/zstd/BUILD.gn
--- a/src/3rdparty/chromium/third_party/zstd/BUILD.gn	2026-06-18 14:32:31.377690500 +0200
+++ b/src/3rdparty/chromium/third_party/zstd/BUILD.gn	2026-06-18 14:36:51.044478900 +0200
@@ -92,6 +92,9 @@
   if (current_cpu == "x64" && !is_win) {
     sources += [ "src/lib/decompress/huf_decompress_amd64.S" ]
   }
+  if (is_mingw) {
+    defines = [ "ZSTD_DISABLE_ASM" ]
+  }
   configs -= [
     "//build/config/compiler:chromium_code",
     "//build/config/compiler:default_optimization",
diff -ruN patch0126-before/src/3rdparty/chromium/ui/gfx/BUILD.gn patch0126-after/src/3rdparty/chromium/ui/gfx/BUILD.gn
--- a/src/3rdparty/chromium/ui/gfx/BUILD.gn	2026-06-18 14:32:31.451366400 +0200
+++ b/src/3rdparty/chromium/ui/gfx/BUILD.gn	2026-06-18 14:36:51.098200900 +0200
@@ -255,7 +255,6 @@
     ]
     if (is_mingw) {
       sources -= [
-        "win/d3d_shared_fence.cc",
         "win/d3d_shared_fence.h",
         "win/text_analysis_source.cc",
         "win/text_analysis_source.h"
diff -ruN patch0126-before/src/3rdparty/chromium/ui/gl/dc_layer_tree.cc patch0126-after/src/3rdparty/chromium/ui/gl/dc_layer_tree.cc
--- a/src/3rdparty/chromium/ui/gl/dc_layer_tree.cc	2026-06-18 15:03:41.695887900 +0200
+++ b/src/3rdparty/chromium/ui/gl/dc_layer_tree.cc	2026-06-18 15:03:16.335564300 +0200
@@ -387,6 +387,7 @@
 
   hdr_metadata_helper_ = std::make_unique<HDRMetadataHelperWin>(d3d11_device_);
 
+#if !defined(__MINGW32__)
   if (Microsoft::WRL::ComPtr<PREVIEW_IDCompositionDevice5> dcomp_device5;
       SUCCEEDED(dcomp_device_.As(&dcomp_device5))) {
     hr = dcomp_device5->CreateDynamicTexture(&primary_plane_surface_);
@@ -395,6 +396,7 @@
                    << logging::SystemErrorCodeToString(hr);
     }
   }
+#endif
 }
 
 VideoProcessorWrapper* DCLayerTree::InitializeVideoProcessor(
diff -ruN patch0126-before/src/3rdparty/chromium/ui/gl/direct_composition_support.cc patch0126-after/src/3rdparty/chromium/ui/gl/direct_composition_support.cc
--- a/src/3rdparty/chromium/ui/gl/direct_composition_support.cc	2026-06-18 14:32:31.516713400 +0200
+++ b/src/3rdparty/chromium/ui/gl/direct_composition_support.cc	2026-06-18 14:36:51.161808700 +0200
@@ -1169,10 +1169,12 @@
   // |InitializeDirectComposition|.
   g_direct_composition_texture_supported = false;
 
+#if !defined(__MINGW32__)
   if (!gfx::D3DSharedFence::IsSupported(d3d11_device.Get())) {
     LOG(WARNING) << "IDCompositionTexture is not supported without fences.";
     return false;
   }
+#endif
 
   Microsoft::WRL::ComPtr<IDCompositionDevice4> dcomp_device4;
   HRESULT hr = dcomp_device.As(&dcomp_device4);
