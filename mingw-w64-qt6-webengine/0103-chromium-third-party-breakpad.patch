diff -ur a/src/3rdparty/chromium/third_party/breakpad/BUILD.gn b/src/3rdparty/chromium/third_party/breakpad/BUILD.gn
--- a/src/3rdparty/chromium/third_party/breakpad/BUILD.gn	2023-11-21 00:08:07.000000000 +0800
+++ b/src/3rdparty/chromium/third_party/breakpad/BUILD.gn	2024-01-02 19:55:40.517562000 +0800
@@ -881,18 +881,25 @@
   # Adding the second condition enables this block for the "true" host
   # toolchain as well, which allows creating 64-bit binaries even when the host
   # toolchain is redefined to a 32-bit target.
-  if (is_clang) {
-    symupload_toolchain = "//build/toolchain/win:win_clang_$host_cpu"
+  if (is_msvc) {
+    if (is_clang) {
+      symupload_toolchain = "//build/toolchain/win:win_clang_$host_cpu"
+    } else {
+      symupload_toolchain = "//build/toolchain/win:$host_cpu"
+    }
   } else {
-    symupload_toolchain = "//build/toolchain/win:$host_cpu"
+      symupload_toolchain = "//build/toolchain/win:mingw_$host_cpu"
   }
   if (current_toolchain == host_toolchain ||
       current_toolchain == symupload_toolchain) {
     static_library("common_windows") {
       include_dirs = [
-        "$visual_studio_path/DIA SDK/include",
         "breakpad/src",
       ]
+      if (is_msvc) {
+        include_dirs += [ "$visual_studio_path/DIA SDK/include", ]
+      }
+
       sources = [
         "breakpad/src/common/windows/dia_util.cc",
         "breakpad/src/common/windows/dia_util.h",
@@ -921,9 +928,11 @@
 
     static_library("ms_symbol_server_converter") {
       include_dirs = [
-        "$visual_studio_path/DIA SDK/include",
         "breakpad/src",
       ]
+      if (is_msvc) {
+        include_dirs += [ "$visual_studio_path/DIA SDK/include", ]
+      }
 
       sources = [
         "breakpad/src/tools/windows/converter/ms_symbol_server_converter.cc",
@@ -931,14 +940,13 @@
 
       deps = [ "//third_party/breakpad:common_windows" ]
 
-      libs = [ "Pathcch.lib" ]
+      libs = [ "pathcch" ]
     }
 
     executable("dump_syms") {
       # TODO(scottmg) using this with VS2015 may break the crash server.
       # https://crbug.com/696671
       include_dirs = [
-        "$visual_studio_path/DIA SDK/include",
         "breakpad/src",
       ]
 
@@ -947,15 +955,19 @@
       deps = [ "//third_party/breakpad:common_windows" ]
 
       lib_dirs = []
-      if (current_cpu == "x64") {
-        lib_dirs += [ "$visual_studio_path/DIA SDK/lib/amd64" ]
-      } else {
-        lib_dirs += [ "$visual_studio_path/DIA SDK/lib" ]
+      libs = []
+      if (is_msvc) {
+        include_dirs += [ "$visual_studio_path/DIA SDK/include", ]
+        if (current_cpu == "x64") {
+          lib_dirs += [ "$visual_studio_path/DIA SDK/lib/amd64" ]
+        } else {
+          lib_dirs += [ "$visual_studio_path/DIA SDK/lib" ]
+        }
+        libs += [ "diaguids" ]
       }
 
-      libs = [
-        "diaguids.lib",
-        "imagehlp.lib",
+      libs += [
+        "imagehlp",
       ]
 
       if (is_clang) {
@@ -973,7 +985,6 @@
     }
     executable("symupload_win") {
       include_dirs = [
-        "$visual_studio_path/DIA SDK/include",
         "breakpad/src",
       ]
       output_name = "symupload"
@@ -998,18 +1009,21 @@
 
       deps = [ "//third_party/breakpad:common_windows" ]
 
-      ldflags = [ "/LARGEADDRESSAWARE" ]
-
       lib_dirs = []
-      if (current_cpu == "x64") {
-        lib_dirs += [ "$visual_studio_path/DIA SDK/lib/amd64" ]
-      } else {
-        lib_dirs += [ "$visual_studio_path/DIA SDK/lib" ]
+      if (is_msvc) {
+        include_dirs += [ "$visual_studio_path/DIA SDK/include", ]
+        if (current_cpu == "x64") {
+          lib_dirs += [ "$visual_studio_path/DIA SDK/lib/amd64" ]
+        } else {
+          lib_dirs += [ "$visual_studio_path/DIA SDK/lib" ]
+        }
+
+        ldflags = [ "/LARGEADDRESSAWARE" ]
       }
 
       libs = [
-        "diaguids.lib",
-        "imagehlp.lib",
+        "diaguids",
+        "imagehlp",
       ]
 
       if (is_clang) {
@@ -1021,7 +1035,6 @@
 
     executable("google_converter") {
       include_dirs = [
-        "$visual_studio_path/DIA SDK/include",
         "breakpad/src",
       ]
 
@@ -1040,22 +1053,25 @@
         "//third_party/breakpad:ms_symbol_server_converter",
       ]
 
-      ldflags = [
-        "/DELAYLOAD:dbghelp.dll",
-        "/DELAYLOAD:dwmapi.dll",
-        "/DELAYLOAD:uxtheme.dll",
-      ]
-
       lib_dirs = []
-      if (current_cpu == "x64") {
-        lib_dirs += [ "$visual_studio_path/DIA SDK/lib/amd64" ]
-      } else {
-        lib_dirs += [ "$visual_studio_path/DIA SDK/lib" ]
+      if (is_msvc) {
+        include_dirs += [ "$visual_studio_path/DIA SDK/include", ]
+        if (current_cpu == "x64") {
+          lib_dirs += [ "$visual_studio_path/DIA SDK/lib/amd64" ]
+        } else {
+          lib_dirs += [ "$visual_studio_path/DIA SDK/lib" ]
+        }
+
+        ldflags = [
+          "/DELAYLOAD:dbghelp.dll",
+          "/DELAYLOAD:dwmapi.dll",
+          "/DELAYLOAD:uxtheme.dll",
+        ]
       }
 
       libs = [
-        "diaguids.lib",
-        "imagehlp.lib",
+        "diaguids",
+        "imagehlp",
       ]
 
       if (is_clang) {
