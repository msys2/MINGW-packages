diff --git a/src/3rdparty/chromium/third_party/webrtc/BUILD.gn b/src/3rdparty/chromium/third_party/webrtc/BUILD.gn
index 6a30426..7a557c2 100644
--- a/src/3rdparty/chromium/third_party/webrtc/BUILD.gn
+++ b/src/3rdparty/chromium/third_party/webrtc/BUILD.gn
@@ -405,7 +405,7 @@ config("common_config") {
       }
     }
 
-    if (is_win && !is_clang) {
+    if (is_msvc && !is_clang) {
       # MSVC warning suppressions (needed to use Abseil).
       # TODO(bugs.webrtc.org/9274): Remove these warnings as soon as MSVC allows
       # external headers warning suppression (or fix them upstream).
@@ -469,8 +469,8 @@ config("common_config") {
 
   if (!build_with_chromium && rtc_win_undef_unicode) {
     cflags += [
-      "/UUNICODE",
-      "/U_UNICODE",
+      "-UUNICODE",
+      "-U_UNICODE",
     ]
   }
 }
diff --git a/src/3rdparty/chromium/third_party/webrtc/common_audio/BUILD.gn b/src/3rdparty/chromium/third_party/webrtc/common_audio/BUILD.gn
index 2ae6d32..d4b4201 100644
--- a/src/3rdparty/chromium/third_party/webrtc/common_audio/BUILD.gn
+++ b/src/3rdparty/chromium/third_party/webrtc/common_audio/BUILD.gn
@@ -248,7 +248,7 @@ if (current_cpu == "x86" || current_cpu == "x64") {
       "resampler/sinc_resampler_sse.cc",
     ]
 
-    if (is_posix || is_fuchsia) {
+    if (is_posix || is_fuchsia || is_mingw) {
       cflags = [ "-msse2" ]
     }
 
@@ -267,7 +267,7 @@ if (current_cpu == "x86" || current_cpu == "x64") {
       "resampler/sinc_resampler_avx2.cc",
     ]
 
-    if (is_win) {
+    if (is_msvc) {
       cflags = [ "/arch:AVX2" ]
     } else {
       cflags = [
diff --git a/src/3rdparty/chromium/third_party/webrtc/modules/audio_processing/aec3/BUILD.gn b/src/3rdparty/chromium/third_party/webrtc/modules/audio_processing/aec3/BUILD.gn
index f5eb5d5..799b8ad 100644
--- a/src/3rdparty/chromium/third_party/webrtc/modules/audio_processing/aec3/BUILD.gn
+++ b/src/3rdparty/chromium/third_party/webrtc/modules/audio_processing/aec3/BUILD.gn
@@ -264,7 +264,7 @@ if (current_cpu == "x86" || current_cpu == "x64") {
       "vector_math_avx2.cc",
     ]
 
-    if (is_win) {
+    if (is_msvc) {
       cflags = [ "/arch:AVX2" ]
     } else {
       cflags = [
diff --git a/src/3rdparty/chromium/third_party/webrtc/modules/audio_processing/agc2/rnn_vad/BUILD.gn b/src/3rdparty/chromium/third_party/webrtc/modules/audio_processing/agc2/rnn_vad/BUILD.gn
index 9093a68..83c1892 100644
--- a/src/3rdparty/chromium/third_party/webrtc/modules/audio_processing/agc2/rnn_vad/BUILD.gn
+++ b/src/3rdparty/chromium/third_party/webrtc/modules/audio_processing/agc2/rnn_vad/BUILD.gn
@@ -122,7 +122,7 @@ rtc_source_set("vector_math") {
 if (current_cpu == "x86" || current_cpu == "x64") {
   rtc_library("vector_math_avx2") {
     sources = [ "vector_math_avx2.cc" ]
-    if (is_win) {
+    if (is_msvc) {
       cflags = [ "/arch:AVX2" ]
     } else {
       cflags = [
diff --git a/src/3rdparty/chromium/third_party/webrtc/modules/desktop_capture/BUILD.gn b/src/3rdparty/chromium/third_party/webrtc/modules/desktop_capture/BUILD.gn
index d60418d..0348286 100644
--- a/src/3rdparty/chromium/third_party/webrtc/modules/desktop_capture/BUILD.gn
+++ b/src/3rdparty/chromium/third_party/webrtc/modules/desktop_capture/BUILD.gn
@@ -525,10 +525,16 @@ rtc_library("desktop_capture") {
       "window_finder_win.cc",
       "window_finder_win.h",
     ]
+    if (is_mingw) {
+      sources -= [
+        "win/desktop_capture_utils.cc",
+        "win/desktop_capture_utils.h",
+      ]
+    }
     libs = [
-      "d3d11.lib",
-      "dxgi.lib",
-      "shcore.lib",
+      "d3d11",
+      "dxgi",
+      "shcore",
     ]
     deps += [
       "../../rtc_base:win32",
@@ -685,7 +691,7 @@ if (use_desktop_capture_differ_sse2) {
       "differ_vector_sse2.h",
     ]
 
-    if (is_posix || is_fuchsia) {
+    if (is_posix || is_fuchsia || is_mingw) {
       cflags = [ "-msse2" ]
     }
   }
diff --git a/src/3rdparty/chromium/third_party/webrtc/modules/desktop_capture/win/d3d_device.cc b/src/3rdparty/chromium/third_party/webrtc/modules/desktop_capture/win/d3d_device.cc
index 3d46117..ccd6d4e 100644
--- a/src/3rdparty/chromium/third_party/webrtc/modules/desktop_capture/win/d3d_device.cc
+++ b/src/3rdparty/chromium/third_party/webrtc/modules/desktop_capture/win/d3d_device.cc
@@ -39,8 +39,12 @@ bool D3dDevice::Initialize(const ComPtr<IDXGIAdapter>& adapter) {
       nullptr, 0, D3D11_SDK_VERSION, d3d_device_.GetAddressOf(), &feature_level,
       context_.GetAddressOf());
   if (error.Error() != S_OK || !d3d_device_ || !context_) {
+#ifdef _MSC_VER
     RTC_LOG(LS_WARNING) << "D3D11CreateDevice returned: "
                         << desktop_capture::utils::ComErrorToString(error);
+#else
+    RTC_LOG(LS_WARNING) << "D3D11CreateDevice error.";
+#endif
     return false;
   }
 
@@ -55,10 +59,15 @@ bool D3dDevice::Initialize(const ComPtr<IDXGIAdapter>& adapter) {
   error = d3d_device_.As(&dxgi_device_);
   if (error.Error() != S_OK || !dxgi_device_) {
     RTC_LOG(LS_WARNING)
+#ifdef _MSC_VER
         << "ID3D11Device is not an implementation of IDXGIDevice, "
         << "this usually means the system does not support DirectX "
         << "11. Error received: "
         << desktop_capture::utils::ComErrorToString(error);
+#else
+        << "ID3D11Device is not an implementation of IDXGIDevice, "
+        << "this usually means the system does not support DirectX 11.";
+#endif
     return false;
   }
 
@@ -72,8 +81,12 @@ std::vector<D3dDevice> D3dDevice::EnumDevices() {
       CreateDXGIFactory1(__uuidof(IDXGIFactory1),
                          reinterpret_cast<void**>(factory.GetAddressOf()));
   if (error.Error() != S_OK || !factory) {
+#ifdef _MSC_VER
     RTC_LOG(LS_WARNING) << "Cannot create IDXGIFactory1: "
                         << desktop_capture::utils::ComErrorToString(error);
+#else
+    RTC_LOG(LS_WARNING) << "Cannot create IDXGIFactory1.";
+#endif
     return std::vector<D3dDevice>();
   }
 
@@ -90,8 +103,12 @@ std::vector<D3dDevice> D3dDevice::EnumDevices() {
       break;
     } else {
       RTC_LOG(LS_WARNING)
+#ifdef _MSC_VER
           << "IDXGIFactory1::EnumAdapters returned an unexpected error: "
           << desktop_capture::utils::ComErrorToString(error);
+#else
+          << "IDXGIFactory1::EnumAdapters returned an unexpected error.";
+#endif
     }
   }
   return result;
diff --git a/src/3rdparty/chromium/third_party/webrtc/modules/desktop_capture/win/dxgi_adapter_duplicator.cc b/src/3rdparty/chromium/third_party/webrtc/modules/desktop_capture/win/dxgi_adapter_duplicator.cc
index 88ec4e2..2b40771 100644
--- a/src/3rdparty/chromium/third_party/webrtc/modules/desktop_capture/win/dxgi_adapter_duplicator.cc
+++ b/src/3rdparty/chromium/third_party/webrtc/modules/desktop_capture/win/dxgi_adapter_duplicator.cc
@@ -61,9 +61,13 @@ bool DxgiAdapterDuplicator::DoInitialize() {
     }
 
     if (error.Error() != S_OK || !output) {
+#ifdef _MSC_VER
       RTC_LOG(LS_WARNING) << "IDXGIAdapter::EnumOutputs returned an unexpected "
                           << "result: "
                           << desktop_capture::utils::ComErrorToString(error);
+#else
+      RTC_LOG(LS_WARNING) << "IDXGIAdapter::EnumOutputs returned an unexpected result.";
+#endif
       continue;
     }
 
diff --git a/src/3rdparty/chromium/third_party/webrtc/modules/desktop_capture/win/dxgi_output_duplicator.cc b/src/3rdparty/chromium/third_party/webrtc/modules/desktop_capture/win/dxgi_output_duplicator.cc
index 9c64125..00f71bc 100644
--- a/src/3rdparty/chromium/third_party/webrtc/modules/desktop_capture/win/dxgi_output_duplicator.cc
+++ b/src/3rdparty/chromium/third_party/webrtc/modules/desktop_capture/win/dxgi_output_duplicator.cc
@@ -105,8 +105,12 @@ bool DxgiOutputDuplicator::DuplicateOutput() {
       output_->DuplicateOutput(static_cast<IUnknown*>(device_.d3d_device()),
                                duplication_.GetAddressOf());
   if (error.Error() != S_OK || !duplication_) {
+#ifdef _MSC_VER
     RTC_LOG(LS_WARNING) << "Failed to duplicate output from IDXGIOutput1: "
                         << desktop_capture::utils::ComErrorToString(error);
+#else
+    RTC_LOG(LS_WARNING) << "Failed to duplicate output from IDXGIOutput1.";
+#endif
     return false;
   }
 
@@ -140,8 +144,12 @@ bool DxgiOutputDuplicator::ReleaseFrame() {
   RTC_DCHECK(duplication_);
   _com_error error = duplication_->ReleaseFrame();
   if (error.Error() != S_OK) {
+#ifdef _MSC_VER
     RTC_LOG(LS_ERROR) << "Failed to release frame from IDXGIOutputDuplication: "
                       << desktop_capture::utils::ComErrorToString(error);
+#else
+    RTC_LOG(LS_ERROR) << "Failed to release frame from IDXGIOutputDuplication.";
+#endif
     return false;
   }
   return true;
@@ -193,8 +201,12 @@ bool DxgiOutputDuplicator::Duplicate(Context* context,
   _com_error error = duplication_->AcquireNextFrame(
       kAcquireTimeoutMs, &frame_info, resource.GetAddressOf());
   if (error.Error() != S_OK && error.Error() != DXGI_ERROR_WAIT_TIMEOUT) {
+#ifdef _MSC_VER
     RTC_LOG(LS_ERROR) << "Failed to capture frame: "
                       << desktop_capture::utils::ComErrorToString(error);
+#else
+    RTC_LOG(LS_ERROR) << "Failed to capture frame.";
+#endif
     return false;
   }
 
@@ -316,8 +328,12 @@ bool DxgiOutputDuplicator::DoDetectUpdatedRegion(
   _com_error error = duplication_->GetFrameMoveRects(
       static_cast<UINT>(metadata_.size()), move_rects, &buff_size);
   if (error.Error() != S_OK) {
+#ifdef _MSC_VER
     RTC_LOG(LS_ERROR) << "Failed to get move rectangles: "
                       << desktop_capture::utils::ComErrorToString(error);
+#else
+    RTC_LOG(LS_ERROR) << "Failed to get move rectangles.";
+#endif
     return false;
   }
   move_rects_count = buff_size / sizeof(DXGI_OUTDUPL_MOVE_RECT);
@@ -327,8 +343,12 @@ bool DxgiOutputDuplicator::DoDetectUpdatedRegion(
   error = duplication_->GetFrameDirtyRects(
       static_cast<UINT>(metadata_.size()) - buff_size, dirty_rects, &buff_size);
   if (error.Error() != S_OK) {
+#ifdef _MSC_VER
     RTC_LOG(LS_ERROR) << "Failed to get dirty rectangles: "
                       << desktop_capture::utils::ComErrorToString(error);
+#else
+    RTC_LOG(LS_ERROR) << "Failed to get dirty rectangles.";
+#endif
     return false;
   }
   dirty_rects_count = buff_size / sizeof(RECT);
diff --git a/src/3rdparty/chromium/third_party/webrtc/modules/desktop_capture/win/dxgi_texture.cc b/src/3rdparty/chromium/third_party/webrtc/modules/desktop_capture/win/dxgi_texture.cc
index b8f5b81..68e4f33 100644
--- a/src/3rdparty/chromium/third_party/webrtc/modules/desktop_capture/win/dxgi_texture.cc
+++ b/src/3rdparty/chromium/third_party/webrtc/modules/desktop_capture/win/dxgi_texture.cc
@@ -50,8 +50,12 @@ bool DxgiTexture::CopyFrom(const DXGI_OUTDUPL_FRAME_INFO& frame_info,
       __uuidof(ID3D11Texture2D),
       reinterpret_cast<void**>(texture.GetAddressOf()));
   if (error.Error() != S_OK || !texture) {
+#ifdef _MSC_VER
     RTC_LOG(LS_ERROR) << "Failed to convert IDXGIResource to ID3D11Texture2D: "
                       << desktop_capture::utils::ComErrorToString(error);
+#else
+    RTC_LOG(LS_ERROR) << "Failed to convert IDXGIResource to ID3D11Texture2D.";
+#endif
     return false;
   }
 
diff --git a/src/3rdparty/chromium/third_party/webrtc/modules/desktop_capture/win/dxgi_texture_mapping.cc b/src/3rdparty/chromium/third_party/webrtc/modules/desktop_capture/win/dxgi_texture_mapping.cc
index 7ecf1ad..af3fcf0 100644
--- a/src/3rdparty/chromium/third_party/webrtc/modules/desktop_capture/win/dxgi_texture_mapping.cc
+++ b/src/3rdparty/chromium/third_party/webrtc/modules/desktop_capture/win/dxgi_texture_mapping.cc
@@ -37,8 +37,12 @@ bool DxgiTextureMapping::CopyFromTexture(
   if (error.Error() != S_OK) {
     *rect() = {0};
     RTC_LOG(LS_ERROR)
+#ifdef _MSC_VER
         << "Failed to map the IDXGIOutputDuplication to a bitmap: "
         << desktop_capture::utils::ComErrorToString(error);
+#else
+        << "Failed to map the IDXGIOutputDuplication to a bitmap.";
+#endif
     return false;
   }
 
@@ -48,8 +52,12 @@ bool DxgiTextureMapping::CopyFromTexture(
 bool DxgiTextureMapping::DoRelease() {
   _com_error error = duplication_->UnMapDesktopSurface();
   if (error.Error() != S_OK) {
+#ifdef _MSC_VER
     RTC_LOG(LS_ERROR) << "Failed to unmap the IDXGIOutputDuplication: "
                       << desktop_capture::utils::ComErrorToString(error);
+#else
+    RTC_LOG(LS_ERROR) << "Failed to unmap the IDXGIOutputDuplication.";
+#endif
     return false;
   }
   return true;
diff --git a/src/3rdparty/chromium/third_party/webrtc/modules/desktop_capture/win/dxgi_texture_staging.cc b/src/3rdparty/chromium/third_party/webrtc/modules/desktop_capture/win/dxgi_texture_staging.cc
index 17e8518..b8f4a8e 100644
--- a/src/3rdparty/chromium/third_party/webrtc/modules/desktop_capture/win/dxgi_texture_staging.cc
+++ b/src/3rdparty/chromium/third_party/webrtc/modules/desktop_capture/win/dxgi_texture_staging.cc
@@ -65,15 +65,23 @@ bool DxgiTextureStaging::InitializeStage(ID3D11Texture2D* texture) {
   _com_error error = device_.d3d_device()->CreateTexture2D(
       &desc, nullptr, stage_.GetAddressOf());
   if (error.Error() != S_OK || !stage_) {
+#ifdef _MSC_VER
     RTC_LOG(LS_ERROR) << "Failed to create a new ID3D11Texture2D as stage: "
                       << desktop_capture::utils::ComErrorToString(error);
+#else
+    RTC_LOG(LS_ERROR) << "Failed to create a new ID3D11Texture2D as stage.";
+#endif
     return false;
   }
 
   error = stage_.As(&surface_);
   if (error.Error() != S_OK || !surface_) {
+#ifdef _MSC_VER
     RTC_LOG(LS_ERROR) << "Failed to convert ID3D11Texture2D to IDXGISurface: "
                       << desktop_capture::utils::ComErrorToString(error);
+#else
+    RTC_LOG(LS_ERROR) << "Failed to convert ID3D11Texture2D to IDXGISurface.";
+#endif
     return false;
   }
 
@@ -109,8 +117,12 @@ bool DxgiTextureStaging::CopyFromTexture(
   _com_error error = surface_->Map(rect(), DXGI_MAP_READ);
   if (error.Error() != S_OK) {
     *rect() = {0};
+#ifdef _MSC_VER
     RTC_LOG(LS_ERROR) << "Failed to map the IDXGISurface to a bitmap: "
                       << desktop_capture::utils::ComErrorToString(error);
+#else
+    RTC_LOG(LS_ERROR) << "Failed to map the IDXGISurface to a bitmap.";
+#endif
     return false;
   }
 
diff --git a/src/3rdparty/chromium/third_party/webrtc/rtc_base/BUILD.gn b/src/3rdparty/chromium/third_party/webrtc/rtc_base/BUILD.gn
index 80ab5a6..0ad42d2 100644
--- a/src/3rdparty/chromium/third_party/webrtc/rtc_base/BUILD.gn
+++ b/src/3rdparty/chromium/third_party/webrtc/rtc_base/BUILD.gn
@@ -593,7 +593,7 @@ rtc_library("timeutils") {
 
   libs = []
   if (is_win) {
-    libs += [ "winmm.lib" ]
+    libs += [ "winmm" ]
     deps += [ ":win32" ]
   }
 }
@@ -1160,9 +1160,9 @@ if (is_win) {
     ]
 
     libs = [
-      "crypt32.lib",
-      "iphlpapi.lib",
-      "secur32.lib",
+      "crypt32",
+      "iphlpapi",
+      "secur32",
     ]
 
     defines = [ "_CRT_NONSTDC_NO_DEPRECATE" ]
diff --git a/src/3rdparty/chromium/third_party/webrtc/rtc_base/network.cc b/src/3rdparty/chromium/third_party/webrtc/rtc_base/network.cc
index 4be6eed..88d0a70 100644
--- a/src/3rdparty/chromium/third_party/webrtc/rtc_base/network.cc
+++ b/src/3rdparty/chromium/third_party/webrtc/rtc_base/network.cc
@@ -868,10 +868,12 @@ bool BasicNetworkManager::CreateNetworks(
             case IF_TYPE_IEEE80211:
               adapter_type = ADAPTER_TYPE_WIFI;
               break;
+#ifdef _MSC_VER
             case IF_TYPE_WWANPP:
             case IF_TYPE_WWANPP2:
               adapter_type = ADAPTER_TYPE_CELLULAR;
               break;
+#endif
             default:
               // TODO(phoglund): Need to recognize other types as well.
               adapter_type = ADAPTER_TYPE_UNKNOWN;
diff --git a/src/3rdparty/chromium/third_party/webrtc/rtc_base/platform_thread_types.cc b/src/3rdparty/chromium/third_party/webrtc/rtc_base/platform_thread_types.cc
index d64ea68..9c45dd4 100644
--- a/src/3rdparty/chromium/third_party/webrtc/rtc_base/platform_thread_types.cc
+++ b/src/3rdparty/chromium/third_party/webrtc/rtc_base/platform_thread_types.cc
@@ -32,6 +32,13 @@ typedef HRESULT(WINAPI* RTC_SetThreadDescription)(HANDLE hThread,
 #include "rtc_base/checks.h"
 #endif
 
+#if defined(__MINGW32__)
+#undef __try
+#define __try try
+#undef __except
+#define __except(x) catch(...)
+#endif
+
 namespace rtc {
 
 PlatformThreadId CurrentThreadId() {
diff --git a/src/3rdparty/chromium/third_party/webrtc/rtc_base/system/file_wrapper.h b/src/3rdparty/chromium/third_party/webrtc/rtc_base/system/file_wrapper.h
index 5e1e3d6..ec57d70 100644
--- a/src/3rdparty/chromium/third_party/webrtc/rtc_base/system/file_wrapper.h
+++ b/src/3rdparty/chromium/third_party/webrtc/rtc_base/system/file_wrapper.h
@@ -13,6 +13,7 @@
 
 #include <stddef.h>
 #include <stdio.h>
+#include <stdint.h>
 
 #include <string>
 
diff --git a/src/3rdparty/chromium/third_party/webrtc/rtc_base/system/rtc_export_template.h b/src/3rdparty/chromium/third_party/webrtc/rtc_base/system/rtc_export_template.h
index 4ac7043..ed35645 100644
--- a/src/3rdparty/chromium/third_party/webrtc/rtc_base/system/rtc_export_template.h
+++ b/src/3rdparty/chromium/third_party/webrtc/rtc_base/system/rtc_export_template.h
@@ -185,7 +185,9 @@
 
 RTC_EXPORT_TEMPLATE_TEST(DEFAULT, );  // NOLINT
 RTC_EXPORT_TEMPLATE_TEST(DEFAULT, __attribute__((visibility("default"))));
+#ifdef _MSC_VER
 RTC_EXPORT_TEMPLATE_TEST(MSVC_HACK, __declspec(dllexport));
+#endif
 RTC_EXPORT_TEMPLATE_TEST(DEFAULT, __declspec(dllimport));
 
 #undef RTC_EXPORT_TEMPLATE_TEST
diff --git a/src/3rdparty/chromium/third_party/webrtc/rtc_base/win/BUILD.gn b/src/3rdparty/chromium/third_party/webrtc/rtc_base/win/BUILD.gn
index cf8bc21..6e2defc 100644
--- a/src/3rdparty/chromium/third_party/webrtc/rtc_base/win/BUILD.gn
+++ b/src/3rdparty/chromium/third_party/webrtc/rtc_base/win/BUILD.gn
@@ -9,11 +9,13 @@
 import("../../webrtc.gni")
 
 rtc_library("create_direct3d_device") {
-  sources = [
-    "create_direct3d_device.cc",
-    "create_direct3d_device.h",
-  ]
-  data_deps = [ "//build/win:runtime_libs" ]
+  if (is_msvc) {
+    sources = [
+      "create_direct3d_device.cc",
+      "create_direct3d_device.h",
+    ]
+    data_deps = [ "//build/win:runtime_libs" ]
+  }
 }
 
 rtc_library("get_activation_factory") {
diff --git a/src/3rdparty/chromium/third_party/webrtc/rtc_base/win/windows_version.cc b/src/3rdparty/chromium/third_party/webrtc/rtc_base/win/windows_version.cc
index 93af137..6914722 100644
--- a/src/3rdparty/chromium/third_party/webrtc/rtc_base/win/windows_version.cc
+++ b/src/3rdparty/chromium/third_party/webrtc/rtc_base/win/windows_version.cc
@@ -17,7 +17,7 @@
 #include "rtc_base/checks.h"
 #include "rtc_base/string_utils.h"
 
-#if !defined(__clang__) && _MSC_FULL_VER < 191125507
+#if !defined(__clang__) && defined(_MSC_VER) && _MSC_FULL_VER < 191125507
 #error VS 2017 Update 3.2 or higher is required
 #endif
 
diff --git a/src/3rdparty/chromium/third_party/webrtc/system_wrappers/BUILD.gn b/src/3rdparty/chromium/third_party/webrtc/system_wrappers/BUILD.gn
index 2576d4e..5004713 100644
--- a/src/3rdparty/chromium/third_party/webrtc/system_wrappers/BUILD.gn
+++ b/src/3rdparty/chromium/third_party/webrtc/system_wrappers/BUILD.gn
@@ -69,7 +69,7 @@ rtc_library("system_wrappers") {
   }
 
   if (is_win) {
-    libs += [ "winmm.lib" ]
+    libs += [ "winmm" ]
 
     # Windows needs ../rtc_base due to include of
     # webrtc/rtc_base/win32.h in source/clock.cc.
diff --git a/src/3rdparty/chromium/third_party/webrtc/webrtc.gni b/src/3rdparty/chromium/third_party/webrtc/webrtc.gni
index 5a1c43c..92ff706 100644
--- a/src/3rdparty/chromium/third_party/webrtc/webrtc.gni
+++ b/src/3rdparty/chromium/third_party/webrtc/webrtc.gni
@@ -231,7 +231,7 @@ declare_args() {
   # When set to true, a capturer implementation that uses the
   # Windows.Graphics.Capture APIs will be available for use. This introduces a
   # dependency on the Win 10 SDK v10.0.17763.0.
-  rtc_enable_win_wgc = is_win
+  rtc_enable_win_wgc = is_win && !is_mingw
 
   # Includes the dav1d decoder in the internal decoder factory when set to true.
   rtc_include_dav1d_in_internal_decoder_factory = true
