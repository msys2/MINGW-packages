--- a/inc/MyBuilder.pm
+++ b/inc/MyBuilder.pm
@@ -225,7 +225,7 @@
     print STDERR "\n** Creating binaries (",join(", ", map { $_.$EXEEXT } @BINARIES), ")\n";
 
     my $extra_linker_flags = sprintf("-Lbtparse/src %s -lbtparse ",
-                                     ($^O !~ /darwin/)?"-Wl,-R${btparselibdir}":"");
+                                     ($^O !~ /darwin|MSWin32|cygwin/)?"-Wl,-R${btparselibdir}":"");
 
     my @toinstall;
 
