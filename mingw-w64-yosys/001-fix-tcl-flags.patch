diff -bur yosys-yosys-0.27-orig/Makefile yosys-yosys-0.27/Makefile
--- yosys-yosys-0.27-orig/Makefile	2023-04-14 04:52:22.546510600 -0600
+++ yosys-yosys-0.27/Makefile	2023-04-14 04:52:41.159631100 -0600
@@ -458,25 +458,12 @@
 LDLIBS += -lz
 endif
 
-
 ifeq ($(ENABLE_TCL),1)
 TCL_VERSION ?= tcl$(shell bash -c "tclsh <(echo 'puts [info tclversion]')")
-ifeq ($(OS), $(filter $(OS),FreeBSD OpenBSD NetBSD))
-# BSDs usually use tcl8.6, but the lib is named "libtcl86"
-TCL_INCLUDE ?= /usr/local/include/$(TCL_VERSION)
-TCL_LIBS ?= -l$(subst .,,$(TCL_VERSION))
-else
-TCL_INCLUDE ?= /usr/include/$(TCL_VERSION)
-TCL_LIBS ?= -l$(TCL_VERSION)
-endif
-
-ifeq ($(CONFIG),mxe)
-CXXFLAGS += -DYOSYS_ENABLE_TCL
-LDLIBS += -ltcl86 -lwsock32 -lws2_32 -lnetapi32 -lz -luserenv
-else
-CXXFLAGS += $(shell PKG_CONFIG_PATH=$(PKG_CONFIG_PATH) $(PKG_CONFIG) --silence-errors --cflags tcl || echo -I$(TCL_INCLUDE)) -DYOSYS_ENABLE_TCL
-LDLIBS += $(shell PKG_CONFIG_PATH=$(PKG_CONFIG_PATH) $(PKG_CONFIG) --silence-errors --libs tcl || echo $(TCL_LIBS))
-endif
+TCL_INCLUDE ?= $(PREFIX)/include/$(TCL_VERSION)
+TCL_LIBS ?= -L$(PREFIX)/lib -ltcl
+CXXFLAGS += -I$(TCL_INCLUDE) -DYOSYS_ENABLE_TCL
+LDLIBS += $(TCL_LIBS)
 endif
 
 ifeq ($(ENABLE_GCOV),1)
