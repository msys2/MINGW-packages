--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -412,7 +412,7 @@
 endif()
 
 # BLAS & LAPACK, PkgConfig
-find_package(Lapack REQUIRED) # also calls find_package(BLAS)
+find_package(cp2kLapack REQUIRED) # also calls find_package(cp2kBLAS)
 
 # SMM (Small Matrix-Matrix multiplication)
 if(CP2K_USE_LIBXSMM)
--- a/cmake/modules/FindBlas.cmake
+++ b/cmake/modules/FindBlas.cmake
@@ -133,7 +133,7 @@
 # might require this information though
 
 find_package_handle_standard_args(
-  Blas REQUIRED_VARS CP2K_BLAS_LINK_LIBRARIES CP2K_BLAS_VENDOR CP2K_BLAS_FOUND)
+  cp2kBlas REQUIRED_VARS CP2K_BLAS_LINK_LIBRARIES CP2K_BLAS_VENDOR CP2K_BLAS_FOUND)
 
 if(NOT TARGET cp2k::BLAS::blas)
   add_library(cp2k::BLAS::blas INTERFACE IMPORTED)
--- a/cmake/modules/FindLapack.cmake
+++ b/cmake/modules/FindLapack.cmake
@@ -18,7 +18,7 @@
 # This solution might not good enough though.
 
 find_package(PkgConfig)
-find_package(Blas REQUIRED)
+find_package(cp2kBlas REQUIRED)
 
 if(NOT CP2K_CONFIG_PACKAGE)
 
@@ -49,7 +49,7 @@
   endif()
 endif()
 # check if found
-find_package_handle_standard_args(Lapack
+find_package_handle_standard_args(cp2kLapack
                                   REQUIRED_VARS CP2K_LAPACK_LINK_LIBRARIES)
 
 if(NOT TARGET cp2k::LAPACK::lapack)
