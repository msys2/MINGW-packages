diff -Naur ogre-1.12.10.a/OgreMain/src/OgreZip.cpp ogre-1.12.10.b/OgreMain/src/OgreZip.cpp
--- ogre-1.12.10.a/OgreMain/src/OgreZip.cpp	2020-12-13 23:57:21.000000000 +0100
+++ ogre-1.12.10.b/OgreMain/src/OgreZip.cpp	2020-12-26 10:30:36.157549300 +0100
@@ -215,11 +215,16 @@
         OGRE_LOCK_AUTO_MUTEX;
         if (!mZzipDir)
         {
-            zzip_error_t zzipError;
-            mZzipDir = zzip_dir_open_ext_io(mName.c_str(), &zzipError, 0, mPluginIo);
-            if (zzipError)
-                OGRE_EXCEPT(Exception::ERR_INTERNAL_ERROR, getErrorDescription(zzipError, mName));
-
+            zzip_error_t zzipError = ZZIP_NO_ERROR;
+            mZzipDir = zzip_dir_open(mName.c_str(), &zzipError);
+            if (zzipError != ZZIP_NO_ERROR)
+            {
+                zzip_dir_open_ext_io(mName.c_str(), &zzipError, 0, mPluginIo);
+                if (zzipError != ZZIP_NO_ERROR)
+                {
+                    OGRE_EXCEPT(Exception::ERR_INTERNAL_ERROR, getErrorDescription(zzipError, mName));
+                }
+            }
             // Cache names
             ZZIP_DIRENT zzipEntry;
             while (zzip_dir_read(mZzipDir, &zzipEntry))
