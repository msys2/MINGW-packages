diff -Naur opencv-2.4.8-orig/cmake/OpenCVGenPkgconfig.cmake opencv-2.4.8/cmake/OpenCVGenPkgconfig.cmake
--- opencv-2.4.8-orig/cmake/OpenCVGenPkgconfig.cmake	2014-04-22 20:27:23.315000000 +0400
+++ opencv-2.4.8/cmake/OpenCVGenPkgconfig.cmake	2014-04-22 20:55:51.462400000 +0400
@@ -53,6 +53,7 @@
   endif()
 
   string(REPLACE "libopencv" "-lopencv" lname "${lname}")
+  string(REPLACE "${OPENCV_DLLVERSION}" "" lname "${lname}")
   set(OpenCV_LIB_COMPONENTS_ "${OpenCV_LIB_COMPONENTS_} ${lname}")
 endforeach()
 
@@ -82,6 +83,6 @@
                "${CMAKE_BINARY_DIR}/unix-install/${OPENCV_PC_FILE_NAME}"
                @ONLY IMMEDIATE)
 
-if(UNIX AND NOT ANDROID)
+if(UNIX OR MINGW AND NOT ANDROID)
   install(FILES ${CMAKE_BINARY_DIR}/unix-install/${OPENCV_PC_FILE_NAME} DESTINATION ${OPENCV_LIB_INSTALL_PATH}/pkgconfig)
 endif()
diff -Naur opencv-2.4.8-orig/cmake/OpenCVModule.cmake opencv-2.4.8/cmake/OpenCVModule.cmake
--- opencv-2.4.8-orig/cmake/OpenCVModule.cmake	2014-04-22 20:27:23.439800000 +0400
+++ opencv-2.4.8/cmake/OpenCVModule.cmake	2014-04-22 20:33:43.335000000 +0400
@@ -546,7 +546,9 @@
   endif()
 
   set_target_properties(${the_module} PROPERTIES
-    OUTPUT_NAME "${the_module}${OPENCV_DLLVERSION}"
+    OUTPUT_NAME "${the_module}"
+    RUNTIME_OUTPUT_NAME "${the_module}${OPENCV_DLLVERSION}"
+    ARCHIVE_OUTPUT_NAME "${the_module}"
     DEBUG_POSTFIX "${OPENCV_DEBUG_POSTFIX}"
     ARCHIVE_OUTPUT_DIRECTORY ${LIBRARY_OUTPUT_PATH}
     LIBRARY_OUTPUT_DIRECTORY ${LIBRARY_OUTPUT_PATH}
diff -Naur opencv-2.4.8-orig/CMakeLists.txt opencv-2.4.8/CMakeLists.txt
--- opencv-2.4.8-orig/CMakeLists.txt	2014-04-22 20:27:23.439800000 +0400
+++ opencv-2.4.8/CMakeLists.txt	2014-04-22 20:23:51.675800000 +0400
@@ -104,20 +104,6 @@
   endif()
 endif()
 
-# Add these standard paths to the search paths for FIND_PATH
-# to find include files from these locations first
-if(MINGW)
-  if(EXISTS /mingw)
-      list(APPEND CMAKE_INCLUDE_PATH /mingw)
-  endif()
-  if(EXISTS /mingw32)
-      list(APPEND CMAKE_INCLUDE_PATH /mingw32)
-  endif()
-  if(EXISTS /mingw64)
-      list(APPEND CMAKE_INCLUDE_PATH /mingw64)
-  endif()
-endif()
-
 # ----------------------------------------------------------------------------
 # OpenCV cmake options
 # ----------------------------------------------------------------------------
@@ -253,7 +239,7 @@
   endif()
 endif()
 
-if(ANDROID OR WIN32)
+if(ANDROID OR WIN32 AND NOT MINGW)
   set(OPENCV_DOC_INSTALL_PATH doc)
 elseif(INSTALL_TO_MANGLED_PATHS)
   set(OPENCV_DOC_INSTALL_PATH share/doc/opencv-${OPENCV_VERSION})
@@ -261,7 +247,7 @@
   set(OPENCV_DOC_INSTALL_PATH share/doc/opencv)
 endif()
 
-if(WIN32)
+if(WIN32 AND NOT MINGW)
   if(DEFINED OpenCV_RUNTIME AND DEFINED OpenCV_ARCH)
     set(OpenCV_INSTALL_BINARIES_PREFIX "${OpenCV_ARCH}/${OpenCV_RUNTIME}/")
   else()
diff -Naur opencv-2.4.8-orig/modules/highgui/CMakeLists.txt opencv-2.4.8/modules/highgui/CMakeLists.txt
--- opencv-2.4.8-orig/modules/highgui/CMakeLists.txt	2013-12-27 21:09:18.000000000 +0400
+++ opencv-2.4.8/modules/highgui/CMakeLists.txt	2014-04-22 21:10:23.184000000 +0400
@@ -287,7 +287,7 @@
 
 if(WIN32 AND WITH_FFMPEG)
   #copy ffmpeg dll to the output folder
-  if(MSVC64 OR MINGW64)
+  if(MSVC64)
     set(FFMPEG_SUFFIX _64)
   endif()
 
