diff --git a/conftest.py b/conftest.py
index cc3bee9..33e3aca 100644
--- a/conftest.py
+++ b/conftest.py
@@ -1,6 +1,7 @@
 """Contains fixtures common across all Pytest tests, including the ones in subdirectories."""
 
 import multiprocessing
+import sys
 
 import pytest
 
@@ -9,7 +10,9 @@ import pytest
 def mp_start_method():
     """Configures the default multiprocessing method for all tests.
 
-    The "forkserver" mode is the same as the one used by the C-Vise CLI.
+    C-Vise uses forkserver on Unix-like systems and spawn on Windows.
     """
     # Enforce the method selection, in case the test framework previously set a different one.
-    multiprocessing.set_start_method('forkserver', force=True)
+    # Windows Python does not provide "forkserver"; use "spawn" there.
+    start_method = 'spawn' if sys.platform == 'win32' else 'forkserver'
+    multiprocessing.set_start_method(start_method, force=True)
diff --git a/cvise-cli.py b/cvise-cli.py
index 1e0292c..08b9ce8 100644
--- a/cvise-cli.py
+++ b/cvise-cli.py
@@ -451,11 +451,14 @@ def do_reduce(args):
         args.interestingness_test = script.name
     assert args.interestingness_test
 
-    # Use forkserver to avoid potential problems due to multi-threading, and to reduce the memory usage in workers.
-    # Preloading is used as a speedup, so that every worker doesn't need to execute all import statements on startup.
-    multiprocessing.set_start_method('forkserver')
-    multiprocessing.set_forkserver_preload(['__main__'] + list(sys.modules.keys()))
-    multiprocessing.forkserver.ensure_running()
+    # Windows Python does not support "forkserver", so use "spawn" there.
+    # On Unix-like systems, keep using forkserver for lower memory usage and thread-safety.
+    start_method = 'spawn' if sys.platform == 'win32' else 'forkserver'
+    multiprocessing.set_start_method(start_method)
+    if start_method == 'forkserver':
+        # Preload modules as a startup optimization for forkserver workers.
+        multiprocessing.set_forkserver_preload(['__main__'] + list(sys.modules.keys()))
+        multiprocessing.forkserver.ensure_running()
 
     try:
         with testing.TestManager(
