--- a/cvise/utils/fileutil.py
+++ b/cvise/utils/fileutil.py
@@ -215,7 +215,9 @@ def _replace_file_test_case_atomically(source: Path, destination: Path, move:
             shutil.move(source, tmp_path)
         else:
             shutil.copy2(source, tmp_path)
-        tmp_path.rename(destination)
+        # On Windows, Path.rename() fails if destination already exists.
+        # os.replace() provides overwrite semantics while remaining atomic on same filesystem.
+        os.replace(tmp_path, destination)
