--- a/cvise/utils/process.py
+++ b/cvise/utils/process.py
@@ -10,6 +10,7 @@ import multiprocessing.managers
 import os
 import queue
 import shlex
+import shutil
 import subprocess
 import threading
 import time
@@ -285,6 +286,9 @@ class ProcessEventNotifier:
     ) -> tuple[bytes, bytes, int]:
         if shell:
             assert isinstance(cmd, str)
+
+        if isinstance(cmd, str) and os.path.splitext(cmd)[1] == '.sh':
+            cmd = [shutil.which('bash'), cmd]
 
         proc = subprocess.Popen(
             cmd,
