--- glsl-optimizer/CMakeLists.txt.orig	2015-07-19 12:59:40.145662300 +0300
+++ glsl-optimizer/CMakeLists.txt	2015-07-19 13:01:15.179097900 +0300
@@ -41,7 +41,10 @@
     # OSX-specific build requirements
     find_library(OpenGL_LIBRARY OpenGL )
     target_link_libraries(glsl_test ${OpenGL_LIBRARY})
-endif(${CMAKE_SYSTEM_NAME} MATCHES "Darwin")
+elseif(MINGW)
+    find_package(OpenGL)
+    target_link_libraries(glsl_test ${OPENGL_gl_LIBRARY})
+endif()
 
 file(GLOB glslopt_sources contrib/glslopt/*.cpp)
 add_executable(glslopt ${glslopt_sources})
