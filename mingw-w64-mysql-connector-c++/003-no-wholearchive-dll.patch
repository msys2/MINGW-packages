diff -bur mysql-connector-c++-9.6.0-src-orig/cmake/libutils/merge_archives.cmake.in mysql-connector-c++-9.6.0-src/cmake/libutils/merge_archives.cmake.in
--- mysql-connector-c++-9.6.0-src-orig/cmake/libutils/merge_archives.cmake.in	2026-01-25 06:23:25.234556900 -0700
+++ mysql-connector-c++-9.6.0-src/cmake/libutils/merge_archives.cmake.in	2026-01-25 06:25:27.564846500 -0700
@@ -227,7 +227,11 @@
       if(item STREQUAL "<LIBS>")
         foreach(lib ${LIBS})
           #message("-- adding lib: ${lib}")
-          if(NOT TOOLSET_MSVC)
+          # Check if this is a DLL import library (.dll.a)
+          if(lib MATCHES "\\.dll\\.a$")
+            # Import libraries should NOT be wrapped with --whole-archive
+            list(APPEND link_line ${lib})
+          elseif(NOT TOOLSET_MSVC)
             list(APPEND link_line
               "-Wl,--whole-archive" ${lib} "-Wl,--no-whole-archive")
           else()
