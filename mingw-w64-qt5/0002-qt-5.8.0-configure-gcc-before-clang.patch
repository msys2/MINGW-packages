--- x86_64/qtbase/configure.bat.orig	2017-05-26 14:43:31.000000000 +0200
+++ x86_64/qtbase/configure.bat	2017-06-03 15:47:06.993162200 +0200
@@ -157,12 +157,12 @@
 if "%PLATFORM%" == "" (
     if not "%icl.exe%" == "" (
         set PLATFORM=win32-icc
-    ) else if not "%clang-cl.exe%" == "" (
-        set PLATFORM=win32-clang-msvc
     ) else if not "%cl.exe%" == "" (
         set PLATFORM=win32-msvc
     ) else if not "%g++.exe%" == "" (
         set PLATFORM=win32-g++
+    ) else if not "%clang-cl.exe%" == "" (
+        set PLATFORM=win32-clang-msvc
     ) else (
         echo Cannot detect host toolchain. Please use -platform. Aborting. >&2
         exit /b 1
