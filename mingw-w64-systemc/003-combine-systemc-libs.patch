--- a/src/CMakeLists.txt
+++ b/src/CMakeLists.txt
@@ -482,12 +482,14 @@
     else(MSVC)

         add_custom_command(
-                TARGET systemc
-                POST_BUILD
-                COMMAND ar x $<TARGET_FILE:systemc>
-                COMMAND ar x $<TARGET_LINKER_FILE:${SYSTEMC_DLL_TARGET}>
-                COMMAND del *.a
-                COMMAND ar qc $<TARGET_FILE:systemc>  *.o *.obj
-                COMMAND del *.o *.obj
+                TARGET systemc
+                POST_BUILD
+                COMMAND ${CMAKE_COMMAND}
+                    -DCOMBINE_LIB=$<TARGET_FILE:systemc>
+                    -DIMPORT_LIB=$<TARGET_LINKER_FILE:${SYSTEMC_DLL_TARGET}>
+                    -DCMAKE_AR=${CMAKE_AR}
+                    -DCMAKE_RANLIB=${CMAKE_RANLIB}
+                    -P ${CMAKE_SOURCE_DIR}/cmake/CombineSystemCLibs.cmake
+                WORKING_DIRECTORY ${CMAKE_CURRENT_BINARY_DIR}
                 COMMENT "Combining SystemC libs..."
         )
