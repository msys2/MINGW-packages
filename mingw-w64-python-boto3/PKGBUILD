# Maintainer: Sarah Ottinger <schalaalexiazeal@gmail.com>

_realname='boto3'
pkgbase="mingw-w64-python-${_realname}"
pkgname="${MINGW_PACKAGE_PREFIX}-python-${_realname}"
pkgver=1.43.101
pkgrel=1
pkgdesc='The AWS SDK for Python (mingw-w64)'
arch=('any')
mingw_arch=('ucrt64' 'clang64' 'clangarm64')
url='https://github.com/boto/boto3'
msys2_repository_url='https://github.com/boto/boto3'
msys2_changelog_url='https://github.com/boto/boto3/blob/develop/CHANGELOG.rst'
msys2_references=(
  'anitya: 29737'
  'archlinux: python-boto3'
  'aur:' # ignore python-boto3-git
  'gentoo: dev-python/boto3'
  'purl: pkg:pypi/boto3'
)
license=('spdx:Apache-2.0')
depends=(
  "${MINGW_PACKAGE_PREFIX}-python-botocore"
  "${MINGW_PACKAGE_PREFIX}-python-jmespath"
  "${MINGW_PACKAGE_PREFIX}-python-s3transfer"
)
makedepends=(
  "${MINGW_PACKAGE_PREFIX}-python-build"
  "${MINGW_PACKAGE_PREFIX}-python-installer"
  "${MINGW_PACKAGE_PREFIX}-python-setuptools"
)
source=(
  "https://pypi.org/packages/source/${_realname::1}/${_realname}/${_realname}-${pkgver}.tar.gz"
)
sha256sums=('49f3eb750f70e050df9929a7e9392e67896c97d7d0a448f13ed3354c634268bd')

build() {
  cp -r "${_realname}-${pkgver}" "build-${MSYSTEM}" && cd "build-${MSYSTEM}"

  python -m build --wheel --skip-dependency-check --no-isolation
}

package() {
  cd "build-${MSYSTEM}"

  MSYS2_ARG_CONV_EXCL="--prefix=" \
    python -m installer --prefix="${MINGW_PREFIX}" --destdir="${pkgdir}" dist/*.whl

  install -Dm644 LICENSE -t "${pkgdir}${MINGW_PREFIX}/share/licenses/python-${_realname}"
}
