diff --git a/CMakeLists.txt b/CMakeLists.txt
index 8b5af3038..38d5c88b7 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -90,7 +90,10 @@ if(NOT ONNX_NAMESPACE)
   set(ONNX_NAMESPACE "onnx")
 endif()

-if(MSVC)
+if(WIN32 AND NOT MSVC)
+  # MinGW needs ONNX dllexport when static objects are linked into torch_cpu.dll.
+  set(ONNX_API_DEFINE "-DONNX_API=__declspec(dllexport)")
+elseif(MSVC)
   if(NOT ONNX_DISABLE_EXCEPTIONS)
     string(APPEND CMAKE_CXX_FLAGS " /EHsc /wd26812")
     string(APPEND CMAKE_C_FLAGS " /EHsc /wd26812")
@@ -490,6 +493,8 @@ if(MSVC)
   else()
     set(ONNX_API_DEFINE "-DONNX_API=")
   endif()
+elseif(WIN32)
+  set(ONNX_API_DEFINE "-DONNX_API=__declspec(dllexport)")
 else()
   # On non-Windows, hide all symbols we don't need
   set(ONNX_API_DEFINE "-DONNX_API=__attribute__\(\(__visibility__\(\"default\"\)\)\)")
@@ -521,6 +526,7 @@ if(CMAKE_SYSTEM_NAME STREQUAL "AIX")
 else()
   add_library(onnx ${ONNX_SRCS})
 endif()
+target_compile_definitions(onnx PRIVATE ${ONNX_API_DEFINE})
 set_target_properties(onnx PROPERTIES CXX_VISIBILITY_PRESET hidden)
 set_target_properties(onnx PROPERTIES VISIBILITY_INLINES_HIDDEN ON)

diff --git a/onnx/checker.h b/onnx/checker.h
index 502a22357..bcf36a27f 100644
--- a/onnx/checker.h
+++ b/onnx/checker.h
@@ -6,6 +6,9 @@

 #include <stdexcept>
 #include <string>
+#ifdef OPTIONAL
+#undef OPTIONAL
+#endif
 #include <unordered_map>
 #include <unordered_set>
 #include <utility>
diff --git a/onnx/common/path.h b/onnx/common/path.h
index 6ded89876..eea342371 100644
--- a/onnx/common/path.h
+++ b/onnx/common/path.h
@@ -7,6 +7,9 @@
 #pragma once

 #include <string>
+#ifdef OPTIONAL
+#undef OPTIONAL
+#endif
 #ifdef _WIN32
 // windows.h has preproc definitions for min and max, which prevents from using std::min and std::max.
 //  defining NOMINMAX disables the preproc macro.
diff --git a/onnx/defs/schema.h b/onnx/defs/schema.h
index acf3aac7e..da5ad475e 100644
--- a/onnx/defs/schema.h
+++ b/onnx/defs/schema.h
@@ -874,7 +874,7 @@
 /**
  * @brief A registry to hold all the operator schemas.
  */
-class OpSchemaRegistry final : public ISchemaRegistry {
+class ONNX_API OpSchemaRegistry final : public ISchemaRegistry {
  public:
   // A singleton class to store domain to min/max op_set version map, as well as
   // domain to last-release op_set version map.
diff --git a/onnx/onnx_pb.h b/onnx/onnx_pb.h
index 0aab3e261..3780d6e32 100644
--- a/onnx/onnx_pb.h
+++ b/onnx/onnx_pb.h
@@ -8,7 +8,7 @@
 // Defines ONNX_EXPORT and ONNX_IMPORT. On Windows, this corresponds to
 // different declarations (dllexport and dllimport). On Linux/Mac, it just
 // resolves to the same "default visibility" setting.
-#if defined(_MSC_VER)
+#if defined(_WIN32)
 #if defined(ONNX_BUILD_SHARED_LIBS) || defined(ONNX_BUILD_MAIN_LIB)
 #define ONNX_EXPORT __declspec(dllexport)
 #define ONNX_IMPORT __declspec(dllimport)
@@ -41,11 +41,13 @@
 //
 // This solution is similar to
 // https://github.com/pytorch/pytorch/blob/master/caffe2/core/common.h
+#ifndef ONNX_API
 #if defined(ONNX_BUILD_SHARED_LIBS) || defined(ONNX_BUILD_MAIN_LIB)
 #define ONNX_API ONNX_EXPORT
 #else
 #define ONNX_API ONNX_IMPORT
 #endif
+#endif
 
 #ifdef ONNX_ML
 #include "onnx/onnx-ml.pb.h"
