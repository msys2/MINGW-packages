--- a/Makefile.PL
+++ b/Makefile.PL
@@ -11,7 +11,7 @@
 use File::Path 2.06 qw( make_path remove_tree );
 
 BEGIN {
-    $::is_Win32 = ($^O =~ /Win32/);
+    $::is_Win32 = ($^O =~ /Win32/ && $Config{cc} eq 'cl');
     if ($::is_Win32) {
         $DEVNULL = 'DEVNULL';
     }
