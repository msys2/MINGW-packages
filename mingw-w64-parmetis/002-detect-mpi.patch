diff -urN parmetis-4.0.3.orig/CMakeLists.txt parmetis-4.0.3/CMakeLists.txt
--- parmetis-4.0.3.orig/CMakeLists.txt	2013-03-30 22:24:49.999998400 +0600
+++ parmetis-4.0.3/CMakeLists.txt	2018-02-07 15:28:03.873789000 +0500
@@ -10,11 +10,11 @@
 
 # Search for MPI.
 # GK commented this out as it seems to be creating problems
-# include(FindMPI)
-# if(NOT MPI_FOUND)
-#   message(FATAL_ERROR "mpi is not found")
-# endif()
-# set(CMAKE_C_FLAGS "${CMAKE_C_FLAGS} ${MPI_COMPILE_FLAGS}")
+ include(FindMPI)
+ if(NOT MPI_FOUND)
+   message(FATAL_ERROR "mpi is not found")
+ endif()
+ set(CMAKE_C_FLAGS "${CMAKE_C_FLAGS} ${MPI_COMPILE_FLAGS}")
 
 # Prepare libraries.
 if(SHARED)
