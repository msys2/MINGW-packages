--- Python-3.7.4/Lib/venv/__init__.py	2019-07-20 20:56:20.832694700 -0400
+++ Python-3.7.4/Lib/venv/__init__2052.py	2019-07-21 09:53:34.624334500 -0400
@@ -283,8 +283,16 @@
         # We run ensurepip in isolated mode to avoid side effects from
         # environment vars, the current directory and anything else
         # intended for the global Python environment
+        # Ensurepip is giving Attribute errors, but it seems to work
+        # after the third time, and then forcing a reinstall:
+        # https://github.com/pypa/pip/issues/5820
         cmd = [context.env_exe, '-Im', 'ensurepip', '--upgrade',
                                                     '--default-pip']
+        subprocess.run(cmd, stderr=subprocess.DEVNULL)
+        subprocess.run(cmd, stderr=subprocess.DEVNULL)
+        subprocess.run(cmd, stderr=subprocess.DEVNULL)
+        cmd = [context.env_exe, '-Im', 'pip', 'install', '-U',
+               '--force-reinstall', 'pip']
         subprocess.check_output(cmd, stderr=subprocess.STDOUT)
 
     def setup_scripts(self, context):
