
_realname=sed
pkgbase=mingw-w64-uutils-${_realname}
pkgname=("${MINGW_PACKAGE_PREFIX}-uutils-${_realname}")
pkgver=0.2.0
pkgrel=1
pkgdesc="Rust rewrite of sed (mingw-w64)"
arch=('any')
mingw_arch=('ucrt64' 'clang64' 'clangarm64')
url='https://github.com/uutils/sed'
msys2_repository_url="${url}"
msys2_references=(
)
license=('spdx:MIT')
makedepends=("${MINGW_PACKAGE_PREFIX}-rust")
source=(
  # missing tag "${url}/archive/refs/tags/${pkgver}.tar.gz"
  "https://static.crates.io/crates/sed/sed-${pkgver}.crate"
)
sha256sums=('19bce1f582ae9c42585d227c1d18cd909e9b532f471dffb386333d4685fa8cab')

prepare() {
  cd "${_realname}-${pkgver}"
  cargo fetch --locked --target "${RUST_CHOST}"
}

build(){
  cd "${_realname}-$pkgver"
  cargo build --release --frozen
}

package() {
  cd "${_realname}-$pkgver"
  install -Dm755 "target/release/${_realname}" "${pkgdir}${MINGW_PREFIX}/bin/uu-${_realname}"
  install -Dm644 LICENSE -t "${pkgdir}${MINGW_PREFIX}/share/licenses/uutils-${_realname}"
}
